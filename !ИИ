везде в глобальных переменных используем префикс A
в переменных используем понятные названия в паскалькейс, кроме кратких временных переменных (таколв стиль проекта):
везде в локальныех переменных как правила используются короткие наименования, где это особенно не заптрудняет чтение:
в счетчиках циклов (i, j,k, m, l избегаем тк похожа на 1)
временная строка - st, st2, st3
временные булевы переменные - b, b1...
временный tvardynarray - va
временный tvardynarray2 - va2, va21..
временный tnamedarr - na, na1...
однако и временные переменные (кроме обычно счетчиков циклов, если в них нет сложной  логики,  где  название  переменной
будет важным для понимания), особенно если они используются несколько раз, можно переименовать в понятные  наименования.
например строка гернерируется сообщение из нескольких промежуточных, промежуточные могут быть st,  st1,  s2  а  итоговая
например ErrorMessage. или например испольуются массивы для получения разных данных в одной функции (а чаще  даже  одних
данных), их тоже лучше назвать осмысленно. неосмысленно именно временные счетчики циклов,  временные  строки,  остальное
все же лучше называть понятно. те сть:
Все неосмысленные имена замененить на понятные (кроме i, j, b, st и т.п., которые допустимы как временные)

проставляем const в парамерах методов/функций

проставляем комментарии, где критично краткие к крду, ко всем методам/свойствам в интерфейсе  выше  описания  метода,  в
реализации такой же после описания метода, в реализации можно ниже добавить такие же однострочные комментарии например с
описанием параметров метода

все комментарии после // без отступа и с маленькой буквы.

там где используются не разово массивы tvardynarray / tvardynarray2 с большим количествои именованных столбцов  например
при выгрузке из запроса скл, лучше переделывать на  tnamedarr,  но  не  использовать  константы  для  названий  -  везде
прописыфваем поля в na.G(i 'fiels_name');

где возможно корректно сдлеать публичные мeтоды свойствами, стоит это реализовать

внутренние методы/функции/переменные с некорректными английскими названиями лучше скорректировать

форматирование:
стиль begin/end на одной строке:
if b then begin
  ...
end
else
  ...

if b then begin
  ...
end
else begin
  ...
end;


длинные мноргомерные массивы  TVarDynArray/TVarDynArray2  разбиваем  построчно,  при  небольшом  количестве  значений  с
трокковой длине получившегося результатата, особенно вы вазове в функции, лучше оставить в строке. пример:
func( select name from fbl here a = :p1$i and p2$s, [2, 'aaaaaaaaaaa']);
но если выражение получается длиным (но  не  80  символов  а  порядка  200,  или  например  просто  запрос  сложный  при
использовании скл, или очень много элементов в массиве в,Ю то разобьем:
    TFrmBasicInput.ShowDialogDB(AOwner, AFormType, DefOpts, AMode, AId, 'pnl_ref_ops_painting', 'Производственные операции по лакокраске', 700, 100,
      [['name$s', cntEdit, 'Наименование','1:400::T'],
       ['norm$f', cntNEdit, 'Норма, мин.', '0.06:600:2:N'],
       ['active$i', cntCheckX, 'Используется']],
      [['caption dlgedit dlgactive']]
    );


-----------------------------------------------------------------------------------------------------------------------------------
для временных например выбора значения из масиива и тп операций использовать методы uString, при наличии хелперов  лучше
использовать их.