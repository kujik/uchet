!алгоритмы.txt - описание алгоритмов бизнес-логики проекта Учет
=================================================================
Файл для накопления описаний "как это на самом деле работает" - по мере
разбора конкретных модулей. Ссылки на код даются по состоянию на момент
записи раздела - при существенной переработке модуля раздел нужно сверить
заново.


РАЗДЕЛ 1. СМЕТЫ: создание, синхронизация при заказах, история изменений
=================================================================
(записано 05.09.2026, по итогам разбора работы со сметами нестандартных
изделий новых (26) заказов - uOrders.pas, uFrmOWOrder.pas, d_orders.sql,
d_estimates.sql, uFrmOGedtEstimate.pas, uFrmOWrepEstimateChanges.pas)


1.1. Два разных вида сметы - смета-эталон и смета позиции заказа
-----------------------------------------------------------------
В таблице estimates запись всегда относится ровно к одному из двух объектов
(взаимоисключающе):
  - смета-эталон стандартного изделия      - estimates.id_std_item заполнен,
                                              id_order_item = null;
  - смета конкретной позиции заказа (слеша) - estimates.id_order_item
                                              заполнен, id_std_item = null.

Это ДВЕ РАЗНЫЕ записи с разными id, даже если по смыслу вторая - это просто
копия первой. Путаница "почему я отредактировал смету, а в истории/журнале
её не видно" почти всегда сводится к тому, что редактировалась одна запись
estimates, а смотрели/ждали изменений в другой (см. 1.5 и 1.6).

Состав сметы хранится в estimate_items (позиции сметы: группа, наименование,
единица, qnt1 - количество на единицу изделия, qnt/qnt_itm - пересчитанное
на количество в заказе/в ИТМ).

Признаки на estimates:
  - isempty = 1  - "без сметы" (отметка, что смета для этого изделия не
                   предусмотрена никогда, не путать с "ещё не загружена");
  - dt           - дата, проставляется КАЖДЫЙ раз при успешном сохранении
                   через TOrders.ApplyEstimateArray, даже если состав сметы
                   фактически не менялся (см. 1.7 - важная ловушка).


1.2. Стандартные и нестандартные изделия, старый и новый (26) формат заказов
-----------------------------------------------------------------------------
or_std_items.id_or_format_estimates определяет принадлежность изделия:
  - > 0   - настоящее стандартное изделие, входит в конкретный формат/
            подгруппу сметных эталонов (or_format_estimates);
  - -1    - нестандартное изделие ПРОИЗВОДСТВА (тип П) нового (26) формата;
  - -2    - нестандартное изделие ОТГРУЗКИ (тип О) нового (26) формата;
  - -3    - нестандартное изделие ПОЛУФАБРИКАТА (тип ПФ) нового (26) формата;
  - 0     - старый формат (до 26), нестандартное изделие без деления на
            группы (создаётся через старую p_CreateOrStdItem_Nstd).

orders.std_item_type (см. константы STDITEM_TYPE_PRODUCTION=0/
STDITEM_TYPE_SHIPMENT=1/STDITEM_TYPE_SEMIPRODUCT=2 в uOrders.pas) - тип
самого заказа. cOrderNew26TypeID - порог id заказов нового формата (заказы
с id >= этого порога считаются заказами формата 2026).

Новое нестандартное наименование в заказе типа П создаётся процедурой
p_create_or_std_item_nonstandard_new_format (d_orders.sql) и заводит СРАЗУ
ДВЕ парных записи в or_std_items:
  - группа -1 (id = p_id_item) - "эталон" для производства; смета к ней
    НЕ создаётся автоматически - её должен вручную заполнить пользователь
    (редактирование см. 1.4, редирект для типа П);
  - группа -2 - "эталон" для отгрузки этого же нестандартного изделия;
    для неё смета создаётся СРАЗУ и автоматически - из одной позиции,
    которая ссылается на изделие группы -1 (в количестве 1 шт., единица
    id_unit=1); эта смета предназначена только для копирования в позиции
    отгрузочных заказов и открывается только на просмотр (см. 1.4).
Для заказа типа О (отгрузка) своё нестандартное наименование завести
нельзя - допустимо только уже существующее (в паре) для родительского
производственного заказа (см. p_get_or_std_item_id_nonstandard_shipment).
Для типа ПФ (полуфабрикат) создаётся одна запись в группе -3, без пары.

ВАЖНО (исправлено 05.09.2026): order_items.id_std_item нестандартной позиции
ОТГРУЗОЧНОГО заказа должен ВСЕГДА указывать на группу -2 этого наименования,
а не на -1 (группу производства) - независимо от того, как позиция попала в
заказ. Раньше (см. TFrmOWOrder.SaveOrderItems, uFrmOWOrder.pas) резолвинг
id_std_item через p_get_or_std_item_id_nonstandard_shipment выполнялся ТОЛЬКО
если имя позиции менялось (IsNameChanged) - а при автосоздании отгрузочного
заказа на основании производственного (см. FCreateShipmentFromProductionId,
1.4b) состав копируется как отдельные НОВЫЕ позиции, но имя НЕ меняется, из-за
чего id_std_item копировался как есть с позиции производственного заказа,
т.е. оставался равен -1. Такая позиция навсегда оставалась привязанной к
чужой (производственной) группе - в т.ч. это ломало пересчёт смет по 1.4
(RefreshEstimatesToOrder брал бы для неё детальный эталон производства, а не
фиксированную "1 шт." отгрузки). Исправлено: резолвинг в группу -2 теперь
выполняется для ЛЮБОЙ нестандартной позиции заказа типа О нового формата,
даже если имя не менялось.

ЕЩЁ ОДНО ИСПРАВЛЕНИЕ (05.09.2026), там же рядом: автоматическая смета-ссылка группы -2 (см. выше -
1 позиция, кол-во 1, ссылка на изделие -1) раньше создавалась p_CreateEstimateItem вообще БЕЗ ГРУППЫ
(id_group передавался как null) и БЕЗ ПРЕФИКСА в наименовании (просто "голое" имя изделия). Правильно -
группа "Готовые изделия" (bcad_groups, у неё is_production = 1) и наименование с префиксом подгруппы -1
('НСТД.П_' || имя) - именно так, аналогично, эта смета-ссылка уже создавалась для НАСТОЯЩИХ стандартных
изделий (см. TFrmODedtOrStdItem.CreateSelfSmeta, uFrmODedtOrStdItem.pas) - там группа была захардкожена
в Delphi константой BCAD_GROUP_FINISHED_ITEMS = 104, а префикс вычислялся отдельной функцией
GetPrefixedName. Обе реализации (для стандартных и для нестандартных изделий) объединены в одну общую
хранимую процедуру p_create_shipment_estimate_from_production_item (d_estimates.sql) - группа ищется по
признаку bcad_groups.is_production (сейчас всегда = 1, но вынесено в параметр на случай появления в
будущем разных групп для стандартных/нестандартных готовых изделий), префикс - по or_format_estimates.
prefix переданной производственной подгруппы (для нестандартных изделий это подгруппа -1 = 'НСТД.П').

ДОИСПРАВЛЕНИЕ (05.09.2026): после внедрения p_create_shipment_estimate_from_production_item группа
проставлялась верно (104), но префикс в наименовании всё равно не появлялся никогда - ни разу, ни в одном
тесте. Причина - ошибка в самой процедуре (d_estimates.sql): условие вычисления итогового имени было
  v_pname := case when v_prefix is not null and v_prefix <> '' then v_prefix || '_' || p_production_name
             else p_production_name end;
а в Oracle пустая строка '' - это NULL, и сравнение любого непустого значения с NULL через <> всегда даёт
NULL (не TRUE) - поэтому вся ветка THEN была НЕДОСТИЖИМА, CASE всегда уходил в ELSE, и префикс не
подставлялся никогда, даже когда v_prefix успешно находился (что и сбивало с толку при диагностике -
изолированный select prefix отрабатывал верно, группа тоже была верна). Исправлено - лишняя проверка
<> '' убрана, оставлено только v_prefix is not null.


1.3. Когда вообще происходит синхронизация смет заказа
---------------------------------------------------------
Точка входа - TFrmOWOrder.SaveEstimates (uFrmOWOrder.pas), вызывается при
каждом сохранении заказа (кроме шаблонов и удаления):
  - если статус заказа "Запущен" или "Утверждён" (ORDER_ID_STATUS_STARTED/
    ORDER_ID_STATUS_APPROVED) - обновляются сметы ПО ВСЕМ позициям заказа
    (RefreshEstimatesToOrder(ID, [], True));
  - иначе (черновик) - обновляются только позиции, попавшие в
    FItemsToEstimateChange - это позиции, которые были ВСТАВЛЕНЫ заново,
    либо у которых изменилось поле, помеченное тегом "est" в описании
    грида (Frg1.Opt.SetFields в TFrmOWOrder.PrepareFrgItems) - в частности,
    поле qnt (количество) помечено этим тегом, поэтому просто изменение
    количества в заказе-черновике ТОЖЕ вызывает пересинхронизацию сметы
    именно этой позиции (но см. 1.7 - это не значит запись в лог).

Таким образом сохранение заказа МОЖЕТ трогать сметы позиций, которые
пользователь напрямую не редактировал - просто потому что заказ уже
запущен/утверждён и обновляются все позиции, либо потому что qnt помечен
как "est"-поле.


1.4. TOrders.RefreshEstimatesToOrder и TOrders.LoadEstimate
--------------------------------------------------------------
RefreshEstimatesToOrder проходит по позициям заказа и для каждой вызывает
LoadEstimate(..., Silent=True, ...):
  - настоящее стандартное изделие (std=1)              - как раньше,
    копирование из сметы-эталона стандартного изделия;
  - нестандартное, НЕ purchase (resale<>1):
      - если формат нового (26) заказа И тип заказа П либо О
        (LUseStdLikeEstimate=True; для ПФ - НЕ применяется, там всегда
        старое поведение) И у позиции есть id_std_item (значит, привязана
        к группе -1/-2) - вызывается LoadEstimate с
        ForceEstimateFromStdItem=True: смета копируется из сметы-эталона
        стандартного изделия (группы -1 для П, -2 для О), а не
        пересчитывается по количеству;
      - иначе (старый формат либо ПФ) - QntChanged=True: состав сметы не
        трогается, просто пересчитываются количества (qnt/qnt_itm) по
        текущему количеству позиции в заказе (p_CorrectEstimateQnt).
  - изделие Д/К (resale=1, покупное) - обычная загрузка по наименованию.

LoadEstimate (uOrders.pas) - универсальная точка входа что для сметы
стандартного изделия (IdStdItem<>null), что для сметы позиции заказа
(IdOrderItem<>null). Ключевой момент для нового (26) формата: для
НЕСТАНДАРТНОГО изделия типа П (std=0, ParentIdStdItem указывает на группу
-1) интерактивное редактирование сметы позиции заказа (когда пользователь
кликает "Смета" в гриде заказа) ПЕРЕНАПРАВЛЯЕТСЯ на редактирование сметы-
эталона стандартного изделия (той самой группы -1) - то есть открывается
и правится ДРУГАЯ запись estimates (id_std_item=..., id_order_item=null),
а не смета самой позиции заказа. Синхронизация из отредактированного
эталона В САМУ позицию заказа (копированием) происходит ОТДЕЛЬНО, только
при следующем сохранении заказа, через RefreshEstimatesToOrder (см. 1.3) -
НЕ сразу после сохранения диалога сметы эталона.
Для типа О - наоборот, редиректа нет: смета уже скопирована автоматически
(из группы -2) и открывается только на просмотр (LReadOnlyEstimate).
Внутри RefreshEstimatesToOrder этот редирект НЕ применяется
(ForceEstimateFromStdItem=True отключает его) - там как раз и нужно
скопировать смету-эталон в позицию заказа, а не открыть сам эталон.

Если у стандартного изделия (группы -1) ещё вообще нет сметы-эталона (то
есть пользователь её ни разу не заполнял) - LoadEstimate выходит без
всякого действия (ни в диалоговом, ни в тихом режиме) - копировать
реально нечего (правка от 05.09.2026 - раньше в тихом режиме создавалась
"фантомная" пустая запись сметы позиции заказа, см. раздел "Известные и
исправленные проблемы" ниже).


1.4a. Насколько строго "копия" совпадает с "эталоном"
-----------------------------------------------------------
По составу - да, полностью: p_copy_std_estimate_to_order_item (d_estimates.sql)
построчно копирует из эталонной сметы в позиционную группу, наименование, единицу,
qnt1/qnt1_itm (количество НА ЕДИНИЦУ изделия) и ссылку id_or_std_item/or_std_item_cnt
БЕЗ ИЗМЕНЕНИЙ, а вот итоговые qnt/qnt_itm пересчитывает как qnt1 * количество_в_заказе
(ceil до 3 знаков). То есть единственное отличие копии от эталона - количество,
пересчитанное на количество этой позиции в заказе; перечень и состав позиций сметы
всегда идентичны эталону НА МОМЕНТ последнего копирования.

Но "всегда" здесь означает "по состоянию на последнее сохранение заказа", а не
непрерывно: копирование происходит только внутри RefreshEstimatesToOrder, то есть при
сохранении заказа (см. 1.3-1.4). Если эталон отредактировали, а заказ после этого ещё
не пересохраняли - копия в позиции заказа физически устарела и отличается от эталона,
пока заказ не сохранят снова.

Модель "эталон + копия в позиции заказа" действует ТОЛЬКО для нестандартных изделий
нового (26) формата типа П и О (и для настоящих стандартных изделий, где "эталон" -
это сама запись стандартного изделия в справочнике). У нестандартных изделий СТАРОГО
формата этой модели нет вообще - там единственная смета, привязанная прямо к позиции
заказа, которую пользователь редактирует непосредственно (состав задаётся вручную,
при сохранении заказа лишь пересчитывается итоговое количество - QntChanged).
Для типа ПФ (полуфабрикат) запись-эталон (группа -3) в справочнике заводится так же,
как для П/О (см. 1.2), но принудительное копирование из неё в RefreshEstimatesToOrder
НЕ включено - LUseStdLikeEstimate явно исключает STDITEM_TYPE_SEMIPRODUCT (uOrders.pas,
строка ~635) - то есть для ПФ по-прежнему работает только старый пересчёт количества,
эталон существует, но никуда автоматически не копируется. Похоже на недоделанный до
конца кусок функциональности, а не осознанное решение - явного объяснения в коде нет,
стоит уточнить у разработчика при случае, не является ли это отдельным дефектом.

И ещё один нюанс: для нестандартных изделий нового формата (что П, что О) сама копия в
позиции заказа НИКОГДА не редактируется пользователем напрямую - кнопка "Смета" на
позиции заказа либо перенаправляет на эталон (тип П, см. 1.4), либо открывает копию
только на просмотр (тип О). Ручное редактирование состава всегда происходит на уровне
эталона (группа -1/-2 в справочнике стандартных изделий), а не на уровне позиции
заказа - позиция заказа только принимает то, что туда скопируют при сохранении заказа.


1.4b. Немедленная синхронизация копии сметы позиции заказа (с 05.09.2026)
------------------------------------------------------------------------------
До этой правки собственная смета позиции заказа (копия, см. 1.1) для
нестандартного изделия типа П синхронизировалась с эталоном ТОЛЬКО при
следующем сохранении заказа целиком (TOrders.RefreshEstimatesToOrder, см.
1.4). То есть если открыть смету из журнала заказов (что реально означает
правку эталона, группа -1 - см. 1.4), отредактировать и сохранить, а сам
заказ при этом НЕ пересохранять - собственная копия в позиции заказа
оставалась старой/пустой до следующего сохранения заказа. Для
нестандартного изделия, которое всегда создаётся для одной конкретной
позиции заказа, откладывать синхронизацию смысла не было - в отличие от
настоящих стандартных изделий, где эталон может годами жить и
редактироваться независимо от уже оформленных (и, возможно, уже
запущенных в производство по прежней версии сметы) заказов.

Исправлено: TOrders.LoadEstimate получил новый параметр
ASyncOrderItemAfterSave: Integer = 0 (id позиции заказа); в момент
редиректа для П (см. 1.4) в него передаётся id ИМЕННО ТОЙ позиции заказа,
из которой было инициировано редактирование. Это значение прокидывается в
TEstimateApplyContext.SyncOrderItemAfterSave, и сразу после успешного
сохранения самой сметы-эталона (внутри ApplyEstimateArray, ПОСЛЕ коммита
транзакции и записи в лог изменений) немедленно выполняется тот же самый
вызов, которым раньше синхронизация делалась только при сохранении заказа -
LoadEstimate(null, IdOrderItem, null, False, False, True, False, True)
(Silent=True, ForceEstimateFromStdItem=True) - копия в позиции заказа
обновляется мгновенно, а не при следующем сохранении заказа. Побочный
эффект: т.к. эта копия сохраняется через тот же ApplyEstimateArray, у неё
тоже сразу появляется своя запись в estimate_change_log (source='4' -
"обновление из сметы стандартного изделия"), не дожидаясь пересохранения
заказа - хотя в норме смотреть стоит именно историю эталона (см. 1.6).

Вызовы RefreshEstimatesToOrder (ForceEstimateFromStdItem=True) сам редирект
для П не проходят (см. условие "not ForceEstimateFromStdItem" в 1.4), так
что ASyncOrderItemAfterSave там всегда остаётся 0 - новая логика не мешает
штатной синхронизации при сохранении заказа и не создаёт рекурсии.

Нюанс: если по этому нестандартному П-изделию уже создан связанный
отгрузочный заказ (копия позиции с тем же id_std_item - группа -1 общая
для производственного и отгрузочных заказов, см. FCreateShipmentFromProductionId
в uFrmOWOrder.pas), немедленная синхронизация распространяется ТОЛЬКО на
ту позицию заказа, ИЗ КОТОРОЙ реально было инициировано редактирование (её
id и передан в ASyncOrderItemAfterSave) - а не на все позиции во всех
заказах, ссылающихся на тот же эталон. Позиции в остальных таких заказах
по-прежнему получат актуальную копию только при следующем сохранении
СВОЕГО заказа (см. 1.3-1.4) - это сознательное ограничение, чтобы не
трогать задним числом чужие (возможно, уже выполненные/отгруженные) заказы
одним лишь фактом правки эталона из другого заказа.


1.5. TOrders.ApplyEstimateArray - что именно сохраняется в БД
-----------------------------------------------------------------
Общая функция сохранения результата (что для эталона, что для позиции
заказа - определяется контекстом Ctx, собранным в LoadEstimate):
  - создаёт/обновляет саму запись estimates (dt всегда = сегодня, см. 1.7);
  - если Ctx.IsOrItemStd (копирование из эталона) - вызывает
    p_copy_std_estimate_to_order_item(эта_смета, эталонная_смета, кол-во) -
    если у эталона своей сметы ещё нет (id эталонной сметы = null),
    процедура просто ничего не копирует (не ошибка!) - тогда после неё
    p_DeleteFreeEstimate должна убрать образовавшуюся пустую запись (но
    с 05.09.2026 этот сценарий вообще не должен возникать, см. 1.4);
  - если Ctx.QntChanged - вызывает p_CorrectEstimateQnt (пересчёт
    количеств, состав не трогается);
  - иначе (ручной ввод/загрузка нестандартной сметы) - создаёт позиции
    через p_createestimateitem по массиву Est из диалога;
  - в конце - p_DeleteFreeEstimate(id сметы): удаляет саму запись
    estimates, если в ней НЕТ ни одной строки estimate_items и isempty
    не равен 1 (в т.ч. isempty is null - см. nvl-правку от 05.09.2026).


1.6. История изменений - estimate_change_log
-----------------------------------------------
Пишется через TOrders.LogEstimateChange(AIdEstimate, ASource, AChanges) -
вызывается из ApplyEstimateArray ПОСЛЕ успешного коммита, и только если:
  - AIdEstimate <> null, и запись в estimates всё ещё существует (не была
    удалена только что отработавшей p_DeleteFreeEstimate - иначе вставка
    строки в estimate_change_log упала бы по внешнему ключу, ORA-02291);
  - AChanges <> '' (BuildEstimateDiffText нашёл реальные отличия по
    составу/количеству на единицу - qnt1) ИЛИ ASource = '0' (это самая
    первая загрузка данной сметы - EstBefore.Count = 0 - тогда пишем лог
    даже при "пустом" диффе, как факт первичной загрузки).
  - ВАЖНО: это условие проверяется НЕЗАВИСИМО от Ctx.Silent - тихий вызов
    из RefreshEstimatesToOrder тоже пишет в лог, если реально что-то
    изменилось или это первая загрузка.
  - НЕ пишется вообще, если Ctx.QntChanged = True (чистый пересчёт
    количества по старой схеме - см. явный комментарий в коде).
  - Для нового (26) формата П/О пересчёт по ForceEstimateFromStdItem не
    ставит QntChanged, НО диффится по qnt1 (кол-во на единицу в эталоне),
    которое от количества В ЗАКАЗЕ не зависит - поэтому простое изменение
    количества позиции в заказе и тут не даёт диффа и не пишется в лог,
    хотя сама функция и вызывается.
  - source - список кодов через запятую (см. TEstDlgChannel.SourceUsed в
    uFrmOGedtEstimate.pas), расшифровка в SourceCodesToText
    (uFrmOWrepEstimateChanges.pas); '0' - первичная загрузка,
    '3'/'4' проставляются в LoadEstimate по EstLogSource := IIf(IsOrItemStd,
    '4', '3') - т.е. '4' для копирования из эталона (в т.ч. для настоящих
    стандартных изделий), '3' для прямого ввода/загрузки нестандартной
    сметы или правки эталона.

Просмотр истории - uFrmOWrepEstimateChanges.pas (открывается через
Wh.ExecDialog(myfrm_Rep_EstimateChanges, ..., AId, AAddParam) - из
справочника стандартных изделий (AddParam=1, AId=id стандартного изделия)
или из журнала заказов, позиции заказа (AddParam<>1, AId=id order_items)).
ИСПРАВЛЕНО (05.09.2026): раньше этот диалог (как и диалог просмотра сметы -
uFrmOGlstEstimate.pas, действие "Просмотр", myfrm_R_Estimate) резолвил
id_estimate НАПРЯМУЮ - "select id from estimates where id_order_item = :id"
для позиции заказа, без учёта редиректа для П (см. 1.4). Из-за этого при
открытии истории или просмотра ИЗ ЖУРНАЛА ЗАКАЗОВ по позиции нестандартного
П-изделия показывалась смета/история САМОЙ ПОЗИЦИИ ЗАКАЗА, а не эталона
(группа -1) - хотя реальное редактирование (клик "Смета" в гриде заказа)
идёт именно на уровне эталона - в результате история казалась пустой или
неполной, а просмотр сметы - пустым, до следующего сохранения заказа.
Добавлена общая функция TOrders.ResolveEstimateDisplayTarget(AIdOrderItem;
out AIdStdItem) - повторяет условие редиректа из LoadEstimate, но только
для определения ЦЕЛИ ПРОСМОТРА, без каких-либо действий с БД; используется
и в TFrmOWrepEstimateChanges.Prepare, и в TFrmOGlstEstimate.PrepareForm -
оба теперь при таком редиректе показывают именно эталон (с пометкой в
заголовке), и правки видны сразу, а не после сохранения заказа. Для
настоящих стандартных изделий (AddParam=1) и для нестандартных изделий
старого формата/типа О/ПФ поведение не менялось - редиректа там не было
и нет. См. также 1.4b - синхронизация собственной копии позиции заказа
теперь тоже происходит немедленно, а не только при сохранении заказа.


1.7. "Дата сметы" в журнале заказов - не то же самое, что история изменений
------------------------------------------------------------------------------
Столбец "дата сметы" в детальной таблице журнала заказов и v_order_items
в целом (v_order_items.dt_estimate = es.dt, d_orders.sql) - это просто
estimates.dt той сметы, что привязана к id_order_item данной позиции.
А estimates.dt проставляется в TOrders.ApplyEstimateArray БЕЗУСЛОВНО,
сегодняшним числом, при КАЖДОМ успешном сохранении этой сметы - включая
чистый пересчёт количества (QntChanged) и повторное копирование из
эталона без изменений состава. То есть "дата сметы" отвечает на вопрос
"когда в последний раз заказ сохранялся и синхронизация трогала смету
этой позиции", а НЕ на вопрос "когда состав сметы содержательно менялся" -
на второй вопрос отвечает именно история изменений (estimate_change_log,
см. 1.6), а не эта дата. Не стоит делать выводы об изменении состава
сметы только по изменившейся дате в журнале заказов.


1.8. Удаление заказа (с 05.09.2026)
---------------------------------------------------------
Кнопка/пункт меню "Удалить заказ" (константа mbtOrderDelete = 1004, TFrmOWOrder,
uFrmOWOrder.pas) - видна только в Mode=fEdit, не для шаблона, при наличии роли
rOr_D_Order_Del (см. SetButtons); дублируется и в заголовке диалога, и в меню грида
изделий заказа (FrgItemsButtonClick) - оба места вызывают один и тот же
TitleButtonClick(mbtOrderDelete). Поведение зависит от статуса заказа (id_status) на
момент нажатия:

- ORDER_ID_STATUS_DRAFT (заказ в оформлении) - ФИЗИЧЕСКОЕ удаление записи заказа из БД.
  После подтверждения ("Заказ будет удален безвозвратно. Продолжить?") выполняется
  delete from orders (TFrmOWOrder.DeleteOrderPhysically) в отдельной пакетной транзакции
  (Q.QBeginTrans(True)/Q.QCommitOrRollback) - все зависимые записи (order_items, estimates
  по id_order_item, order_changes, order_complaints, or_payments, orders_add и т.п.)
  удаляются каскадно, через on delete cascade у соответствующих внешних ключей. Это не
  идёт через общий механизм ChangeOrderStatus/Save (там нет физического удаления, только
  смена статуса) - перехватывается ОТДЕЛЬНО, в самом начале TitleButtonClick, до вызова
  ChangeOrderStatus. Если delete упадёт по внешнему ключу - транзакция откатится, ошибку
  покажет сам QExecSql, диалог заказа останется открытым. Из известных НЕкаскадных ссылок
  на order_items/orders: fk_j_tasks_order_item (j_tasks.id_order_item -> order_items.id)
  переведён (05.09.2026, вручную, отдельным --!go блоком в d_references.sql) на on delete
  set null - больше не блокирует удаление заказа. Остаются некаскадными (не блокируют
  удаление ТОЛЬКО если для заказа/его позиций нет ссылающихся строк):
  fk_j_tasks_order (j_tasks.id_order -> orders.id) и fk_order_item_route_item
  (order_item_route.id_order_item -> order_items.id) - для заказа, ещё не запущенного в
  работу, обычно пустые, но не гарантированно.
- ORDER_ID_STATUS_STOPPED (заказ Остановлен) - "мягкое" удаление: после подтверждения
  ("Заказ будет помечен как "Удаленный", и останется доступным для просмотра. Продолжить?")
  статус меняется на ORDER_ID_STATUS_DELETED и заказ сохраняется обычным порядком (см.
  ChangeOrderStatus, ветка Tag = mbtOrderDelete, и общий механизм в TitleButtonClick) -
  запись в БД не удаляется, заказ остаётся доступен для просмотра.
- Любой другой статус (Утверждён/Запущен/Отклонён/уже Удалён) - действие недоступно,
  выводится "Действие недопустимо для этого заказа!" (см. ChangeOrderStatus).

См. отдельно уже существовавшую (до этой правки) кнопку/тег mbtDelete (константа = 16,
общий фреймворковый "Удалить") - для заказа она либо не показывается вовсе, либо видна
только на этапе добавления/копирования ещё не сохранённого заказа (Mode in [fAdd, fCopy]) -
с уже существующим (сохранённым) заказом в разделе mbtOrderDelete не пересекается.


1.9. Экспорт и отправка паспорта заказа (ПЗ) при запуске/изменении заказа (с 05.09.2026)
---------------------------------------------------------
Перенесена (портирована) логика TDlg_Order.ExportPassportToXLSX в новый диалог заказа
TFrmOWOrder - вызывается из SetTaskForServer (создание задачи для серверного процесса),
по тому же месту, что и в старом диалоге, но с БОЛЕЕ ИЗБИРАТЕЛЬНЫМИ условиями создания
файла (сам экспорт использует уже существующий общий метод Orders.ExportPassportToXLSX,
uOrders.pas, - никакой новой логики формирования содержимого паспорта не писалось):

- при ПЕРЕХОДЕ статуса заказа в ORDER_ID_STATUS_STARTED (Запущен) - паспорт создаётся
  (Orders.ExportPassportToXLSX(ID, LOrderPath, False, True)) и отправляется БЕЗУСЛОВНО,
  как и раньше в TDlg_Order;
- если заказ УЖЕ был в статусе Запущен и сохраняется снова (остаётся Запущен) - паспорт
  пересоздаётся, ТОЛЬКО если среди фактически изменившихся полей заказа есть хотя бы одно,
  используемое в шаблоне паспорта (тег вида #имя_поля# в самом файле шаблона). Если ни одно
  изменившееся поле не встречается в шаблоне - файл не создаётся вообще (и, соответственно,
  не будет вложен/скопирован в задачу серверным процессом при этом сохранении).

Определение набора тегов шаблона - TFrmOWOrder.NeedPassportExport (uFrmOWOrder.pas):
- набор тегов шаблона читается функцией GetXlsxTemplateTags(FileName) (uExcel2.pas,
  новая) - сканирует ПЕРВЫЙ лист файла шаблона (Module.GetReportFileXlsx('ПЗ')) через
  LibXL (без реального Excel - быстро, в отличие от Orders.ExportPassportToXLSX, который
  использует медленную COM-автоматизацию Excel через uExcel.pas/TA7Rep), ищет во всех
  текстовых ячейках подстроки вида #имя# и возвращает уникальный список найденных имён
  (без решёток, в нижнем регистре). Если файл шаблона не найден/не открылся - возвращается
  пустой массив, и NeedPassportExport на всякий случай считает, что паспорт нужен создать
  (т.е. отсутствие информации о тегах трактуется в пользу пересоздания файла);
- список ИЗМЕНИВШИХСЯ полей заказа берётся из уже существовавшей GetOrderChangedfieldNames
  (шапка заказа, тег 'chg' в DefineFields) и из новой TFrmOWOrder.GetItemsChangedFieldNames
  (табличная часть - грид изделий, тег 'chg' у колонок грида, аналогично существовавшей
  GetItemsChangesText). В шаблоне ПЗ таблица изделий представлена ОДНОЙ повторяющейся
  строкой (один и тот же набор тегов для всех позиций), поэтому проверка ведётся по
  объединённому (по всем строкам грида) списку изменившихся полей, без разбора по позициям;
- особый случай - добавление или удаление позиций в гриде изделий (сама структура таблицы
  меняется, а не значение конкретного поля) - GetItemsChangedFieldNames в этом случае
  добавляет в список фиктивное имя '*', которое NeedPassportExport трактует как изменение
  ВСЕГДА (безусловно требует пересоздания паспорта), независимо от набора тегов шаблона.

Отключаемая проверка - константа PASSPORT_EXPORT_ONLY_IF_TEMPLATE_FIELDS_CHANGED
(uFrmOWOrder.pas, = True по умолчанию). Если поставить в False - проверка по тегам
полностью отключается, и паспорт пересоздаётся и отправляется при КАЖДОМ сохранении
заказа в статусе Запущен, независимо от того, что именно изменилось (прежнее, безусловное
поведение, как в TDlg_Order) - оставлено на случай проблем с определением набора тегов.
На переход самого статуса в Запущен эта константа не влияет - паспорт создаётся и
отправляется в этом случае в любом случае, константа касается только повторных сохранений
уже Запущенного заказа.

ВАЖНЫЙ ПРИНЯТЫЙ КОМПРОМИСС (проговорено с пользователем явно): если в конкретном
сохранении паспорт не пересоздаётся (условия не сработали), временного файла паспорта не
будет, и серверный процесс НЕ включит вложение в рассылку в этот раз, даже если файл
паспорта в каталоге заказа на сервере уже существует с прошлого раза (актуальный, но
старый файл просто не пересылается повторно). Идеальным решением было бы: пересоздавать
файл только при изменении используемых полей, но ВСЕГДА отправлять/прикреплять уже
существующий файл (пересозданный или нет) - но это потребовало бы переделки самого
серверного процесса рассылки, что явно вынесено за рамки данной задачи.

Явно НЕ входит в эту правку (по решению пользователя, отдельная задача на будущее):
полный перевод самого экспорта паспорта (Orders.ExportPassportToXLSX) с медленной
COM-автоматизации Excel (uExcel.pas/TA7Rep) на быструю запись через LibXL (uExcel2.pas) -
в uExcel2.pas на данный момент реализовано только ЧТЕНИЕ (включая GetXlsxTemplateTags),
запись через LibXL ранее была начата и заброшена (см. закомментированный код в конце
файла) и не используется.


1.10. Диалог сметы (uFrmOGedtEstimate.pas): типы НП/НО, тип изделия заказа из вью,
выбор полуфабриката без префикса, вставка производственного изделия (с 05.09.2026)
---------------------------------------------------------
В связи с появлением нестандартных изделий новых типов НП/НО (id_format = -1/-2 в
or_format_estimates - см. 1.2) доработан диалог ввода/редактирования сметы:

- ВАЖНЫЙ БАГ (исправлен): v_or_std_items.type_name (d_orders.sql) для настоящих (не
  нестандартных) отгрузочных изделий возвращает ЛАТИНСКУЮ 'O' (fi.type = 1), тогда как
  в uFrmOGedtEstimate.pas сравнение шло с КИРИЛЛИЧЕСКОЙ 'О' - из-за этого заголовок
  сметы и кнопка "Выбрать производственное изделие" НИКОГДА не срабатывали для настоящих
  отгрузочных изделий (сравнение всегда давало false). Исправлено на латинскую 'O'
  везде, где сравнивается FTypeOfItem. Аналогичный баг пользователь нашел и исправил
  сам и в d_estimates.sql.
- Отдельно была опечатка в самой v_or_std_items - CASE для type_name ветвей НП/НО/НПФ
  ссылался на несуществующий алиас f.id_format (в FROM только fi) - исправлена
  пользователем вручную (должно быть fi.id_format).
- FTypeOfItem изделия заказа (AddParam = 0) раньше был захардкожен как 'И' - теперь
  берется из новой вью-колонки v_order_items.type_name (d_orders.sql), вычисляемой
  ТЕМ ЖЕ способом, что и v_or_std_items.type_name (по ee.id_format/ee.type той же
  or_format_estimates, что и у стандартного изделия заказа). Изделие заказа без
  привязки к стандартному изделию не встречается, отдельный запасной вариант не нужен.
- Кнопки "Выбрать материал"/"Выбрать полуфабрикат" показываются только при FTypeOfItem
  in ('П','НП'), кнопка "Выбрать производственное изделие" - только при FTypeOfItem in
  ('O','НO') (латинская O, см. выше) - это уже сделано пользователем самостоятельно, до
  правки type_name для изделий заказа кнопки для них вообще не показывались (тип 'И' не
  входил ни в один из этих списков).

ВАЖНО (факт, отсутствовавший в документации): при выборе полуфабриката (кнопка
"Выбрать полуфабрикат", диалог myfrm_R_OrderStdItems_SelSemiproduct) в саму строку
сметы ВСЕГДА должно попадать ГОЛОЕ наименование (or_std_items.name, без префикса
группы), даже если в списке выбора для наглядности показывается fullname (с префиксом).
БАГ (исправлен 05.09.2026): диалог выбора возвращал только fullname, и оно же
вставлялось в смету - полуфабрикат попадал в смету С ПРЕФИКСОМ. Исправлено - в диалог
выбора добавлено отдельное поле name (голое), и в смету теперь подставляется именно оно.

Также для выбора полуфабриката добавлен фильтр: если родительское изделие (то, к
которому смета) само стандартное (AddParam = 1 у диалога сметы), недопустим
НЕстандартный полуфабрикат (id_format = -3, НПФ) - см. myfrm_R_OrderStdItems_SelSemiproduct.
НПФ пока неактивен (or_format_estimates.active = 0 у id = -3), поэтому фильтр сейчас
ни на что не влияет, но поставлен заранее.

У диалога "Выбрать производственное изделие" (myfrm_R_OrderStdItems_SelProdStdItem)
попутно исправлена синтаксическая ошибка в SQL (была "and and" и пропущенное "and"
между условиями - диалог падал бы при открытии). Диалог оставлен как запасной вариант.

ДОБАВЛЕНО (05.09.2026): кнопка переименована в "Вставить производственное изделие" и
больше не открывает диалог выбора - см. TFrmOGedtEstimate.InsertProductionItemFromRow.
Производственное изделие определяется однозначно: ищется РОВНО ОДНА производственная
подгруппа (or_format_estimates, type = 0) в группе FGroupOfItem, а в ней - изделие с
ТЕМ ЖЕ ГОЛЫМ наименованием, что и у отгрузочного (родительского) изделия сметы. Если
производственных подгрупп в группе нет или больше одной, либо изделие с таким
наименованием в найденной подгруппе не найдено - выводится предупреждение, ничего не
вставляется; запасной диалог выбора НЕ открывается (по решению пользователя - такие
ситуации должны стать редкими/невозможными после приведения справочников в порядок).

ЕДИНООБРАЗИЕ ЗАГОЛОВКА (06.09.2026): у изделия заказа (AddParam = 0) первая строка
заголовка диалога раньше была общей надписью "Смета к изделию заказа:" без указания
типа. Теперь строится ТЕМ ЖЕ способом, что и у стандартного изделия (AddParam = 1) -
"Смета к <тип-фраза> заказа:", где <тип-фраза> - тот же текст по FTypeOfItem
(отгрузочному/производственному стандартному, нестандартному отгрузочному/
производственному, полуфабрикату, нестандартному изделию старого образца, либо
стандартному изделию по умолчанию), что и у стандартного изделия - т.е. единообразно
для О, П, ПФ, НО, НП, Н. FFormatCaption (группа/подгруппа/префикс) для изделия заказа
по-прежнему не добавляется - у него нет своего формата/спецификации, но собственная
идентификация позиции (наименование, а через "слеш" в его составе - и номер заказа)
по-прежнему показывается второй строкой заголовка (FName), как и раньше.

РЕДИРЕКТ ДЛЯ НСТД.П (06.09.2026): для нестандартного изделия типа П нового (26) формата
TOrders.LoadEstimate перенаправляет редактирование сметы позиции заказа на смету-эталон
стандартного изделия (or_std_items, группа -1) - диалог в этом случае открывается с
AddParam = 1 и ID = айди эталона, и до этой правки заголовок вообще не показывал, к какой
позиции заказа относится редактируемая смета (описание выше про AddParam = 1 не помогало -
у эталона нет ни своего заказа, ни слеша). Добавлен новый параметр
TOrders.LoadEstimate/AOrderContextCaption (готовая строка вида слеш - наименование - кол-во)
по исходной позиции заказа и новое поле TEstDlgChannel.OrderCaption (боковой канал,
uFrmOGedtEstimate.pas) - при редиректе строка передаётся через канал и добавляется к
заголовку в квадратных скобках после FFormatCaption. Формат строки - тот же, что уже
использовался для аналогичной надписи при ПРОСМОТРЕ сметы (см. TFrmOGlstEstimate.PrepareForm,
TOrders.ResolveEstimateDisplayTarget) - для единообразия.

ШАПКА ПРОСМОТРЩИКА СМЕТЫ (06.09.2026, uFrmOGlstEstimate.pas, myfrm_R_Estimate): раньше вся
информация (слеш заказа, наименование изделия, кол-во, признак сметы-эталона) шла ОДНОЙ
строкой без расцветки через общий динамический ярлык (Frg1.CreateAddControls(cntLabel, FCapt,
...)) внутри грида - плохо читалось. Переделано по образцу uFrmOGedtDistributeQnt.pas (см. 1.9
из более старой правки - там же объяснение, почему НЕ через FTitleTexts/CreateLabelColors, а
именно так): две статичные раскрашенные метки (lblCapt1/lblCapt2) в pnlTop формы, текст задаётся
через SetCaption2 (uLabelColors, коды цвета $BBGGRR прямо в тексте) - заказ (слеш) отдельной
строкой (lblCapt1), изделие и количество - отдельной (lblCapt2), признак смены-эталона -
заметным красным хвостом первой строки. Для отдельно просматриваемого стандартного изделия
(без позиции заказа) - только первая строка. Динамический ярлык через CreateAddControls
оставлен без изменений для другого режима этой же формы - myfrm_R_AggEstimate (общая смета по
заказу/заказам) - там шапка и раньше была одной простой строкой без смешения разнородной
информации, менять не требовалось; pnlTop показывается только для myfrm_R_Estimate.

ШАПКА РЕДАКТОРА СМЕТЫ (06.09.2026, uFrmOGedtEstimate.pas): та же переделка читаемости, что и
для просмотрщика сметы (см. выше) - раньше заголовок собирался через FTitleTexts/
CreateLabelColors (TFrmBasicEditabelGrid.PrepareForm) одной строкой без расцветки, особенно
плохо читалась ветка AddParam<>1 (смета позиции заказа) - слеш заказа и наименование изделия
шли слитно. FTitleTexts теперь не заполняется вовсе (пустой массив - безопасный no-op, см.
защиту на пустом массиве в TControlsHelper.CreateLabelColors, uForms.pas), вместо него - три
статичные раскрашенные метки lblCapt1/lblCapt2/lblCapt3 в pnlTop формы (добавлены вручную в
.dfm, pnlTop.Height=65), текст задаётся через SetCaption2 (uLabelColors) - по образцу
uFrmOGedtDistributeQnt.pas/uFrmOGlstEstimate.pas. Для сметы стандартного изделия (AddParam=1):
lblCapt1 - "Смета к: <тип-фраза>", lblCapt2 - "Изделие: <FName>    Формат: <FFormatCaption>",
lblCapt3 - только при редиректе НСТД.П (см. TEstDlgChannel.OrderCaption выше) - "Смета-эталон
нестандартного изделия - заказ, слеш: <контекст>" красным, иначе пустая строка/невидима. Для
сметы позиции заказа (AddParam<>1): lblCapt1 - "Смета к: <тип-фраза> заказа", lblCapt2 -
"Заказ, слеш: <слеш>    Изделие: <FName>", lblCapt3 не используется. Заодно запрос данных
позиции заказа в этой ветке переведён с одной конкатенированной колонки (slash || ' ' || name)
на раздельные (slash, name) - для единообразия с веткой AddParam=1, где FName и так был только
наименованием без слеша.

АЛЬТЕРНАТИВНЫЙ ПРОСМОТР СМЕТЫ (06.09.2026, uFrmOGlstEstimate.pas, myfrm_R_Estimate): в
просмотрщик сметы добавлена кнопка "Альтернативный просмотр" (грид-тулбар, тег -cBtnAltView =
-1001, по аналогии с cBtnCreateSemiproduct в uFrmOGedtEstimate.pas) - доступна ТОЛЬКО когда
просмотрщик открыт по одному слешу (FormDoc = myfrm_R_Estimate), для общей сметы заказа
(myfrm_R_AggEstimate) кнопки нет вовсе (см. условный Frg1.Opt.SetButtons в PrepareForm). По
нажатию открывается диалог РЕДАКТИРОВАНИЯ сметы (TFrmOGedtEstimate) в режиме fView (только
чтение) - напрямую, вызовом TFrmOGedtEstimate.Show(FrmMain, myfrm_Dlg_EdtEstimate, [...], fView,
ID, AddParam), в обход TOrders.LoadEstimate и бокового канала EstDlgChannel - никакой загрузки
массива/сохранения не требуется, диалог сам читает текущий состав сметы из БД (ветка
FUseInputArray = False в TFrmOGedtEstimate.PrepareForm). Если для текущего просмотра сработал
редирект на смету-эталон нестандартного изделия (FIdStdItemForEstimate <> null, см.
TOrders.ResolveEstimateDisplayTarget выше), альтернативный просмотр открывает именно эталон
(AddParam=1) - чтобы показать то же самое, что и в гриде, а не пустую/неактуальную позицию
заказа. Открытие с Mode=fView с самого начала (а не через понижение с fEdit после неудачной
попытки блокировки, как в обычном пути через TOrders.LoadEstimate) проверено безопасно: Q.DBLock
(uDBOra.pas, TmyDBOra.DBLock) при Mode не в [fNone, fEdit, fDelete] выходит сразу после
Result:=[...Mode], блокировка вообще не пытается устанавливаться.

БАГ ТИПА ИЗДЕЛИЯ ДЛЯ НСТД.О/НСТД.ПФ (06.09.2026, ИСПРАВЛЕН, d_orders.sql): после появления шапки
редактора сметы с текстовым названием типа изделия (см. выше) обнаружилось, что для настоящего
нестандартного изделия ОТГРУЗКИ (НСТД.О, заказ типа STDITEM_TYPE_SHIPMENT) и в редакторе сметы, и
при просмотре через "Альтернативный просмотр" из справочника стандартных изделий тип показывался
как НСТД.П (нестандартное производства) - хотя сама смета (состав позиций) была верной, ошибался
только текст шапки. Причина - в v_order_items.type_name и v_or_std_items.type_name (d_orders.sql):
у ВСЕХ ТРЁХ виртуальных групп нестандартных изделий (or_format_estimates.id = -1/-2/-3, см. 1.2)
столбец id_format ОДИНАКОВ и равен -1 (это FK на or_formats.id = -1 "Нестандартные изделия", общий
для всех трёх - так и задумано), а различать сами группы нужно по id (-1/-2/-3), а не по id_format.
CASE в обеих вью проверял именно ee.id_format/fi.id_format, поэтому ветка "when id_format = -1 then
'НП'" срабатывала первой для ЛЮБОЙ из трёх групп, маскируя НСТД.О и НСТД.ПФ под НСТД.П (для самой
НСТД.П это, наоборот, случайно давало верный результат). Просмотрщик сметы (uFrmOGlstEstimate.pas)
эту ошибку не показывал просто потому, что его шапка тип изделия текстом не выводит вовсе (только
слеш/наименование/кол-во) - баг был скрыт до появления шапки редактора. Исправлено - в обеих вью
CASE теперь проверяет s.id_or_format_estimates/i.id_or_format_estimates (сама группа) вместо
ee.id_format/fi.id_format.

БАГ КИРИЛЛИЧЕСКОЙ/ЛАТИНСКОЙ 'O' ДЛЯ НАСТОЯЩИХ ОТГРУЗОЧНЫХ ИЗДЕЛИЙ (06.09.2026, ИСПРАВЛЕН,
подтверждено пользователем): при разборе бага НСТД.О/НСТД.ПФ выше замечено, что в тех же CASE
(v_order_items/v_or_std_items) ветка для НАСТОЯЩЕГО (не нестандартного) отгрузочного изделия -
"when ee.type = 1 then 'О'"/"when fi.type = 1 then 'О'" - использовала КИРИЛЛИЧЕСКУЮ 'О', тогда как
в TFrmOGedtEstimate.PrepareForm (LTypePhrase, см. выше) и в условии кнопки "Вставить производственное
изделие" (A.InArray(FTypeOfItem, ['O', 'НO'])) используется ЛАТИНСКАЯ 'O' - несоответствие означало,
что для настоящего отгрузочного изделия шапка редактора показывала "стандартному изделию" вместо
"отгрузочному стандартному изделию" (срабатывал default в Decode), а кнопка "Вставить производственное
изделие" не появлялась вовсе. Подтверждено пользователем как реальный баг. Исправлено в пользу
ЛАТИНСКОЙ 'O' (d_orders.sql, обе вью) - для единообразия с уже латинским 'НO' парой строк ниже и с
TFrmOGedtEstimate.PrepareForm/кнопкой. Заодно поправлен единственный другой потребитель этого же
type_name, который сравнивал с КИРИЛЛИЧЕСКОЙ 'О' - uFrmOWrepEstimateChanges.pas ("История изменений
сметы"), его Decode-ключ переведён на латинскую 'O' вслед за вью.

УПРОЩЕНИЕ ТЕКСТА О СМЕТЕ-ЭТАЛОНЕ (06.09.2026, uFrmOGedtEstimate.pas/uFrmOGlstEstimate.pas): третья
строка шапки редактора (lblCapt3) и хвост первой строки шапки просмотрщика (LCapt1)/подпись печати
(FCapt), показывающие, что реально редактируется/просматривается смета-эталон нестандартного
изделия П (см. редирект выше), раньше явно упоминали архитектуру хранения ("Смета-эталон
нестандартного изделия..."). По просьбе пользователя - хранение сметы через эталон пользователю не
интересно и только запутывает - текст упрощён до нейтрального "К производственному (отгрузочному)
изделию заказа: <слеш, наименование, кол-во>" (в редакторе) / "(к производственному (отгрузочному)
изделию заказа)" (в просмотрщике - без повтора слеша/наименования, они уже показаны строками выше).


Известные и исправленные проблемы (для справки, не переописывать заново)
-----------------------------------------------------------------------------
- До правки 05.09.2026: при новом нестандартном наименовании в гриде
  заказа wo_estimate ставился в null (не в 0) - uFrmOWOrder.pas,
  CalculateFrgItemsRow. Это null доходило до estimates.isempty при
  автосоздании сметы позиции заказа, из-за чего p_deletefreeestimate
  (условие isempty <> 1) не удаляла такую пустую смету (NULL <> 1 в
  Oracle даёт NULL, а не TRUE) - в журнале заказов "повисала" сегодняшняя
  дата при фактически пустой смете. Исправлено: wo_estimate теперь 0.
- Там же, p_deletefreeestimate (d_estimates.sql) защищена через
  nvl(isempty, 0) <> 1 - на случай, если isempty окажется null по любой
  другой причине.
- TOrders.LoadEstimate: копирование из сметы-эталона (IsOrItemStd) больше
  не создаёт пустую запись сметы позиции заказа, если у эталона ещё нет
  собственной сметы - функция выходит сразу (раньше это ограничение
  действовало только в интерактивном режиме, не в тихом вызове из
  RefreshEstimatesToOrder).
- TFrmOWOrder.SaveOrderItems: id_std_item для нового/переименованного
  нестандартного изделия раньше уходил в БД как null из-за того, что
  массив NewValues снимался до того, как id_std_item фактически
  создавался/находился - из-за этого v_order_items.itemname и подобные
  поля (берутся из or_std_items по id_std_item) были null. Исправлено -
  NewValues точечно патчится после создания id_std_item.
- TFrmOWOrder.PrepareFrgItems: копирование позиций заказа при
  Mode=fCopy (в т.ч. "заказ из шаблона") могло приводить к тому, что
  нетронутые позиции вообще не сохранялись в новый заказ (не только по
  шаблону, а при любом копировании) - исправлено генерализацией remap
  id на псевдо-id для любого FIdSource<>0.
- TOrders.ApplyEstimateArray: LogEstimateChange мог упасть по внешнему
  ключу (ORA-02291, "данные не найдены в справочнике"), если смета только
  что была удалена p_DeleteFreeEstimate (типичный случай - копирование из
  ещё не заполненного вручную эталона нестандартного изделия) - добавлена
  проверка на существование сметы перед вызовом LogEstimateChange.
- TFrmOWrepEstimateChanges.Prepare / TFrmOGlstEstimate.PrepareForm: для
  нестандартного П-изделия история изменений и просмотр сметы, открытые
  из журнала заказов, показывали смету САМОЙ ПОЗИЦИИ ЗАКАЗА вместо эталона
  (группа -1), где реально происходит редактирование - см. 1.6. Исправлено
  добавлением TOrders.ResolveEstimateDisplayTarget и её использованием в
  обоих диалогах.
- TOrders.LoadEstimate/ApplyEstimateArray: собственная копия сметы позиции
  заказа для нестандартного П-изделия синхронизировалась с эталоном только
  при следующем сохранении заказа целиком - см. 1.4b. Исправлено добавлением
  ASyncOrderItemAfterSave/Ctx.SyncOrderItemAfterSave - синхронизация теперь
  происходит сразу после сохранения эталона.
- TFrmOWOrder.SaveOrderItems: id_std_item нестандартной позиции отгрузочного
  заказа резолвился в группу -2 (p_get_or_std_item_id_nonstandard_shipment)
  только при изменении имени (IsNameChanged) - при автосоздании отгрузочного
  заказа на основании производственного (копирование состава без изменения
  имён, см. 1.2/1.4b) позиции оставались привязаны к группе -1 (производство)
  вместо -2 (отгрузка). Исправлено: резолвинг теперь выполняется всегда для
  нестандартных позиций заказа типа О нового формата (см. LForceResolveShpNstd).
- p_create_or_std_item_nonstandard_new_format (d_orders.sql): автоматическая
  смета-ссылка группы -2 создавалась без группы (id_group = null) и без префикса
  в наименовании - см. 1.2. Исправлено вынесением создания такой сметы (и для
  стандартных, и для нестандартных изделий) в общую хранимую процедуру
  p_create_shipment_estimate_from_production_item (d_estimates.sql; группа - по
  bcad_groups.is_production, префикс - по or_format_estimates.prefix); заодно
  убран дублирующий Delphi-код в TFrmODedtOrStdItem.CreateSelfSmeta.
- p_create_shipment_estimate_from_production_item (d_estimates.sql), доисправление:
  даже после переноса создания в эту процедуру группа проставлялась верно, а префикс -
  всё равно никогда. Причина - v_pname := case when v_prefix is not null and
  v_prefix <> '' then ... - в Oracle '' есть NULL, сравнение с ним через <> всегда даёт
  NULL, ветка THEN была недостижима. Исправлено - лишняя проверка <> '' убрана.


РАЗДЕЛ 2. СГП (СКЛАД ГОТОВОЙ ПРОДУКЦИИ): СУЩЕСТВУЮЩАЯ ЛОГИКА (АНАЛИЗ, ПОДГОТОВКА К ПЕРЕДЕЛКЕ)
=================================================================
(анализ 07.09.2026, по задаче переделки учёта СГП так, чтобы он одинаково
работал для старого и нового (26) формата заказов; ниже - описание ТЕКУЩЕЙ
(существующей) логики, без изменений кода - d_order_stages.sql, d_sgp.sql,
uFrmOGrepSgp.pas, uFrmOGedtSgpRevision.pas, uFrmOGjrnOrderStages.pas,
uFrmOGedtOrderStages.pas)


2.1. Этапы прохождения заказа - order_item_stages (d_order_stages.sql)
-----------------------------------------------------------------
Таблица order_item_stages(id, id_order_item, id_stage, qnt, dt) хранит
датированные записи о прохождении каждой позиции заказа (order_items)
через этапы обработки. id_stage:
  - 1 - в производство;
  - 2 - приёмка на СГП;
  - 3 - отгрузка с СГП;
  - 4 - приёмка на ОТК.

Триггер trg_order_item_stages_biud_r поддерживает order_items.qnt_to_sgp
(кэшированную нарастающую сумму по этапу 2 - приёмке на СГП) и пишет в
P_ToUchetLog.

Представления: v_order_items_for_stages (базовая выборка) ->
v_order_item_stages1 (по каждой позиции заказа - количества/даты/статус по
всем 4 этапам через коррелированный pivot на sys_context) ->
v_order_stages1 (по заказу в целом - агрегирование статусов позиций через
F_GetOrderStage_State, которая сворачивает min/max статусы позиций в
null/0/1/2 - "не начат"/"частично"/"завершён").

Процедуры:
  - p_OrderStage_SetMainTable(IdOrder, IdStage) - при полном завершении
    этапа 2 или 3 по всем позициям заказа проставляет orders.dt_to_sgp
    (этап 2) или orders.dt_from_sgp (этап 3);
  - p_OrderStage_SetItem(IdOrderItem, IdStage, NewDt, NewQnt,
    UpdateOrders, out ResQnt, Adding=0) - устанавливает/добавляет/удаляет
    одну датированную запись, ограничивая количество остатком, ещё не
    принятым/отгруженным по данной позиции; при UpdateOrders=True в конце
    вызывает p_OrderStage_SetMainTable.

В этом же файле присутствует более старый и, по всей видимости,
частично не используемый механизм ОТК (order_otk/v_order_otk_items,
завязанный на внешнюю схему uchet.), а также много не относящегося к СГП
легаси-содержимого (material_groups, order_smeta, order_otk,
delayed_prod_reasons, or_montage и т.п.) - в анализ задачи по СГП это не
входит.

Флаг order_items.sgp (число(1), "отгрузка позиции с СГП") -
используется в ряде статусных вычислений v_order_item_stages1 как
исключающий признак (case when not(nvl(i.sgp,0) = 1 ...)) - позиции без
этого флага не участвуют в учёте СГП по старому формату.


2.2. Динамический расчёт остатков по стандартным изделиям - d_sgp.sql
-----------------------------------------------------------------
Отчёт "Текущее состояние СГП" (стандартные изделия) считается
динамически, без хранения остатков - на каждый показ заново
пересчитывается вся цепочка представлений.

Ключевые принципы (из заголовочного комментария файла):
  - учитываются только стандартные изделия, у которых в or_std_items
    установлен by_sgp = 1 - причём этот флаг должен стоять ТОЛЬКО на
    отгрузочных (сбытовых) форматах; если ошибочно проставлен на
    производственном - учёт по этому формату будет неверным;
  - "запущено в производство" определяется по orders.id_organization = -1;
    любая другая организация - это заказ клиента (отгрузка);
  - сопоставление производственного и отгрузочного изделия делается
    ТОЛЬКО ПО ИМЕНИ (or_std_items.name) - в комментарии к файлу прямо
    сказано, что предполагается (но никак не проверяется), что у каждой
    группы есть ровно один производственный и один сбытовой подформат;
  - формат КБ РЦ из этого механизма явно исключён;
  - нестандартные изделия (nstd=1) полностью исключены из этого
    механизма сопоставления по имени - для них отдельная параллельная
    логика (см. 2.3).

Цепочка представлений: v_order_items_all/v_order_items__ ->
v_sgp_sell_formats -> v_sgp_sell_items (список отслеживаемых изделий) ->
v_sgp_by_psp_sell(_list)/v_sgp_by_psp_prod(_list) (всего когда-либо
заказано/запущено) -> v_sgp_registered(_list)/v_sgp_shipped(_list)
(фактическое движение, из order_item_stages, этапы 2/3, с фильтром
dt > sgp_params.dt_beg) -> v_sgp_shipped_plan(_list)/v_sgp_in_prod(_list)
(план/в работе, для ещё не закрытых заказов) -> v_sgp_move_list (полный
аудит движения - UNION событий по паспортам производства/отгрузки и по
актам) -> v_sgp_items (итоговая агрегация: qnt = registered + act_in -
shipped + act_out; плюс qnt_need и цена/сумма через v_sgp_item_prices,
которая берёт себестоимость сырья из сметы соответствующего
производственного изделия - ИТМ).

Таблица sgp_params (одна строка) хранит дату отсечки (сейчас 01.08.2024) -
движение по стадиям учитывается только после этой даты.


2.3. Параллельный учёт нестандартных изделий - v_sgp2 ("Розница")
-----------------------------------------------------------------
Для нестандартных изделий (nstd=1) сопоставление по имени невозможно
(имена, как правило, уникальны и не парны), поэтому используется
отдельный, более простой механизм - учёт не по имени/группе, а
непосредственно по позиции заказа (по "слешу"):

v_sgp2_order_items (все order_items с nstd=1) -> v_sgp2 (агрегация по
каждой ПОЗИЦИИ ЗАКАЗА - количества этапов 2/3 из order_item_stages плюс
акты, с фильтром id_or_format_estimates = 0, то есть только "старые"
плоские нестандартные изделия без деления на группы -1/-2/-3, см. 1.2).

Отчёт по нестандартным изделиям ("Состояние СГП (нестандартные
изделия)") использует именно v_sgp2 и открывается как myfrm_Rep_Sgp2 -
обрабатывается универсальным uFrmXGlstMain.pas (не отдельным юнитом).


2.4. Ревизия СГП - уже существующий механизм актов инвентаризации
-----------------------------------------------------------------
uFrmOGedtSgpRevision.pas (диалог "Ревизия СГП", открывается и для
стандартных - grid на v_sgp_items, и для нестандартных изделий - grid на
v_sgp2, в зависимости от ID = 0/<>0) - это уже готовый, работающий
механизм актов инвентаризации:
  - показывает расчётное количество (qnt, из v_sgp_items/v_sgp2) рядом с
    полем для ввода фактического количества по пересчёту (qnt_fact);
  - при сохранении (TFrmOGedtSgpRevision.Save) по каждой позиции с
    заполненным qnt_fact считает разницу qnt_fact - qnt; положительные
    расхождения собираются в один акт оприходования (sgp_act_in +
    sgp_act_in_items), отрицательные - в один акт списания (sgp_act_out +
    sgp_act_out_items), оба - одной транзакцией (Q.QBeginTrans/
    QCommitOrRollback);
  - элементы актов ссылаются либо на id_std_item (стандартные изделия),
    либо на id_or_item (нестандартные, = order_items.id) - выбор
    определяется тем же условием ID = 0;
  - явно указано пользователю: "скорректировать либо отменить ревизию и
    созданные по ней акты нельзя".

Таким образом, идея "выгрузить текущее состояние в акт инвентаризации и
учитывать его" технически уже реализована как работающий инструмент - её
нужно не изобретать заново, а переиспользовать (в том числе вызывая
создание таких актов также из диалогов приёмки/отгрузки с СГП по старым
заказам, с комментарием о заказе-источнике).


2.5. Экраны Delphi, задействованные в учёте СГП
-----------------------------------------------------------------
  - uFrmOGrepSgp.pas (myfrm_Rep_Sgp = 'Rep_Sgp') - отчёт "Текущее
    состояние СГП" по стандартным изделиям, grid на v_sgp_items,
    фильтр по формату (комбобокс CbFormat), кнопки "Ревизия"/"Журнал
    ревизий", подробная расшифровка по столбцам через uFrmOGinfSgp.pas
    (двойной клик по ячейке остатка показывает движение номенклатуры);
  - myfrm_Rep_Sgp2 = 'Rep_Sgp2' - тот же отчёт для нестандартных
    изделий, обрабатывается общим uFrmXGlstMain.pas, grid на v_sgp2;
  - uFrmOGedtSgpRevision.pas - диалог "Ревизия СГП" (см. 2.4);
  - uFrmOGjrnOrderStages.pas - "Журнал этапов заказа": основной грид,
    из которого выполняется как массовая отметка "весь заказ выполнен",
    так и быстрый ввод в отдельной ячейке; оба варианта вызывают
    p_OrderStage_SetItem (три точки вызова), после чего дополнительно
    вызывается Orders.FinalizeOrder(...);
  - uFrmOGedtOrderStages.pas - диалог-"календарь" по одной позиции
    заказа, открывается из журнала; в отличие от журнала работает с
    order_item_stages НАПРЯМУЮ (не через p_OrderStage_SetItem), затем
    вызывает p_OrderStage_SetMainTable.


2.6. Дискриминаторы старого и нового формата заказов
-----------------------------------------------------------------
  - СТАРЫЙ формат: orders.id_organization = -1 отмечает "производство"
    (запуск), любое другое значение - заказ клиента/отгрузка;
  - НОВЫЙ формат: orders.std_item_type (0 = STDITEM_TYPE_PRODUCTION,
    1 = STDITEM_TYPE_SHIPMENT, 2 = STDITEM_TYPE_SEMIPRODUCT,
    проставляется из or_format_estimates.type, см. 1.2);
  - граница между форматами по времени - константа cOrderNew26TypeID =
    18510 (uOrders.pas) - id заказа в схеме Учёта, начиная с которого
    заказ считается "новым (26)" форматом (id < 18510 - старый).

Существенное отличие, напрямую относящееся к предлагаемой переделке:
в НОВОМ формате производственное и отгрузочное нестандартные изделия
УЖЕ гарантированно создаются парой с одинаковым именем - см. 1.2,
p_create_or_std_item_nonstandard_new_format создаёт сразу две записи
or_std_items (группы -1 и -2) с общим именем при создании
производственного нестандартного изделия. Это значит, что для нового
формата и для нестандартных изделий тоже можно применить сопоставление
"по имени и группе", как для стандартных - то есть предлагаемое единое
правило ("чёткое поступление/отгрузка изделий, совпадающих по имени и
параллельных по группе") для нового формата уже имеет техническую основу
в структуре данных, тогда как в СТАРОМ формате нестандартные изделия
такой парности не имеют в принципе (отсюда и существующий обходной путь
через v_sgp2 - учёт по позиции заказа/слешу, а не по имени).


2.7. Резюме - соответствие текущей архитектуры и предложения по переделке
-----------------------------------------------------------------
Это раздел - только фиксация текущего состояния для последующей
переделки (задача поставлена 07.09.2026, начало цитаты: "нужно
переделать учёт по СГП... пока проанализируй, также запиши существующую
логику..."); согласно этой же постановке, реализация переделки в данный
момент НЕ ведётся и требует отдельного подтверждения. Ниже - как
соотносятся предложенные пункты с уже существующим кодом:
  - "приёмка/отгрузка с СГП в новом формате не использует флаг С СГП,
    все изделия приходуются по производственным паспортам, отгружаются
    по отгрузочным" - технически уже возможно единообразно провести
    через order_item_stages (этапы 2/3), т.к. таблица не завязана на
    orders.id_organization или order_items.sgp напрямую - эти признаки
    используются только в отчётных представлениях (v_order_item_stages1,
    d_sgp.sql), а не в самой структуре хранения этапов;
  - "учёт по группам, а не по подгруппам изделий" - текущий v_sgp_items
    строится на уровне or_std_items (сопоставление по имени внутри
    подформата); понятие "группа" в текущем коде - or_std_items.id_group/
    группа BCAD (см. 1.2, 1.10) - переход на агрегацию по группе
    потребует новой логики агрегирования, которой сейчас в d_sgp.sql нет;
  - "старую логику (d_sgp.sql) для старых заказов исключить, текущее
    состояние выгрузить в акт инвентаризации" - механизм актов
    инвентаризации (sgp_act_in/out) уже существует и уже используется
    диалогом "Ревизия СГП" (см. 2.4) - его не нужно создавать заново;
  - "при приёмке/отгрузке с СГП по старым заказам формировать акты
    инвентаризации с комментарием о заказе" - в текущем коде
    p_OrderStage_SetItem и прямая работа с order_item_stages из
    uFrmOGedtOrderStages.pas никак не создают записи sgp_act_in/out -
    это действительно новая функциональность, которую нужно будет
    добавить.

Переделка начата 07.09.2026 (см. 2.8 ниже) - раздел выше зафиксирован как
описание логики ДО начала переделки.


2.8. Ход переделки СГП (обновляется по мере реализации)
-----------------------------------------------------------------
Уточнения от пользователя, положенные в основу переделки:
  - "группа" = or_formats (id_or_formats), "подгруппы" = or_format_estimates/
    or_std_items внутри неё (одна производственная, одна или несколько
    отгрузочных; в старых форматах бывает и несколько производственных).
    Сейчас (v_sgp_items) остаток считается отдельно по каждой отгрузочной
    подгруппе - если у группы несколько отгрузочных подгрупп с by_sgp=1,
    один и тот же физический товар даёт несколько отдельных строк остатка.
    Новая логика считает по ИЗДЕЛИЮ (имени), просуммированному по всем
    отгрузочным подгруппам группы; в комбобоксе выбор - группа целиком, а
    не конкретная отгрузочная подгруппа. Для старых групп с несколькими
    производственными/отгрузочными подгруппами движения дальше не будет,
    но остаток на момент среза переносится так же - по имени, без разбивки
    по подгруппе.
  - Снимок текущего состояния - материализация в таблицу (не через
    sgp_act_in/out), старый диалог (uFrmOGrepSgp.pas) остаётся, но
    переводится в readonly.
  - Логика автосоздания актов при приёмке/отгрузке старых заказов
    выносится в одну хранимую процедуру (единая точка входа).
  - Нестандартные изделия - ТОЛЬКО снимок (просмотр прошлого состояния),
    без ongoing-актов: новые нестандартные изделия нового формата никак не
    пересекаются со старыми, поэтому ни начальное состояние, ни
    промежуточные акты инвентаризации по ним не нужны - в отличие от
    стандартных, где физический остаток на складе общий для старых и
    новых заказов и требует непрерывного учёта на переходный период.
  - Порог старый/новый заказ - как и раньше, по cOrderNew26TypeID.
  - Цена/себестоимость в замороженном (readonly) отчёте - показывается
    так же, как сейчас (фиксируется в снимке на момент среза).
  - Новая логика "Текущее состояние СГП" для новых заказов - отдельный
    новый модуль (не правка uFrmOGrepSgp.pas), интерфейс может отличаться.
  - Нестандартные изделия в новом (26) формате - тоже отдельный отчёт
    (не тот же грид, что стандартные): ключевое отличие от стандартных -
    изделие встречается строго один раз в одном заказе (агрегация по
    заказам не нужна, в отличие от стандартных, где одно и то же изделие
    может заказываться многократно).

Сделано (07.09.2026, d_sgp.sql):
  - Таблица sgp_snapshot_std_items - снимок остатка по стандартным
    изделиям на момент среза, ключ (id_or_formats, item_name), полный
    набор разрезов (как сейчас в v_sgp_items: заказано/запущено/принято/
    отгружено/план/потребность/цена/сумма/цена и сумма по смете).
  - Таблица sgp_snapshot_nstd_items - снимок по нестандартным изделиям
    старого формата, ключ - id_order_item (без агрегации по имени, т.к.
    нестандартное изделие уникально для своего заказа), полный набор
    разрезов, как в v_sgp2 (заказано/в производстве/принято/отгружено/
    остаток/цена/сумма/даты).
  - Представления v_sgp_snapshot_std_items / v_sgp_snapshot_nstd_items -
    тонкие обёртки над таблицами снимков с именами колонок, совпадающими
    с текущими полями грида (format_name, name и т.п.) - чтобы в Delphi
    можно было просто переключить SetTable на снимок, почти не трогая
    список колонок.
  - v_sgp_revisions переопределено на группу (or_formats) вместо
    отгрузочного подформата - т.к. readonly-отчёт выбирает группу целиком.
    Колонка id_format в выдаче теперь = id группы; фильтр в
    uFrmXGlstMain.pas (myfrm_J_Sgp_Acts) не менялся, он как принимал
    числовой параметр, так и принимает - просто теперь это id группы.
  - Представление v_sgp_items_by_group - агрегация тех же данных, что и
    v_sgp_items, но по (id_format, name), т.е. по ГРУППЕ, а не по
    отгрузочному подформату. НЕ построено как простой group by поверх
    v_sgp_items - часть его колонок (заказано/запущено/принято/отгружено/
    план) уже посчитана на уровне группы и одинаково повторяется на каждой
    строке-подформате (простой sum() умножил бы их на количество
    подформатов с этим именем), а часть (поправки по актам sgp_act_in/out)
    считана по конкретному id_std_item подформата и потому у дублей-строк
    отличается (sum() для них как раз нужен). Поэтому представление
    пересчитывает нужные компоненты заново от первичных данных (v_order_
    items__, order_item_stages, sgp_act_in_items/sgp_act_out_items),
    сразу сгруппированными по (id_format, name) - без прохода через
    v_sgp_items. Цена и цена по смете при нескольких отгрузочных
    подформатах с разной ценой на одноимённое изделие берутся средним -
    задокументированное упрощение для этого редкого/ошибочного случая.
  - Процедура p_Sgp_CreateSnapshot(PForce number default 0) - разово
    заполняет sgp_snapshot_std_items из v_sgp_items_by_group и
    sgp_snapshot_nstd_items из v_sgp2 (как есть, без изменения агрегации).
    Защищена от случайного повторного запуска (raise_application_error,
    если снимок уже есть и PForce <> 1) - т.к. повторный запуск после
    того, как отчёт уже переключат на readonly, перезаписал бы
    исторический снимок текущим (уже изменившимся) состоянием живых вью.

Важно: процедуру пока НЕ запускали - таблицы снимка пока пустые. Экраны
uFrmOGrepSgp.pas и Rep_Sgp2 УЖЕ переведены в readonly и смотрят на снимок
(см. ниже) - значит, до запуска p_Sgp_CreateSnapshot оба этих экрана
будут показывать ПУСТУЮ таблицу. Это ожидаемо только на время между
доставкой этого кода и фактическим запуском процедуры - запускать
p_Sgp_CreateSnapshot нужно как можно скорее после разворачивания этой
версии (в идеале - в одном релизе/сразу после), иначе пользователи
временно останутся без вообще какого-либо отчёта по СГП для старых
заказов.

Сделано (07.09.2026, uFrmOGrepSgp.pas, uFrmXGlstMain.pas):
  - uFrmOGrepSgp.pas (стандартные изделия) переведён в readonly-архив:
    источник данных - v_sgp_snapshot_std_items (вместо v_sgp_items),
    комбобокс выбирает ГРУППУ (or_formats), а не отгрузочный подформат
    (список групп берётся из тех, что реально есть в снимке), кнопка
    "Ревизия" и правка "Мин. остатка" (Frg1CellValueSave) убраны, двойной
    клик - детализация движения (Frg1OnDbClick) убран (id строки в снимке
    больше не соответствует or_std_items.id, да и живая детализация
    статичному архиву не нужна). "Журнал ревизий" оставлен - фильтр по
    id_format в uFrmXGlstMain.pas не менялся, но теперь это id группы
    (см. переопределённый v_sgp_revisions выше).
  - uFrmXGlstMain.pas, обработчик myfrm_Rep_Sgp2 (нестандартные изделия) -
    аналогично переведён в readonly-архив: источник - 
    v_sgp_snapshot_nstd_items (вместо v_sgp2). Кнопка "Ревизия" убрана,
    "Журнал ревизий" оставлен (для нестандартных фильтр всегда был по
    id_format = 0, не меняется). Детализация по столбцу "qnt"
    (Frg1OnDbClick) убрана - по той же причине, что и для стандартных.
  - Заголовки обоих экранов и текст InfoArray переписаны, чтобы явно
    объяснять пользователю архивный характер данных и ссылаться на
    "!Алгоритмы" за подробностями.
  - Приведено к нормальному стилю именования Oracle (напоминание
    пользователя 07.09.2026 - для всех НОВЫХ хранимых процедур/функций
    впредь только так): процедура p_Sgp_CreateSnapshot переименована в
    p_sgp_create_snapshot, параметр PForce - в p_force, переменная c - в
    v_cnt. Также устранено дублирование v_sgp_revisions - вместо второго
    (нового) create or replace в конце файла исправлено ОРИГИНАЛЬНОЕ
    определение на своём месте, а дубликат в конце удалён.

Сделано (07.09.2026, переход or_std_items.price -> price_base):
  Пользователь убрал из or_std_items колонку price (осталась только
  price_base - базовая цена изделия без учёта НДС; см. d_orders.sql,
  однократный --$go блок drop column price, и компенсирующее вычисляемое
  поле price в v_or_std_items = round(price_base * 1.22, 2), помеченное
  там же как временное - для старых потребителей, до их переноса на
  price_base/price_with_nds). В d_sgp.sql проверены и поправлены
  оставшиеся прямые обращения к or_std_items.price (напрямую к таблице,
  а не через v_or_std_items):
  - v_sgp_sell_items (уже помечено --$+, живое) - si.price -> si.price_base,
    поправлено пользователем самостоятельно.
  - v_sgp_items_by_group, подзапрос средней цены продажи (prc) -
    osi.price -> osi.price_base, поправлено дополнительно.
  В обоих местах цена взята напрямую из price_base, БЕЗ домножения на
  НДС (1.22), которое временно делает v_or_std_items.price. То есть это
  не просто переименование колонки: цена/сумма в отчётах СГП (и в
  будущем снимке) фактически стали ценой БЕЗ НДС, а не с НДС, как было
  раньше через старое поле price. Решение принято по аналогии с правкой
  пользователя в v_sgp_sell_items - явного отдельного подтверждения
  корректности отказа от НДС в цифрах СГП пока не запрашивалось.

  Найдено, но НЕ поправлено (вне d_sgp.sql, требует отдельного решения
  пользователя): d_planned_orders.sql - представления
  v_planned_orders_w_sum и v_planned_order_items напрямую джойнят
  or_std_items и используют osi.price (расчёт денежных сумм по плановым
  заказам, порядка 10 мест) - тоже сломаны отсутствием колонки price.
  Файл не размечен тегами SqlUpdater (--!+/--$+), поэтому неясно,
  актуален ли модуль плановых заказов и как/когда он накатывается на
  базу. Пользователь подтвердил (07.09.2026): такие места есть и в
  других модулях кроме СГП, но пока их сознательно НЕ трогаем - в
  рамках этой задачи правим только то, что относится к учёту СГП
  (d_sgp.sql). d_planned_orders.sql и прочее вне СГП - намеренно вне
  области действия текущей правки, не забыть вернуться к этому отдельно.

Сделано (07.09.2026, запуск снимка на реальной базе):
  Пользователь запустил p_sgp_create_snapshot на реальной базе - отработало,
  экраны uFrmOGrepSgp.pas и Rep_Sgp2 (Rep_Sgp2 - через uFrmXGlstMain.pas)
  показывают снимок корректно.

Сделано (07.09.2026, автосоздание актов СГП по старым заказам):
  Единая точка автосоздания sgp_act_in/sgp_act_out при приёмке/отгрузке
  СТАНДАРТНЫХ изделий по СТАРЫМ заказам - НЕ отдельная хранимая процедура,
  вызываемая из p_OrderStage_SetItem, а сам триггер trg_order_item_stages_biud_r
  на таблице order_item_stages (d_order_stages.sql). Выбрано так, потому что
  в order_item_stages пишут ДВА разных пути - p_OrderStage_SetItem (используется
  много где) и uFrmOGedtOrderStages.pas (диалог-календарь, пишет в таблицу
  напрямую insert/update/delete по id, минуя процедуру) - триггер на самой
  таблице ловит оба пути одной логикой, дублировать её в двух местах не нужно.
  В триггер добавлена вложенная процедура p_sgp_act_on_stage_change(
  p_id_order_item, p_id_stage, p_delta, p_dt), вызываемая из всех трёх веток
  (deleting/updating/inserting) с уже готовой дельтой количества (insert -
  :new.qnt, update - :new.qnt - :old.qnt, delete - -:old.qnt):
  - работает только для этапов 2 (приёмка на СГП) и 3 (отгрузка с СГП);
  - только для строк order_items со std = 1 и заполненным id_std_item -
    нестандартные изделия по-прежнему без ongoing-актов (см. выше);
  - только для СТАРЫХ заказов: id_order сравнивается с порогом, который
    раньше был константой cOrderNew26TypeID только в Delphi (uOrders.pas).
    По просьбе пользователя не стали заводить новую хранимую константу,
    а продублировали значение через уже существующую таблицу properties/
    процедуру P_SetProp (d_adm.sql) - имя свойства id_order_format_26
    (нормальный оракловый snake_case, а не "корявое" по регистру и стилю
    cOrderNew26TypeID) - и prop, и subprop равны этому имени. Заполнение
    свойства (значение 18510) - --!go блок в конце d_temp.sql (P_SetProp).
    Если порог cOrderNew26TypeID в Delphi когда-нибудь изменится - обновить
    и значение свойства id_order_format_26 в базе (properties), иначе SQL
    и Delphi разъедутся в трактовке старый/новый заказ.
  - знак дельты определяет акт: этап 2, дельта > 0 - sgp_act_in (пришло),
    дельта < 0 - sgp_act_out (приёмку скорректировали вниз); этап 3,
    дельта > 0 - sgp_act_out (отгрузили), дельта < 0 - sgp_act_in (отгрузку
    отменили/уменьшили, товар вернулся на склад);
  - id_format акта берётся из or_std_items.id_or_format_estimates по
    id_std_item (подформат, как и в существующих ручных актах-ревизиях);
  - в строку акта, помимо id_std_item, дополнительно пишется id_or_item
    (order_items.id) - для трассировки, из какого заказа/позиции пришла
    корректировка (колонка и FK под это в sgp_act_in_items/out_items уже
    были, просто раньше не заполнялись для стандартных изделий);
  - comm акта - автотекст со номером заказа (orders.ornum), позицией и
    датой этапа (не sysdate - используется реальная дата приёмки/отгрузки
    :new.dt/:old.dt, чтобы акты были хронологически привязаны к событию,
    а не ко времени срабатывания триггера);
  - вся процедура обёрнута в exception when no_data_found then null - если
    по order_items/or_std_items/orders/properties не нашлось строки, акт
    молча не создаётся (не ломает основную транзакцию); снаружи её вызов
    также попадает под общий exception when others then null у самого
    триггера (как и раньше было с P_ToUchetLog).
  Не проверено на реальной базе (только статически по коду) - нужно
  протестировать приёмку/отгрузку/корректировку по конкретному старому
  заказу со стандартным изделием и посмотреть на sgp_act_in/sgp_act_out
  и на v_sgp_revisions.

Проверено (07.09.2026, накладная перемещения на СГП):
  Пользователь напомнил про uFrmOWInvoiceToSgp.pas/d_invoice_to_sgp.sql -
  отдельный экран приёмки на СГП по накладной перемещения. Проверено - он
  тоже завершает приёмку вызовом p_OrderStage_SetItem (IdStage = 2, Adding = 1),
  т.е. пишет в order_item_stages тем же путём, что и журнал этапов - значит,
  уже покрыт триггером trg_order_item_stages_biud_r, отдельно ничего не
  требуется. Известных путей записи в order_item_stages теперь три:
  p_OrderStage_SetItem (используется и из журнала этапов, и из накладной
  перемещения), и uFrmOGedtOrderStages.pas (диалог-календарь, напрямую) -
  все три ловятся одним триггером.

Сделано (07.09.2026, or_std_items.id_prod_std_item - id-связь отгрузка/производство):
  Для нового модуля "Текущее состояние СГП" (новый формат) нужна прямая
  id-связь отгрузочного стандартного изделия с производственным - раньше её
  нигде явно не было (только окольно, через первую позицию сметы отгрузочного
  изделия). Пользователь выбрал не сопоставлять по имени, а завести отдельную
  колонку:
  - or_std_items.id_prod_std_item (number(11), FK на or_std_items(id), сам на
    себя) - у ОТГРУЗОЧНОГО изделия ссылка на его производственного "соседа"
    по семье (d_orders.sql, --!go блок сразу после or_std_items/его триггеров,
    перед v_or_std_items). Одноразовый backfill существующих данных - по
    имени изделия, в пределах (id_format, sync_group), и ТОЛЬКО когда в этой
    паре ровно одна активная производственная подгруппа (иначе сопоставление
    неоднозначно - оставляем null). Отдельным select'ом (не --!go, для
    информации) после backfill выводится список отгрузочных изделий, где
    связь не проставилась - их надо будет посмотреть/поправить вручную.
  - uFrmODedtOrStdItem.pas, Save() - в этом диалоге и так уже была готовая
    инфраструктура "семьи" изделий (FRows/LRowIds, синхронизация цены/
    маршрута между произв./отгруз./п/ф подгруппами по sync_group, см. общий
    комментарий в начале модуля и LProdCount у CheckSelfSmetaAction). Туда же,
    сразу после подсчёта LProdCount (до commit транзакции - это просто
    денормализованная ссылка на уже сохранённые в этой же транзакции записи,
    в отличие от CreateSelfSmeta которая нарочно вызывается ПОСЛЕ commit),
    добавлено: при LProdCount = 1 у всех отгрузочных строк семьи проставляется
    id_prod_std_item = id производственной строки. Работает и при создании
    новых изделий, и при редактировании/переименовании существующих - Save()
    и так всегда пересохраняет всю семью целиком.

Сделано (07.09.2026, id_prod_std_item для нестандартных изделий нового формата):
  Пользователь напомнил про p_create_or_std_item_nonstandard_new_format
  (d_orders.sql) - процедуру создания нестандартных изделий нового формата
  (2026), которая при p_std_item_type = 0 (производство) создаёт СРАЗУ пару
  строк or_std_items - в группе -1 (нестандарт производства, p_id_item) и
  в группе -2 (нестандарт отгрузки, v_id_stditem2) - но раньше связь между
  ними нигде не сохранялась (v_id_stditem2 использовался только для создания
  сметы-ссылки, p_create_shipment_estimate_from_production_item, и терялся).
  Добавлено: update or_std_items set id_prod_std_item = p_id_item where
  id = v_id_stditem2 - та же колонка (or_std_items.id_prod_std_item), что и
  у настоящих стандартных изделий, чтобы новый модуль "Текущее состояние
  СГП" мог работать с ней единообразно для std и nstd изделий нового
  формата. Процедура была помечена --$+ (уже применена) - перепомечена на
  --!+ (тело изменилось, нужно накатить заново).
  Ветка p_std_item_type = 1 (создание ТОЛЬКО отгрузочного изделия, без пары)
  не тронута - там нет никакого производственного id под рукой, сопоставлять
  не с чем (сопоставление в этом случае, если оно вообще нужно, судя по
  комментариям у p_get_or_std_item_id_nonstandard_shipment, идёт другим,
  отдельным путём - не разбирался подробно, т.к. не просили).
  Backfill уже существующих (созданных до этой правки) пар -1/-2 по имени -
  --!go блок в конце d_temp.sql (только когда в группе -1 ровно одно
  совпадение по имени, иначе оставляет null).

  ВАЖНО - находка пользователя по ходу дела: SqlUpdater неверно обрабатывает
  строки с "exec ИмяПроцедуры(...)" внутри --$go/--!go блока (в d_temp.sql
  пользователь вручную поправил мой --!go блок с "exec P_SetProp(...)" на
  --$go, т.е. фактически применил его сам в обход SqlUpdater, и оставил
  пометку об этой ошибке). Вывод на будущее: в --go-блоках использовать
  анонимный PL/SQL блок (begin ... end; /), а НЕ короткую форму "exec
  Процедура(...);" - именно так уже сделан новый backfill-блок выше.

Разобрано (07.09.2026, "странность" в uFrmOGedtDistributeQnt.pas -
почему сравнение по raw id_std_item там работает, хотя ожидалось, что
id_std_item у производственных и отгрузочных изделий всегда разный):
  Пользователь заметил, что диалог распределения количества (сопоставляет
  позиции заказа П и родственных ему заказов О, связанных через
  orders.id_production_order, по СЫРОМУ равенству id_std_item - см. код
  там, комментарий "сопоставление - по id_std_item, а НЕ по slash") на
  практике отработал нормально - что противоречило его же более раннему
  утверждению "он всегда должен быть разный".
  Разгадка нашлась в uFrmOWOrder.pas:
  - PrepareFrgItems (комментарий у строк 839-846): при автосоздании заказа
    О из заказа П (FCreateShipmentFromProductionId - ровно тот сценарий,
    с которым и работает диалог распределения, т.к. использует то же самое
    id_production_order) состав копируется из П как есть, "при простом
    копировании заказа... id_std_item не переопределяется - остаётся тем
    же, что и у источника".
  - SaveOrderItems (строки 3236-3295): блок пересчёта/переопределения
    id_std_item (создание нестандартного изделия или резолв в группу -2 -
    IsNameChanged/LForceResolveShpNstd) целиком находится под условием
    nstd=1 - т.е. касается ТОЛЬКО нестандартных изделий. Для стандартных
    изделий (nstd=0) в этой функции id_std_item нигде не трогается.
  Итог: для стандартных изделий в СЦЕНАРИИ "заказ О автосоздан из заказа П"
  id_std_item в order_items заказа О буквально совпадает с id_std_item
  соответствующей позиции заказа П (группа производства, не отгрузки) -
  поэтому сравнение по raw id_std_item в диалоге распределения работает
  верно, это не случайность, а следствие того, как устроено копирование.
  Утверждение "id_std_item всегда разный" верно только для изделий,
  добавленных в заказ О самостоятельно через справочник (picker) - там
  выбор идёт из v_or_std_items по ЕГО СОБСТВЕННОЙ id_or_format_estimates
  (группа отгрузки), а не скопирован из П.
  Решение пользователя: диалог распределения количества (uFrmOGedtDistribute-
  Qnt.pas) НЕ трогаем, оставляем сравнение по raw id_std_item как есть - это
  осознанно правильное поведение именно для его сценария (родственные П/О).
  На переделку через id_prod_std_item сознательно не пошли.

Сделано (07.09.2026, живой отчёт "Текущее состояние СГП" для нового формата -
первая часть, SQL): добавлен view v_sgp_new_format_items (d_sgp.sql, в конце
файла) - живой (не замороженный) аналог архивного v_sgp_items_by_group, но
только для заказов нового формата (id_order >= properties.id_order_format_26)
и с сопоставлением производство<->отгрузка по or_std_items.id_prod_std_item
вместо совпадения наименования. Договорённости по дизайну (обсуждено с
пользователем):
  - Форма - НОВАЯ отдельная (не вкладка/режим старой), вызывается вместо
    старой; старая (архивная uFrmOGrepSgp.pas) вызывается ИЗ новой через
    кнопку/пункт меню (рабочее название - "Архив по старым заказам", можно
    поправить при реализации формы).
  - Единица агрегации строки - как и раньше: одно ПРОИЗВОДСТВЕННОЕ стандартное
    изделие (эталон, or_std_items.by_sgp=1, id_or_format_estimates с type=0,
    active=1) - вокруг него через id_prod_std_item стягиваются все связанные
    отгрузочные изделия (не важно, из какого именно отгрузочного подформата).
  - Источник "сколько сейчас на складе" - order_item_stages (этапы 2/3,
    ТЕКУЩЕЕ значение qnt по каждой (order_item, этап), т.к. таблица не
    ведёт историю - см. trg_order_item_stages_biud_r), а не устаревший
    механизм паспортов, на котором считает архивный отчёт.
  - Ручные акты-корректировки (sgp_act_in/sgp_act_out, журнал ревизий) тоже
    учитываются в остатке нового отчёта (решение пользователя) - но только
    те, что относятся к позициям заказов НОВОГО формата (фильтр через
    id_or_item -> order_items.id_order -> orders.id >= порога) - чтобы не
    задвоить с архивным отчётом по старым заказам.
  - Колонки отчёта - те же, что в старом (qnt_psp_sell/qnt_psp_prod/
    qnt_sgp_registered/qnt_shipped/qnt_in_prod/qnt_to_shipped/qnt/qnt_need/
    price/summ/priceraw/sumraw), но без dt_snapshot (отчёт живой).
  - priceraw/sumraw (цена и сумма по смете) переиспользуют существующий
    v_sgp_item_prices как есть (join по g.id_prod_std_item = rawp.id) - он
    и раньше работал по имени изделия через оценки/номенклатуру, эта логика
    не привязана к формату заказа и не нуждается в переделке.
  - price/summ (цена и сумма по продаже, без НДС) - avg(price_base) по всем
    or_std_items с by_sgp=1 в той же "семье" (id_prod_std_item), аналогично
    тому, как в v_sgp_items_by_group это делалось по (id_format, name).
  - Известное ограничение (см. ниже, "ещё не сделано"): отгрузочные изделия
    без заполненного id_prod_std_item (несопоставленные после backfill) в
    отчёт не попадают - т.к. их "семья" по формуле coalesce(id_prod_std_item,
    id) равна их собственному id, а не id производственного эталона, и они
    просто не находят себе группу g. Нужно проверить после запуска backfill.
  - Тег --!+ у view (новый объект, накатить через SqlUpdater).
  - СДЕЛАНО (07.09.2026, часть 3): Delphi-форма. Новый unit+dfm
    uFrmOGrepSgpNew.pas/.dfm - клон uFrmOGrepSgp.pas/.dfm (у обоих форм вся
    видимая часть строится кодом в PrepareForm, .dfm хранит только унаследованные
    от TFrmBasicGrid2 геометрию панелей и RTF печати - поэтому клонирование .dfm
    с заменой имени класса безопасно). Отличия от архивной формы: другая таблица
    (v_sgp_new_format_items), нет dt_snapshot, кнопка "Архив по старым заказам"
    (Tag = 1000, локальный для формы, не требует регистрации в общем реестре
    кнопок - см. uForms.pas, поиск по myDefaultBtns идёт по значению, необнаруженный
    тег просто не переопределяет уже заданный инлайн Caption) - ведёт на старый
    архивный отчёт через Wh.ExecReference(myfrm_Rep_Sgp, Self, [], Null).
    Регистрация:
      - uData.pas: добавлена константа myfrm_Rep_SgpNew='Rep_SgpNew' (рядом с
        myfrm_Rep_Sgp).
      - uWindows.pas: добавлена ветка диспетчера (TWindowsHelper.ExecReference)
        "else if AFormType = myfrm_Rep_SgpNew then TFrmOGrepSgpNew.Show(...)"
        рядом со старой веткой myfrm_Rep_Sgp; uFrmOGrepSgpNew добавлен в uses.
      - uFrmMain.pas: пункт главного меню "Текущее состояние СГП (стандартные
        изделия)" теперь ведёт на myfrm_Rep_SgpNew вместо myfrm_Rep_Sgp - старый
        архивный отчёт остался полностью рабочим, но доступен только из новой
        формы (кнопка "Архив по старым заказам"), из главного меню больше
        напрямую не вызывается.
      - Роль доступа не менялась - обе формы используют rOr_Rep_Sgp_V (пункт
        меню) и rOr_Rep_Sgp_ViewPrice (видимость колонок цены).
    ЕЩЁ НЕ СДЕЛАНО - см. список ниже.

Исправлено (07.09.2026, часть 4 - ошибка в первой версии v_sgp_new_format_items):
после подключения формы обнаружилось, что комбобокс группы содержал только "[все]",
таблица была пустой. Причина - неверное предположение при проектировании: считал,
что признак or_std_items.by_sgp стоит на ПРОИЗВОДСТВЕННЫХ изделиях (эталонах),
и фильтровал базовую выборку отчёта условием "p.by_sgp = 1" на производственной
строке. Пользователь подтвердил факт по реальным данным (700+ строк с by_sgp=1,
почти все с ofe.type=1) - by_sgp физически стоит ТОЛЬКО на ОТГРУЗОЧНЫХ изделиях,
у производственных всегда 0 (несмотря на комментарий в uOrders.pas про
"относится только к производственному изделию" - тот комментарий описывает
смысловую принадлежность поля к производственной части техпроцесса и поведение
синхронизации новых отгрузочных строк (by_sgp туда не копируется, а не то, что
флаг физически стоит на производственной строке).
Исправление в v_sgp_new_format_items (d_sgp.sql): базовая выборка "g" теперь
фильтрует производственный эталон не по своему by_sgp (условие убрано), а по
наличию exists (select 1 from or_std_items ship where ship.id_prod_std_item =
p.id and ship.by_sgp = 1) - т.е. "есть ли у этого производственного изделия
хотя бы одно связанное через id_prod_std_item отгрузочное изделие с пометкой
учёта по СГП". Остальные подзапросы (prc - средняя цена, sell/shp/toship/ai/ao
через coalesce(id_prod_std_item, id)) не менялись - они изначально не зависели
от того, на какой стороне стоит by_sgp, и остаются верными.
ВАЖНО для будущих задач: by_sgp - признак ОТГРУЗОЧНОГО изделия ("Учёт по СГП"),
а не производственного, несмотря на формулировку в старом комментарии uOrders.pas.

Исправлено (07.09.2026, часть 4а - краш при первом запуске новой формы):
при первом запуске uFrmOGrepSgpNew.pas у пользователя, пока значение комбобокса
CbFormat ещё не сохранено в конфиге, Frg1.GetControlValue('CbFormat') возвращает
пустую строку (не Null) - сравнение '' >= 0 в Delphi бросает исключение (Invalid
variant operation). Это известная в проекте проблема (см. аналогичный комментарий
и обход через принудительный ItemIndex:=0 в uFrmOGrefOrStdItems.pas, SetCbEstimate) -
но сам ItemIndex:=0 не помогает, если персистентный слой всё равно возвращает ''
при следующем чтении контрола. Исправлено в Frg1OnSetSqlParams: пустое значение
(S.NSt(FIdFormat) = '') принудительно заменяется на -1000 (тот же смысл, что и
явный выбор "[все]" в комбобоксе) ДО сравнения ">= 0". Тот же паттерн, вероятно,
стоит когда-нибудь применить и к архивной uFrmOGrepSgp.pas (там код идентичный,
не трогали - не просили).

Исправлено (07.09.2026, часть 4б - забыл перепометить тег после правки уже
применённого объекта): после того как пользователь один раз прогнал
v_sgp_new_format_items через SqlUpdater, тот сам переписал тег с --!+ на --$+
("уже применено") прямо в файле d_sgp.sql на диске. Когда я затем правил ТЕЛО
этого же view (фикс by_sgp, см. выше), тег не тронул - в результате SqlUpdater
не увидел изменений и не переприменил бы правку. ВЫВОД - ОБЩЕЕ ПРАВИЛО НА
БУДУЩЕЕ: перед правкой SQL-объекта, у которого уже стоит --$+ (или --$go),
обязательно менять тег на --!+ (или --!go) как часть той же правки - иначе
следующий прогон SqlUpdater правку молча пропустит.

УТОЧНЕНИЕ ПРАВИЛА (07.09.2026, после правки v_rep_suppliers_negative_demand в разделе
3 про снабжение): это правило - ПО УМОЛЧАНИЮ, а не абсолютное. Если правка разовая и
тег для апдейтера не нужен (например, пользователь применяет изменение вручную и не
хочет, чтобы SqlUpdater его повторно трогал) - пользователь явно скажет об этом и/или
уберёт тег сам, это НЕ ошибка, тег в этом случае восстанавливать не нужно. Также
пользователь предупредит отдельно, когда для конкретной правки не нужны развёрнутые
поясняющие комментарии (как я обычно добавляю с датой и обоснованием) - в этом случае
тоже не нужно их дописывать/восстанавливать самому.

Сделано (07.09.2026, часть 5 - перенос остатка снимка + все акты в
v_sgp_new_format_items): пользователь сообщил, что по новому формату списки
групп/изделий в живом отчёте были верными, но количества по всем позициям -
нулевые, хотя в замороженном снимке (старый формат) на момент перехода уже
был реальный физический остаток на складе по этим же изделиям (движения
по новому формату просто ещё реально не было). Обсуждали два варианта -
сравнение по имени прямо в самом view, или разовая привязка снимка к
производственному эталону через id_prod_std_item. Пользователь выбрал
второй вариант ("снимок предлагаю изменить - добавить в него и обсчитать
ссылку на произв изделие, а не добавлять логику сравнения по имени в сам
вью сгп"), а также подтвердил, что ручные и авто-акты по СГП нужно учитывать
ВСЕ, независимо от того, к заказу какого формата (старого или нового) и как
созданы (автоматически stage-триггером или вручную) они относятся.
  1) d_sgp.sql - в sgp_snapshot_std_items добавлена колонка id_prod_std_item
     (number(11), fk на or_std_items) и разовый --!go backfill-блок,
     заполняющий её сопоставлением по (id_or_formats, item_name) с активным
     производственным эталоном (type=0, active=1), только при ровно одном
     совпадении по имени в группе - тот же паттерн, что и более ранний
     backfill or_std_items.id_prod_std_item. Диагностический select в конце
     блока показывает несопоставленные строки снимка.
  2) v_sgp_new_format_items полностью переработан:
     - новый left join snap - сумма qnt из sgp_snapshot_std_items по
       id_prod_std_item (стартовый остаток на дату перехода на новый
       формат), плюс max(dt_snapshot) по семье;
     - ai/ao (ручные+авто акты прихода/списания) переписаны - убрано
       ограничение "только по заказам нового формата", теперь берутся ВСЕ
       акты семьи (через coalesce(id_prod_std_item, id)), отфильтрованные
       по дате акта позже даты снимка ЭТОЙ семьи (то, что было до снимка,
       уже включено в snap.qnt выше); если для семьи снимка нет (изделие
       появилось уже после перехода на новый формат) - берутся вообще все
       акты по ней без ограничения по дате;
     - формулы qnt/qnt_need/summ/sumraw дополнены слагаемым nvl(snap.qnt, 0).
     - тег вида (--$+, SqlUpdater переприменил и снова перезатёр после
       предыдущей правки by_sgp) перед правкой снова возвращён на --!+.
  Итог - отчёт теперь должен показывать ФАКТИЧЕСКИЙ физический остаток на
  складе (снимок + всё движение после него), а не только движение с даты
  перехода на новый формат. Требуется прогнать SqlUpdater и перепроверить
  отчёт на тестовой базе - особенно по изделиям, у которых на дату снимка
  был реальный остаток.

Подтверждено (07.09.2026): пользователь протестировал часть 5 на тестовой базе -
остаток на дату снимка подтянулся корректно, а также появились изделия по
тестовым заказам НОВОГО формата, запущенным днём ранее - живое движение считается верно.

Сделано (07.09.2026, часть 6 - детализация по двойному клику для живого отчёта
нового формата): пользователь попросил вернуть детализацию по даблклику, которая
была у старого живого отчёта (uFrmOGrepSgp.pas) до перевода того в архив (тогда
Frg1OnDbClick и обслуживавший её диалог uFrmOGinfSgp.pas были отключены - см. выше,
"Сделано (07.09.2026, uFrmOGrepSgp.pas, uFrmXGlstMain.pas)"). Решение пользователя -
не создавать копию, а переделать сам uFrmOGinfSgp.pas под новый формат, раз старому
отчёту он больше не нужен.
  1) uFrmOGinfSgp.pas переработан: ID теперь означает g.id_prod_std_item (семья
     производственного эталона), а не отдельный отгрузочный подформат/имя, как
     было раньше. Заголовочный лейбл теперь берёт группу и имя из
     v_sgp_new_format_items (по id_prod_std_item). Все 7 разрезов (PspSell, PspProd,
     Registered, Shipped, Shipped_Plan, In_Prod, Move) перенаправлены на новые вью.
     Frg1CellButtonClick (кнопка "Паспорт" на слеше) и Frg1OnSetSqlParams не
     потребовали изменений - работают по тому же ID.
  2) d_sgp.sql - добавлены детализирующие вью (все ключ - g.id_prod_std_item, как в
     v_sgp_new_format_items, данные - только заказы нового формата, кроме актов):
     v_sgp_new_psp_prod_list, v_sgp_new_psp_sell_list, v_sgp_new_registered_list,
     v_sgp_new_shipped_list, v_sgp_new_in_prod_list, v_sgp_new_to_shipped_list -
     детализация соответствующих колонок отчёта (по одному ряду на заказ/событие
     этапа), и v_sgp_new_move_list - объединённая лента движения (клик по колонке
     "Текущий остаток"): строка "остаток на дату снимка" (стартовый остаток семьи
     из sgp_snapshot_std_items, если есть) + приёмка (+) + отгрузка (-) + акты
     прихода (+) + акты списания (-), акты - все, после даты снимка ЭТОЙ семьи (или
     все, если снимка нет) - логика идентична агрегатным snap/ai/ao в
     v_sgp_new_format_items, чтобы сумма ленты сходилась с колонкой "qnt" отчёта.
  3) uFrmOGrepSgpNew.pas - добавлен обработчик Frg1OnDbClick, определяющий разрез по
     имени столбца (qnt_psp_sell/qnt_psp_prod/qnt_sgp_registered/qnt_shipped/
     qnt_to_shipped/qnt_in_prod/qnt) и вызывающий TFrmOGinfSgp.Show(...) с
     соответствующей константой myfrm_Dlg_Sgp_InfoGrid_* и Fr.ID (=id_prod_std_item).
     uFrmOGinfSgp добавлен в uses. Диспетчер в uWindows.pas не трогали - у этих
     диалогов константы ADoc вызываются напрямую через .Show(), без центрального
     ExecDialog (так было устроено изначально).
  Требуется проверить на тестовой базе: клик по каждой из 7 колонок открывает
  соответствующую детализацию, суммы в ленте "Движение" сходятся с колонкой
  "Текущий остаток" отчёта.

Ещё не сделано:
  - ВАЖНО, отложено по прямому указанию пользователя (07.09.2026) - не делать, пока
    не будет протестирована детализация выше: везде, кроме журнала заказов
    (uFrmOGjrnOrders.pas) и сопутствующего проведения заказов менеджерами, нужно
    отсекать заказы НОВОГО формата со статусами, отличными от "Запущен" и
    "Остановлен" - такие заказы вообще нигде больше проходить не должны (в т.ч.,
    видимо, в v_sgp_new_format_items и во всех v_sgp_new_*_list/v_sgp_new_move_list
    выше - нужно уточнить у пользователя точный список мест и способ фильтрации
    перед реализацией).
  - Протестировать автосоздание актов СГП по старым заказам (см. выше) на
    реальных данных.
  - Запустить --!go блоки id_prod_std_item (d_orders.sql и backfill в
    d_temp.sql) на реальной базе, посмотреть на список несопоставленных
    отгрузочных изделий после backfill (стандартных - селектом в d_orders.sql
    сразу после backfill-блока).
  - Прогнать SqlUpdater и запустить --!go backfill-блок id_prod_std_item в
    d_sgp.sql (sgp_snapshot_std_items) на тестовой базе, посмотреть
    диагностический select на несопоставленные строки снимка.
  - Проверить переработанный живой отчёт "Текущее состояние СГП"
    (v_sgp_new_format_items, uFrmOGrepSgpNew.pas, пункт меню "Текущее
    состояние СГП (стандартные изделия)") на тестовой базе после части 5 -
    особенно что остаток на дату снимка теперь корректно подтягивается и
    складывается с движением после снимка.
  - Отдельный новый отчёт для нестандартных изделий нового формата
    (ключевое отличие от стандартных - изделие встречается строго один
    раз в одном заказе, агрегация по заказам не нужна) - аналог уже
    существующего myfrm_Rep_Sgp2 (нестандартные изделия старого формата,
    generic-форма TFrmXGlstMain, см. uFrmXGlstMain.pas), но для новых
    заказов - ещё не начато.


=================================================================
РАЗДЕЛ 3. СНАБЖЕНИЕ: ОТЧЁТ ПО ОТРИЦАТЕЛЬНОЙ ПОТРЕБНОСТИ
=================================================================

Сделано (07.09.2026): пользователь попросил доработать серверную задачу
TTasksS.ReportForSuppliersNegativeDemand (uServerTasks.pas) - ежедневную рассылку
по сырью с отрицательной потребностью (need_m < 0, т.е. остатка+резерва+"в пути" не
хватает даже до минимального остатка):
  1) добавлена колонка "Стоимость потребности с учетом остатка" (need_m_cost =
     round(price_main * need_m)) - цена взята price_main (цена последнего прихода по
     любому поставщику), т.к. этим же полем в v_spl_minremains уже считаются
     qnt_cost/order_cost/onway_cost - для единообразия. Пользователь подтвердил выбор
     цены; про саму "минимальную потребность" пользователь пояснил, что имел в виду
     уже существующую колонку need_m ("Потребность с учетом остатка") - отдельную
     колонку под таким названием заводить не стали, добавили только её стоимость.
  2) рассылка теперь разбита по категориям заявок на снабжение (спр. spl_categoryes,
     назначается на номенклатуру полем spl_itm_nom_props.id_category, например
     "снабжение" (id=1), "инструмент" и т.д. - конкретный список категорий не
     хардкодится, берётся из справочника) - каждая категория отдельной таблицей в теле
     письма (с заголовком-названием категории) И отдельным приложенным xlsx-файлом
     (было - одна общая таблица и один файл на всё). Номенклатура без категории
     (id_category is null) попадает в группу "без категории", идёт последней.
  3) новая вью v_rep_suppliers_negative_demand (d_suppliers.sql, рядом с
     v_spl_minremains) - v_spl_minremains + join на spl_categoryes для имени категории
     + колонка need_m_cost. Саму v_spl_minremains (используется много где ещё) не
     трогали - специально сделали отдельную вью поверх неё, по аналогии с другими
     отчётными вью в этом файле (v_rep_for_orders_planned_to_start_tomorrow и т.п.).
  4) в Delphi-процедуре - список категорий, по которым вообще есть отрицательная
     потребность, получается отдельным (лёгким) select distinct, дальше в цикле по
     каждой категории отдельным запросом (тот же паттерн Q.QGetSql+Fields, что и был)
     строится таблица для письма и отдельный xlsx; итоговый HTML - конкатенация
     таблиц по категориям, итоговый список файлов - конкатенация имён файлов
     (FileToSendArr := FileToSendArr + [FileToSend], стандартная для проекта идиома
     конкатенации динамических массивов, см. также D_Order.pas).
  Требуется проверить на тестовой/реальной рассылке: суммы стоимости адекватны,
  разбивка по категориям соответствует ожиданиям (в т.ч. группа "без категории",
  если такая номенклатура есть).

Доработано пользователем (07.09.2026) вручную поверх сделанного выше:
  - добавлена ещё одна пара колонок: need ("Мин. потребность" - потребность БЕЗ учета
    минимального остатка, в отличие от need_m) и need_cost (её стоимость);
  - формула стоимости (для need_cost и need_m_cost) уточнена - цена теперь берётся
    price_main, а если он null - price_check (контрольная цена) как запасной вариант
    (было - только price_main, при null стоимость считалась по 0); также оба значения
    оборачиваются в abs(), чтобы стоимость всегда была положительным числом, независимо
    от знака потребности (need/need_m отрицательные, т.к. это дефицит).
  - я восстановил только тег SqlUpdater у v_rep_suppliers_negative_demand (--!+) - при
    ручной правке он пропал, а без тега апдейтер не подхватит вью; остальную часть
    правки не трогал.

РАЗДЕЛ 4. РАБОТНИКИ: ЖУРНАЛ ПЛАНОВЫХ НАЧИСЛЕНИЙ ПО ДОЛЖНОСТЯМ
=================================================================

Сделано (07-08.09.2026): по таблице w_job_salaries (id_job, dt, fixed_pay, variable_pay -
суммарное начисление НЕ хранится, только фиксированная и стимулирующая часть, PK
(id_job, dt), FK на w_jobs), заведённой пользователем в d_workers_new.sql, реализован
новый журнал в модуле Работники:
  1) вью v_w_job_salaries_grid (d_workers_new.sql) - по одной строке на активную
     должность (w_jobs.active=1), с прошлым/текущим/следующим календарным месяцем
     (trunc(sysdate,'mm') и соседние), по каждому - fixed_pay/variable_pay и вычисляемое
     суммарное (не хранится); флаг can_edit_cur=1, если по должности нет ни одной строки
     за прошлый месяц.
  2) вью v_w_job_salaries_history - плоская история начислений по должности, для
     детальной таблицы журнала.
  3) процедура p_w_job_salaries_set_value(p_id_job, p_dt, p_field, p_value) - сохраняет
     одно значение (FIXED_PAY/VARIABLE_PAY); если строки за месяц ещё нет - создаёт,
     незаданную явно графу обнуляет; дублирует на сервере бизнес-правило
     редактируемости (см. ниже).
  4) процедура p_w_job_salaries_fill_next_month + задание шедулера
     w_job_salaries_fill_next_month_job (d_sheduled_tesks.sql, ежедневно) - переносит
     текущий месяц в следующий, если по должности заполнен текущий, но не заполнен
     следующий.
  5) роли rW_J_JobSalaries_V ('2-105', просмотр) и rW_J_JobSalaries_Ch ('2-106', ввод
     данных) + пункт меню "Плановые начисления по должностям" в Работники - Журналы,
     рядом со "Статусы работников".
  6) новый модуль uFrmWGjrnJobSalaries.pas/.dfm (класс TFrmWGjrnJobSalaries) - Frg1:
     список должностей, объединённые заголовки по месяцам ("Август 2026" и т.п., синтаксис
     'Группа|Подпись'/'!Подпись'), галочка "Редактировать" вверху (доступна только при
     наличии права на запись); Frg2: история по выбранной должности, только просмотр.
     Бизнес-правило редактируемости: следующий месяц - всегда, текущий - только если по
     должности нет ни одной записи за прошлый месяц (Frg1GetCellReadOnly, по флагу
     can_edit_cur); сохранение ячейки - через Frg1CellValueSave, вызывает
     p_w_job_salaries_set_value.

Доработано пользователем (08.09.2026) вручную поверх сделанного выше:
  - имя модуля/юнита исправлено на uFrmWGjrnJobSalaries (было по ошибке
    uFrmWJrnJobSalaries, затем uFrmWGJrnJobSalaries) - под реальное соглашение (сравни
    uFrmWGjrnEmployees), файл подключен в Uchet.dpr;
  - формат числовых колонок 'f:' заменён на 'r'; заголовок "Стимулирующая" перенесён на
    две строки ('Стиму-'#13#10'лирующая') под ширину колонки; сортировка в Frg1 больше не
    отключается (убрано '- [myogSorting]');
  - добавлено Frg1.GridReadOnly (общий переключатель грида по галочке "Редактировать",
    выставляется и в PrepareForm, и в Frg1AddControlChange через Frg1.InvalidateGrid) -
    работает вместе с Frg1GetCellReadOnly, который остаётся точечным правилом по колонкам;
  - расширен текст подсказки (Frg1.InfoArray), в т.ч. добавлена строка "Данные для
    расчетных ведомостей и штатного расписания берутся из этого журнала" - подразумевает
    интеграцию с "Расчетные ведомости" и "Штатное расписание"; сама интеграция (откуда
    именно и как эти экраны должны брать плановые начисления из w_job_salaries) пока НЕ
    реализована - потребуется отдельная задача, если нужно.
  Изменения от пользователя проверены (структура, BOM/CRLF, скобки) и не трогались -
  проект успешно собрался (Uchet.exe/Работники.exe пересобраны).


РАЗДЕЛ 5. МОБИЛЬНЫЙ СКАНЕР ШТРИХКОДОВ ПРОДУКЦИИ
=================================================================

Сделано (10.09.2026): первая (тестовая) версия связки "телефон сканирует штрихкод -
Учет показывает информацию и выполняет действие". Просьба пользователя: сканирование
продукции, по успешному сканированию - информация на телефоне и определённые действия
в Учете; первое действие - приёмка на СГП (потом добавилась и отгрузка).

  Архитектура (согласована с пользователем через уточняющие вопросы):
  - клиент - НЕ отдельное Delphi FMX приложение, а мобильная веб-страница (scan.html),
    открывается в Chrome на Android; только локальная Wi-Fi сеть цеха/офиса, интернет
    не нужен - библиотека сканирования (@zxing/library, UMD) вшита в сам html-файл;
  - сервер - НЕ отдельный .exe, а новая задача командной строки /scanapi в уже
    существующем диспетчере TTasksS.Run (uServerTasks.pas) - поднимает HTTP-сервер на
    Indy (TIdHTTPServer, новый модуль uScanApi.pas) и, в отличие от прочих задач, НЕ
    завершает процесс (правка - FrmMain.Close теперь не вызывается для этой задачи) -
    Indy сам обслуживает запросы в фоновых потоках, пока висит процесс Uchet.exe;
  - scan.html лежит рядом с exe, отдаётся тем же сервером (GET / и /scan.html).

  Штрихкоды - по ходу обсуждения пользователь дважды менял схему:
  - сначала завели физическое поле order_items.barcode (+триггер +бэкфилл +уникальный
    индекс) под старую задачу "только изделия заказа";
  - затем пользователь расширил задачу на три типа сущностей (стандартное изделие,
    изделие заказа, работник) и решил, что штрихкод - это ПРЕФИКС ТИПА (2 буквы,
    жёстко задан для типа) + id самой сущности, отдельное поле в БД заводить не надо,
    где можно без него обойтись - код просто разбирается на сервере (ParseBarcode
    в uScanApi.pas): SI<id> - or_std_items.id, OI<id> - order_items.id, EM<id> -
    w_employees.id (он же бейджик для входа, см. ниже);
  - т.к. SQL с order_items.barcode пользователь ещё не прогонял - откатили его
    полностью (колонка/индекс/бэкфилл/правка триггера убраны из d_orders.sql без
    следа, триггер trg_order_items_bi_r вернулся к исходному виду) - order_items
    теперь тоже участвует в общей беспамятной (без хранения) схеме OI<id>.

  Вход по бейджику - обязателен для ЛЮБОГО действия в сканере (в т.ч. простого
  просмотра карточки изделия), по решению пользователя:
  - каждый телефон/браузер сам генерирует и хранит у себя случайный ClientId
    (scan.html, localStorage) и передаёт его на сервер при каждом запросе;
  - сервер держит в памяти (uScanApi.pas, FSessions: TStringList под
    TCriticalSection - Indy обслуживает разные соединения в разных потоках) карту
    ClientId -> id вошедшего работника;
  - сканирование EM-бейджика - переключатель: чужой бейджик (или бейджик, когда
    никто не вошёл) - вход (авто-смена работника на этом телефоне БЕЗ обязательного
    явного выхода - так решил пользователь, это нормально для одного телефона на
    несколько человек за смену); свой же бейджик повторно - выход, дальше на этом
    телефоне ничего сделать нельзя, пока кто-то не войдёт заново.

  Права на действия (приёмка/отгрузка) - пользователь решил делать ПОЗЖЕ отдельной
  задачей. Промежуточный шаг (заведение w_employees.scan_can_accept/scan_can_ship)
  пользователь тоже отменил - SQL ещё не прогонялся, откатили без следа. Сейчас
  v_scan_api_employee (d_scan_api.sql) отдаёт эти поля константой 1 (разрешено всем) -
  когда будет реальная система прав, здесь появится настоящее условие, остальной код
  (uScanApi.pas, scan.html) трогать не придётся - он просто читает эти два поля из вью.

  Приёмка/отгрузка (только для типа OI) выполняются вызовом уже существующей и
  проверенной процедуры p_OrderStage_SetItem (d_order_stages.sql, stage=2/3) в режиме
  добавления (Adding=1) - по прецеденту uFrmOWInvoiceToSgp.pas; для SI (стандартное
  изделие) в первой версии никаких действий нет, только карточка с информацией.

  Запрошено на будущее (не реализовано): кнопка "Отмена последнего сканирования" -
  пользователь пояснил, что она понадобится для действий вроде приёмки (т.е. отмена
  последнего вызова p_OrderStage_SetItem, а не просто сброс экрана) - когда будет
  делаться, скорее всего это тот же вызов с отрицательным количеством.

  Файлы: uScanApi.pas (новый, весь HTTP-сервер), scan.html (новый, лежит рядом с exe),
  SQL/d_scan_api.sql (новый - вью v_scan_api_order_item/v_scan_api_std_item/
  v_scan_api_employee, на основе уже существующих v_order_items/v_or_std_items/
  v_w_employees), uServerTasks.pas (задача /scanapi + правка FrmMain.Close),
  Uchet.dpr/.dproj (регистрация uScanApi.pas).

  Не проверено (нет возможности): компиляция Delphi-проекта - все правки сделаны по
  чтению кода и аналогии с прецедентами (uFrmOWInvoiceToSgp.pas и др.), сам не
  компилирую и не запускаю; scan.html прогнан через headless Chromium (Playwright,
  подменённые /api/*) - загрузка ZXing, вход/выход, все три карточки, экранирование
  кириллицы/спецсимволов, действие приёмки - без ошибок, но это не замена проверке на
  реальном телефоне.

Сделано (10.09.2026, отладочные инструменты сканера): по ходу тестирования
потребовались два вспомогательных инструмента, оба - только для отладки, на сам
API/протокол сканера не влияют.

  Окно "живого" лога сканирований (uScanApi.pas):
  - при первом обращении к /api/scan|accept|ship лениво создаётся немодальное окно
    (TForm.CreateNew без dfm + TMemo) и показывает каждый запрос (штрихкод, ClientId,
    время) построчно, максимум 1000 строк (старые удаляются);
  - т.к. Indy обслуживает запросы не в главном потоке VCL, запись в мемо всегда идёт
    через TThread.Queue (метод LogDebug, можно звать из любого потока).

  Экранный штрихкод для проверки сканером без печати бейджика (новый юнит
  uBarcode128.pas + Ctrl+Shift+B в uFrmWGjrnEmployees.pas):
  - в проекте не было готового способа показать штрихкод НЕ на печати - штатный
    механизм (TfrxBarCodeView, FastReport) целиком завязан на заранее загруженный
    .fr3-шаблон, а таких шаблонов нет ни в репозитории, ни под рукой (символика
    штрихкода задаётся в самом шаблоне, не в коде) - поэтому сделан отдельный
    генератор Code128 напрямую в Delphi;
  - реализован только набор символов B (без переключения на A/C) - этого достаточно,
    т.к. все штрихкоды системы имеют вид <2 буквы-префикс><число>, то есть всегда в
    диапазоне кодов 32..95;
  - таблица штрихов и алгоритм контрольной суммы (mod 103) сначала проверены в
    песочнице: сгенерирован тестовый штрихкод и распознан обратно той же библиотекой
    ZXing, что использует scan.html (round-trip через headless Chromium), включая
    отрицательный тест - штрихкод с намеренно неверной контрольной суммой ZXing не
    распознаёт - только после этого таблица перенесена в Delphi;
  - Ctrl+Shift+B на выделенной строке в журнале работников (uFrmWGjrnEmployees.pas)
    открывает окошко со штрихкодом EM<id> текущего работника - можно сразу навести
    телефон и проверить сканирование, не печатая настоящий бейджик.

  Заметка (не код): на Android (Xiaomi, Chrome) камера сканера не открывалась -
  "cannot read properties of undefined (reading 'getUserMedia')" - причина в том, что
  страница открыта не по HTTPS и не на localhost, поэтому navigator.mediaDevices
  вообще не создаётся браузером; лечится флагом chrome://flags/#unsafely-treat-
  insecure-origin-as-secure (уже настраивался ранее) - но также нужен полный
  перезапуск браузера после включения флага, простого обновления страницы
  недостаточно.

  Файлы: uBarcode128.pas (новый - кодировщик Code128B + отрисовка + немодальное
  окно), uFrmWGjrnEmployees.pas (правка - Frg1DbGridEh1KeyDown, Ctrl+Shift+B),
  uScanApi.pas (правка - окно живого лога), Uchet.dpr/.dproj (регистрация
  uBarcode128.pas).

Сделано (10.09.2026, печать штрихкода + распространение на заказы и
справочник изделий): по итогам предыдущего шага (экранный штрихкод для отладки,
см. выше) пользователь попросил добавить реальную печать штрихкода, и повторить
всю функциональность (экран + печать) ещё в двух местах - в позициях заказа и в
справочнике стандартных изделий.

  Печать (uBarcode128.pas, новая процедура PrintBarcode128):
  - готового способа печатать НЕ через FastReport в проекте не было вообще (весь
    поиск дал только один файл, использующий модуль Printers - и тот только ради
    константы poLandscape для готового грид-принтера EhLib, без единого
    самостоятельного BeginDoc/Canvas/EndDoc) - поэтому печать сделана напрямую
    через TPrinter, благо DrawCode128Bars (см. выше) изначально принимает
    абстрактный TCanvas и одинаково работает что с экраном, что с Printer.Canvas;
  - перед печатью - стандартный TPrintDialog (выбор/подтверждение принтера - у
    бейджей/этикеток обычно отдельный принтер, не совпадающий с обычным офисным);
  - ширина модуля и высота штрихов считаются в пикселях из РЕАЛЬНОГО DPI
    выбранного принтера (GetDeviceCaps(Printer.Handle, LOGPIXELSX/Y) - доступно
    только между BeginDoc и EndDoc), а не захардкожены - модуль получается около
    0.4 мм независимо от разрешения конкретного принтера.

  Горячие клавиши (единая схема везде): Ctrl+Shift+B - показать штрихкод на
  экране (как раньше), Ctrl+Alt+Shift+B - напечатать. Реализовано в трёх местах:
  - uFrmWGjrnEmployees.pas (журнал работников) - EM<id>, было и раньше, добавлена
    только печать;
  - uFrmOGjrnOrders.pas (журнал заказов) - на детальном гриде Frg2 (позиции
    заказа, v_order_items) - OI<id>; у мастер-грида Frg1 (сами заказы) уже был
    свой обработчик клавиш (Ctrl+Alt+W - просмотр всех заказов), он не затронут -
    штрихкод повешен отдельным обработчиком только на Frg2;
  - uFrmOGrefOrStdItems.pas (справочник стандартных изделий) - на Frg1
    (v_or_std_items) - SI<id>.
  Во всех трёх местах - тот же приём, что и в самом первом варианте (см. выше):
  сохраняем прежний DbGridEh1.OnKeyDown в поле FOldFrgNDbGridEh1KeyDown, вызываем
  его первым, свою проверку - после, чтобы не потерять уже существующие горячие
  клавиши грида.

  Файлы: uBarcode128.pas (правка - PrintBarcode128 + общая обёртка
  TryEncodeOrShowError), uFrmWGjrnEmployees.pas (правка - Ctrl+Alt+Shift+B),
  uFrmOGjrnOrders.pas (правка - Frg2DbGridEh1KeyDown, оба сочетания),
  uFrmOGrefOrStdItems.pas (правка - Frg1DbGridEh1KeyDown, оба сочетания).

Сделано (10.09.2026, разрешение камеры и фокусировка в scan.html):
пользователь сообщил, что штрихкод (что с экрана, что с бумаги, при разном
освещении) распознался всего один раз, дальше - камера открывается нормально,
картинка чёткая на глаз, но штрихкод не читается вообще; движение телефоном не
помогло. Отдельное специализированное приложение-сканер тот же штрихкод
распознаёт мгновенно.

  Причина - разница в том, что браузер и нативное приложение просят у камеры.
  Запрос потока в reader.decodeFromConstraints указывал только facingMode -
  никакого разрешения и режима фокусировки. Без явного запроса браузер часто
  выбирает скромный поток (порядка 640x480, как для видеозвонков) - на глаз
  выглядит нормально, но мелких деталей штрихкода объективно меньше, чем видит
  нативная камера на полном разрешении сенсора. Плюс видеопоток не всегда
  фокусируется непрерывно (фокусируется один раз при открытии, как для звонка).

  Правка (scan.html, reader.decodeFromConstraints): в constraints добавлены
  width/height с ideal (1920x1080 - не жёсткое требование, при отсутствии
  поддержки браузер молча возьмёт максимум доступного, ошибки не будет) и
  advanced: [{focusMode: 'continuous'}] - просьба фокусироваться непрерывно,
  тоже best-effort, на устройствах без поддержки просто игнорируется.
  Проверено headless Chromium с поддельной камерой (--use-fake-device-for-
  media-stream) - перехвачен реальный вызов getUserMedia, подтверждено, что
  ровно эти constraints уходят в браузер, поток открывается без ошибок на
  1920x1080. Настоящую проверку на реальном телефоне сделать не могу - нужно
  протестировать и сообщить, помогло ли.

  Если не поможет - следующий шаг: увеличить физический размер модуля печатного
  штрихкода (uBarcode128.pas, MODULE_MM сейчас 0.4) до 0.5-0.6 мм - крупнее
  модуль почти всегда снимает проблему ценой немного большей этикетки.
----------------------------------------------------------------------
Сканер штрихкодов (scan.html): найдена и устранена РЕАЛЬНАЯ причина
нечитаемости камерой - баг встроенной библиотеки ZXing, а не
разрешение/фокус камеры
----------------------------------------------------------------------

Дата: после записи выше про увеличение разрешения камеры и непрерывный
автофокус (см. предыдущую запись). Пользователь сообщил, что эта правка
эффекта не дала - штрихкод камерой по прежнему почти никогда не читался,
ни с экрана, ни с бумаги, при любом освещении, хотя обычное приложение-
сканер тот же самый штрихкод читает мгновенно и без проблем.

Это означало, что предыдущая гипотеза (низкое разрешение видеопотока и
однократный автофокус) была НЕВЕРНОЙ - реальная причина в другом.
Проведено отдельное расследование в изолированной среде:

1. Побайтово сверена таблица кодов Code128 и алгоритм контрольной суммы в
   uBarcode128.pas с независимым эталоном - расхождений нет, наша
   генерация штрихкода не является причиной проблемы.

2. Через headless-браузер с подменой камеры на заранее подготовленное
   видео с идеально чётким штрихкодом (без реальной камеры, освещения,
   фокуса и дрожания рук как переменных) баг удалось стабильно
   воспроизвести - то есть дело не в условиях съёмки.

3. Тот же самый кадр, сохранённый как обычная картинка, декодером
   распознаётся всегда и мгновенно (через одноразовый разбор
   изображения). Тот же кадр как часть НЕПРЕРЫВНОГО видеопотока -
   практически никогда. Значит, проблема именно в пути обработки живого
   видео внутри библиотеки декодирования, а не в качестве картинки.

4. Проверено на QR-коде (полностью другой формат штрихкода) - та же самая
   картина, то есть дело не в формате Code128, а в самой видео-ветке
   библиотеки ZXing в целом.

5. В исходном коде встроенной библиотеки ZXing (класс
   HTMLCanvasElementLuminanceSource, метод toGrayscaleBuffer) найден
   статический счётчик-переключатель (FRAME_INDEX), который инвертирует
   яркость кадра (чёрное<->белое) через раз, но ТОЛЬКО когда источником
   является видео-элемент (<video>) - при разборе обычных картинок этот
   переключатель не используется вовсе, поэтому там всегда всё исправно.
   На практике это значит, что примерно каждый второй кадр с камеры,
   прежде чем попасть в собственно распознавание штрихкода, приходит в
   виде фотонегатива - и штрихкод на нём принципиально не читается. Это
   настоящий баг стороннего кода библиотеки ZXing, а не наша ошибка и не
   следствие условий съёмки.

Исправление в scan.html (функция startScan/decodeLoop): вместо штатного
reader.decodeFromConstraints(...), который отдаёт декодеру кадры прямо с
<video> и тем самым включает баг, поток с камеры получаем через
navigator.mediaDevices.getUserMedia сами и в цикле сами же переносим
текущий кадр с <video> на обычный canvas (ctx.drawImage). Дальше вручную
собираем объект распознавания (HTMLCanvasElementLuminanceSource с явным
признаком "это не видео", HybridBinarizer, BinaryBitmap) и вызываем
reader.decodeBitmap(...) - у этого пути переключатель инверсии не
взводится вовсе, поэтому кадр разбирается так же надёжно, как обычная
картинка. Разрешение камеры и непрерывный автофокус из прошлой правки
сохранены как есть - вреда от них нет, просто не они были причиной.

Исправление проверено в изолированной среде (headless-браузер + подмена
камеры на заранее записанное видео с Code128- и QR-штрихкодом) - после
правки распознавание срабатывает стабильно на обоих форматах, тогда как
до правки в тех же условиях не срабатывало ни разу. Проверка на реальном
телефоне мне недоступна - нужно тестирование пользователем.

----------------------------------------------------------------------
uServerTasks.pas / uScanApi.pas: резидентный сервер /scanapi - самообновление,
минутный планировщик задач, устранён зависавший exception при занятом порту
----------------------------------------------------------------------

Задача: переделать серверный процесс так, чтобы он работал без завершения, сам
проверял обновления (раз в 5 минут) и корректно восстанавливался после краша
через повторный запуск задачей планировщика (тоже раз в 5 минут). Также нужен
минутный таймер для задач с жёстко заданным в коде временем, без проверки
пересечений между ними.

Перед реализацией проверил Uchet.dpr и обнаружил, что единственность самого
резидентного процесса УЖЕ обеспечена мьютексом (CheckInstance в Uchet.dpr) -
имя мьютекса учитывает и модуль, и переданный параметр командной строки. Для
варианта "Сервер" ($IFDEF SRV) при не удавшемся захвате мьютекса программа
тихо завершается (Halt, без диалога). То есть если планировщик Windows будет
пытаться запускать "Сервер.exe /scanapi" каждые 5 минут - при уже работающем
резидентном сервере такие попытки будут сами по себе мгновенно и тихо
завершаться, а если резидентный процесс упал - именно такая попытка его и
поднимет заново. Ничего дополнительно для этого в коде писать не потребовалось,
важно только настроить сам повтор в планировщике заданий Windows (это делается
отдельно от кода, повторение задачи /scanapi каждые 5 минут).

Для проверки обновлений (по просьбе пользователя - "приспособить" приём из
uUpdater.pas) сделано так же, как там: сравнение даты изменения файла exe
(TFile.GetLastWriteTime) с той, что была на момент старта текущего процесса,
без обращения к updater.dir/Uchet_Updater.exe - тот функционал в uUpdater.pas
привязан к запуску из %LOCALAPPDATA%\Uchet\ и рассчитан на скачивание файлов
с сетевого ресурса, что для сервера, который сам пересобирается прямо в месте
запуска, не требуется - здесь просто нужно заметить, что файл на диске стал
новее, и корректно завершиться.

Добавлен новый класс TScanApiWatchdog (uServerTasks.pas) - создаётся только в
резидентном режиме (задача /scanapi), держит два TTimer:
  - раз в 5 минут - сравнивает дату файла exe с датой на момент старта, и если
    она изменилась - логирует, останавливает HTTP-сервер (ScanApi.Stop) и
    закрывает приложение (FrmMain.Close), как обычную задачу;
  - раз в минуту - вызывает новую процедуру TTasksS.MinutelyTasks (объявлена в
    интерфейсе TTasksS рядом с HourlyTasks, пока пустая - задачи с конкретным
    временем добавляются туда постепенно, по аналогии с HourlyTasks, без
    проверки пересечений между ними, как и просил пользователь).
Перенос уже существующих задач планировщика (/fromparsec, /turvreport и т.п.)
на MinutelyTasks НЕ делался - это отдельная постепенная работа, сейчас только
каркас механизма.

Попутно устранена реальная причина, из-за которой раньше пропадало
диагностическое окно сервера (см. более раннюю запись про алгоритм
взаимодействия телефона с сервером): TScanApi.Start вызывал
FHttpServer.Active := True без обработки исключений - если порт уже занят
(например, предыдущий процесс не успел до конца его освободить после аварийного
завершения), необработанное исключение просто зависало на диалоге madExcept,
который некому было закрыть. Теперь эта установка обёрнута в try/except: при
неудаче пишется причина в лог (Module.ToLogFile) и Start корректно возвращает
управление с Active = False, без исключения. TTasksS.Run в этом случае теперь
тоже штатно завершает процесс (раньше при таком сценарии осталось бы неясно,
что вообще происходит) - следующая попытка запуска той же задачи планировщиком
(через 5 минут) начнёт всё заново.

Компиляцию Delphi проверить не могу - только побайтовая проверка кодировки
(UTF-8 с BOM, только CRLF) и баланс begin/end/скобок с учётом комментариев.
Нужна проверка сборкой и реальным запуском на сервере.

----------------------------------------------------------------------
Поправка: проверка обновлений резидентного сервера - без прямой перезаписи
работающего exe
----------------------------------------------------------------------

Пользователь поправил предыдущий вариант (сравнение даты файла exe с самим
собой на момент старта процесса): прямая перезапись исполняемого файла уже
запущенного приложения приводит к краху, так делать нельзя. Правильный подход -
как в uUpdater.CheckForUpdatesAndRunUpdater, эта инфраструктура (Updater\
updater.dir рядом с exe, сравнение с копией на сервере, сам обновлятор
Uchet_Updater.exe) уже есть и рассчитана именно на безопасную замену файла:
обновлятор запускается ОТДЕЛЬНЫМ процессом и дожидается закрытия обновляемого
файла, прежде чем его подменить.

Переделано: в uUpdater.pas добавлены три самостоятельные функции (без побочных
эффектов вроде MessageBox/Halt внутри самой проверки - они бы зависли на
headless-сервере) - GetUpdateServerBasePath (чтение updater.dir),
IsExeUpdateAvailable (сравнение дат exe с копией на сервере) и
RunUpdaterAndHalt (синхронизация папки Updater, запуск Uchet_Updater.exe с
текущими параметрами командной строки, Halt(0)). Сама функция
CheckForUpdatesAndRunUpdater (используется остальными, клиентскими модулями)
не тронута - расчитанный риск не трогать уже проверенную клиентскую логику.

TScanApiWatchdog.UpdateCheckTimerTimer (uServerTasks.pas) переписан: вместо
сравнения даты своего же файла с самим собой теперь читает Updater\updater.dir
рядом с exe сервера, сравнивает с копией на сервере (через новые функции выше),
и при наличии обновления - останавливает HTTP-сервер (ScanApi.Stop), запускает
Uchet_Updater.exe (RunUpdaterAndHalt - сам делает Halt(0) при успехе) и таким
образом закрывается; если запуск обновлятора не удался (например, нет
Uchet_Updater.exe рядом) - логирует ошибку и восстанавливает работу сервера
(ScanApi.Start), чтобы не остаться в подвешенном состоянии до следующей
попытки через 5 минут.

Предполагаю (не проверено, т.к. не вижу сервер живьём), что рядом с серверным
exe (там, где он реально запускается) уже есть своя папка Updater с
updater.dir, по тому же принципу, что и у клиентских модулей в
%LOCALAPPDATA%\Uchet\Updater\ - если это не так и такой папки там нет,
IsExeUpdateAvailable просто не будет находить обновлений (GetUpdateServerBasePath
вернёт False), и эта часть работать не будет, пока эта папка не появится.


----------------------------------------------------------------------
Сервер (uServerTasks.pas, uFrmMain.pas, Uchet.dpr): сервер стал полностью
резидентным для ВСЕХ задач, не только для сканирования штрихкодов.

Уточнение от пользователя: предыдущая реализация (см. предыдущую запись
выше) была слишком узкой - резидентным был сделан только /scanapi, а
остальные задачи (/fromparsec, /hourly и т.п.) по-прежнему завершали
процесс сразу после выполнения. Правильное намерение - сервер целиком
(независимо от того, с каким параметром командной строки он запущен)
работает резидентно неограниченно долго и никогда не завершается сам
после разовой задачи.

Изменения:

1) TTasksS.Run (uServerTasks.pas) теперь всегда запускает ScanApi.Start
   (сканирование штрихкодов работает всегда, независимо от параметра
   запуска - конфликтов по доступу к БД с прочими задачами нет, это
   подтверждено пользователем), выполняет разовую задачу по параметру
   через новую функцию TTasksS.ExecuteTaskByName, и создаёт
   TServerWatchdog - процесс НИКОГДА не закрывается сам (FrmMain.Close
   больше не вызывается).

2) TTasksS.ExecuteTaskByName(ATaskName: string): Boolean - разовая задача
   вынесена из Run в отдельную функцию, чтобы её можно было использовать
   и при старте, и при выполнении задания вручную (см. п. 6). Ошибка
   внутри задачи логируется (см. п. 4) и не прерывает работу резидентного
   процесса.

3) TScanApiWatchdog переименован в TServerWatchdog (класс теперь отвечает
   не только за сканер штрихкодов, а за весь резидентный сервер) и
   расширен: его минутный таймер теперь дополнительно -
   - проверяет соединение с БД (Q.Connected) - при потере соединения
     процесс сразу завершается (Halt), т.к. без БД не может работать ни
     сканирование, ни прочие задачи; следующая попытка - через 5 минут по
     сторожевому заданию планировщика;
   - в начале каждого часа (MinuteOf(Now) = 0) вызывает
     TTasksS.HourlyTasks - таким образом, часовые задачи теперь тоже
     запускаются из общего минутного таймера резидентного процесса, а не
     отдельным заданием планировщика "/hourly" (хотя это задание пока не
     удалено - см. заголовок uServerTasks.pas, миграция постепенная);
   - вызывает TTasksS.MinutelyTasks (без изменений в самом механизме);
   - и то и другое выполняется, только если разрешено расписание (см. п.5).

4) Логирование: добавлена LogTaskEvent(ItemName, Comment) - пишет
   одновременно и в файловый лог (Module.ToLogFile), и в БД (таблица
   adm_db_log через уже существующий метод TmyDBOra.QLog). Добавлена
   RunLoggedTask(TaskName, TaskProc: TProc) - выполняет переданную
   процедуру, логирует результат (выполнено/ошибка) через LogTaskEvent, и
   ошибка НЕ прерывает работу - используется для КАЖДОЙ отдельной
   процедуры внутри TTasksS.HourlyTasks (например, все ~18 отчётов в блоке
   8 часов утра теперь логируются индивидуально, а не одной строкой на
   всю пачку). Это решение одновременно закрывает два пожелания
   пользователя: обязательное логирование ошибок, и "если найдёшь иное
   решение [чем крах приложения через мадэксцепт] - отлично" - здесь
   ошибка одной задачи не приводит к падению всего резидентного процесса
   (и не мешает сканеру штрихкодов). Глобальный перехват необработанных
   исключений (madExcept, с завершением процесса) остаётся как
   дополнительная защита на случай ошибки вне этих обработчиков - это
   осталось приемлемым для пользователя, процесс перезапустится по
   сторожевому заданию планировщика.

5) Режим разработки: при наличии файла "dev" (Module.DevFileExists) при
   старте TTasksS.Run выводится вопрос (MyQuestionMessage) - выполнять ли
   задания по расписанию; ответ сохраняется в IsScheduleEnabled и
   проверяется в TServerWatchdog перед вызовом HourlyTasks/MinutelyTasks.
   Сканирование и проверка обновлений от этого флага не зависят.

6) Выполнение задания вручную: в TFrmMain (uFrmMain.pas) добавлен пункт
   главного меню "Выполнить задание..." (раздел {$IFDEF SRV}, виден
   всегда, не только в режиме разработки) - запрашивает имя задания
   (InputBox) и вызывает TasksS.ExecuteTaskByName, показывая результат.

7) Uchet.dpr: мьютекс единственности процесса для модуля Сервер (CheckInstance)
   изменён - раньше имя мьютекса учитывало параметр командной строки
   (что было нужно, пока разные параметры означали разные, независимо
   завершающиеся задачи); теперь, когда весь модуль резидентен независимо
   от параметра, имя мьютекса общее для модуля целиком (как и для
   остальных, не-серверных модулей) - иначе процессы, запущенные с разными
   параметрами, могли бы работать резидентно одновременно, дублируя
   выполнение задач и конкурируя за порт сканера штрихкодов. Проверка
   ParamCount = 1 (обязателен ровно один параметр) не изменилась.

Не сделано в этом раунде (оставлено на усмотрение пользователя, т.к.
явно только пользователь может расставить конкретное время): перенос
конкретных задач (/fromparsec, /turvreport и т.п.) с планировщика Windows
на жёстко заданное время внутри TTasksS.MinutelyTasks - процедура
специально оставлена пустой, с примером использования RunLoggedTask в
комментарии.


----------------------------------------------------------------------
Сервер (uServerTasks.pas, uFrmMain.pas): расписание задач переведено с
жёстко закодированных if-условий на декларативный массив с cron-подобным
расписанием и защитой от потерянных из-за просрочки запусков.

Уточнение от пользователя: не делать задания как ключи (строки вида
"/hourly", проверяемые вложенными if с HourOf(Now)/DayOfWeek(Date)), а
явно описывать задание как запись: краткое имя (на русском), необязательный
комментарий, выполняемая процедура, расписание в стиле cron. Обрабатывать
в минутном таймере по расписанию; если задача должна была выполниться, пока
выполнялась другая (запуск просрочен по этой причине, таких может быть
несколько), выполнять их после завершения предыдущей, в порядке нахождения
в массиве. Для ручного запуска - выбирать из заголовков (названий) заданий.

Реализация (uServerTasks.pas):

1) TScheduledTask - запись одной задачи расписания: Name (краткое имя на
   русском), Comment (необязательный), Cron (расписание, 5 полей через
   пробел - минута/час/день месяца/месяц/день недели, как в обычном cron:
   "*", списки через запятую, диапазоны "8-19", шаг "*/15" или "8-19/2"),
   Proc (TScheduledTaskProc = reference to procedure - сама задача).

2) CronFieldMatches/CronMatches - проверка, подходит ли конкретный момент
   времени под одно поле/под всё расписание целиком.

3) ScheduledTasks: array of TScheduledTask - само расписание, заполняется
   один раз при старте резидентного процесса функцией InitScheduledTasks.
   Сюда перенесены все задачи из прежней TTasksS.HourlyTasks - время каждой
   было и раньше жёстко задано в коде, здесь оно просто выражено через cron
   вместо вложенных if. Прежние часовые задачи -> "0 * * * *", задача в 4
   часа -> "0 4 * * *", пачка 8-часовых отчётов -> "0 8 * * *", задачи 1
   числа месяца -> "0 8 1 * *". Задачи "по понедельникам" оставлены с
   прежней проверкой DayOfWeek(Date) = 1 ВНУТРИ тела задачи (а не через
   поле дня недели в cron) - в модуле одновременно доступны DateUtils и
   SysUtils, у которых DayOfWeek может по-разному нумеровать дни недели, и
   чтобы не ошибиться при переносе, условие сохранено буквально таким же,
   как было в оригинале.
   Задачи, чьё время не было и раньше явно прописано в коде программы (перенос
   с параметров /fromparsec, /turvreport и т.п. - их время сейчас знает
   только планировщик Windows), в расписание пока НЕ добавлены - это
   отдельная, следующая работа.

4) ProcessScheduledTasks(AFrom, ATo) - проверяет расписание на каждую минуту
   диапазона [AFrom; ATo] (не только на текущую минуту) и выполняет
   совпавшие задачи строго в порядке их расположения в массиве
   ScheduledTasks - это и есть механизм защиты от просрочки: если минутный
   таймер не сработал вовремя (был занят выполнением предыдущей задачи),
   при следующем срабатывании AFrom/ATo захватывают весь пропущенный
   интервал, и ни одна задача, чьё время наступило за это время, не
   теряется. TServerWatchdog хранит FLastScheduleCheckMoment и передаёт в
   ProcessScheduledTasks диапазон от последней проверки до текущего момента.

5) TTasksS.ExecuteTaskByName: параметр командной строки "/hourly" убран -
   вся его нагрузка теперь в расписании и выполняется резидентным процессом
   сама, без внешнего запуска.

6) Для ручного запуска добавлены TTasksS.ScheduledTaskNames (имена всех
   задач расписания) и TTasksS.RunScheduledTask(индекс) (немедленное
   выполнение вне очереди). В uFrmMain.pas пункт меню "Выполнить задание..."
   переделан с ввода текста (InputBox) на выбор из списка - используется уже
   существующий в проекте диалог uFrmChooseDialog.TFrmChooseDialog.ShowDialog
   (радиогруппа, возвращает индекс выбранного варианта).

Не сделано в этом раунде (следующий шаг по мере необходимости): перенос
задач со старых параметров планировщика Windows (/fromparsec, /turvreport
и т.п.) в ScheduledTasks - для них ещё нет уже готового, однозначного
времени внутри кода программы, которое можно было бы просто выразить через
cron без риска что-то напутать.


----------------------------------------------------------------------
Сервер (Uchet.dpr, uFrmMain.pas, uServerTasks.pas, uData.pas, uWindows.pas,
uFrmXGlstMain.pas, SQL/d_adm.sql): дальнейшая доработка резидентного режима
и расписания заданий по запросу пользователя - 4 части.

1) Запуск сервера только параметром /run (или из-под IDE).

Раньше сервер запускался с любым распознаваемым параметром командной строки
(строка вида "/задача" - каждый обозначал конкретное разовое задание, см.
прежний TTasksS.ExecuteTaskByName). Теперь, так как весь функционал
выполняется резидентным процессом по расписанию (см. предыдущую запись в
этом файле про cron-массив), отдельные параметры на конкретные задачи больше
не нужны. Проверка вынесена в Uchet.dpr, сразу после проверки ParamCount <> 1
(то есть ДО создания главной формы и подключения к БД): если ParamStr(1) не
равен '/run', и при этом DebugHook <> 1 (то есть процесс не запущен из-под
IDE - см. этот же приём в TModule.GetRunFromIDE, uModule.pas), процесс
немедленно завершается (Halt). uFrmMain.TimerAfterMainShowTimer, соответственно,
больше не проверяет параметр и не спрашивает подтверждение - просто
безусловно вызывает TasksS.Run. Из uServerTasks.pas удалены: константа
TASK_SCAN_API, функция TTasksS.ExecuteTaskByName целиком (вместе со всеми
её ветками по прежним параметрам /turvreport1, /fromparsec,
/deleteolddata, /getcalendar, /calcplanned, /CloseItmWorkPeriod и т.п.) -
TTasksS.Run больше не вызывает её и не делает вообще никакого разбора
параметра. Планировщик Windows должен быть настроен на запуск "Сервер.exe
/run" (единственный вариант) каждые 5 минут - как и раньше, это даёт
"сторожевой" эффект (если резидентный процесс уже жив, новый запуск сразу
завершится по мьютексу).
Замечание: DeleteOldData, GetProductionCalendar, ReportForOrdersWithoutEstimate,
CloseItmWorkPeriod остались объявлены и реализованы в TTasksS, но сейчас
ничем не вызываются (раньше вызывались только через удалённые параметры
командной строки) - перенос их в расписание (ScheduledTasks) не делался в
этом раунде, так как для них нет уже готового, однозначного времени
выполнения внутри кода, которое можно было бы просто выразить через cron.

2) Структурированный журнал выполнения заданий расписания в БД.

Добавлена таблица adm_scheduled_tasks_log (id, task_name, dt_start, dt_end,
duration_sec, error_message) - см. SQL/d_adm.sql. Функция LogTaskRun
(uServerTasks.pas) пишет в неё одну строку при КАЖДОМ выполнении задачи
расписания (и по расписанию, и вручную) - имя задачи, время начала,
время окончания, продолжительность в секундах (SecondsBetween), текст
ошибки (пустая строка, если ошибки не было). Вызывается из RunLoggedTask,
которая теперь замеряет время начала (Now, до ATaskProc()) и по завершении
(успешном или с ошибкой) вызывает LogTaskRun - в ДОПОЛНЕНИЕ к уже
существовавшему общему логу событий (LogTaskEvent, файл + adm_db_log) -
оба лога ведутся одновременно, не заменяют друг друга. Ошибка записи в
adm_scheduled_tasks_log (как и в LogTaskEvent) не считается фатальной.

3) Настройка расписания (cron, активность) через модуль Администратор.

Добавлена таблица adm_scheduled_tasks (id, task_name unique, cron, active)
- см. SQL/d_adm.sql. TScheduledTask (uServerTasks.pas) получил новое поле
Active: Boolean. Новая процедура SyncScheduledTasksFromDb (вложенная в
InitScheduledTasks, вызывается в конце неё) для каждой задачи расписания
ищет строку в adm_scheduled_tasks по task_name: если строка есть - Cron и
Active задачи переопределяются значениями из БД; если строки нет - она
создаётся автоматически со значением cron по умолчанию (взятым из кода,
см. AddTask) и active = 1, чтобы задача сразу была видна в admin-интерфейсе.
Ошибка синхронизации (например, нет соединения с БД на момент вызова) не
считается фатальной - расписание в этом случае остаётся таким, как задано
в коде. ProcessScheduledTasks пропускает неактивные задачи (Active = False)
при автоматическом выполнении по расписанию - на ручной запуск (см. ниже)
признак Active не влияет.
Admin-интерфейс: добавлена константа myfrm_R_ServerTasks (uData.pas),
зарегистрирована в uWindows.pas (TWindowsHelper.ExecReference, список
A.InArray - дальше ведёт к общему справочнику TFrmXGlstMain, отдельная
форма не создавалась). В uFrmXGlstMain.pas (TFrmXGlstMain.PrepareForm)
добавлен блок FormDoc = myfrm_R_ServerTasks - три поля (Задание - только
для чтения, Расписание (cron) - правится прямо в ячейке грида по тегу 'e',
Активно - чекбокс, теги 'chb'+'e'), SetTable('adm_scheduled_tasks'),
кнопки только 'rfs' (обновить/фильтр/настройки - без добавления/удаления/
диалога редактирования, так как набор заданий определяется кодом, а не
пользователем). Пункт меню "Расписание заданий сервера" добавлен в
{$IFDEF ADMIN} раздел DefineMainMenu (uFrmMain.pas), группа "Справочники".

4) Разовое выполнение заданий в модуле Администратор.

Использован уже существующий (реализован в предыдущем раунде для модуля
Сервер) пункт меню "Выполнить задание..." и его обработчик в
TFrmMain.ExecuteMainMenuItem - обработчик ни от IFDEF SRV, ни от IFDEF
ADMIN не зависит (общий, срабатывает по совпадению подписи пункта меню), в
модуль uFrmMain.pas добавлена только сама строка пункта меню в
{$IFDEF ADMIN} раздел DefineMainMenu (группа "Задания"). Так как модуль
Администратор не вызывает TTasksS.Run (в отличие от модуля Сервер),
TTasksS.ScheduledTaskNames и TTasksS.RunScheduledTask доработаны: если
массив ScheduledTasks ещё пуст, они сначала лениво вызывают
InitScheduledTasks - таким образом список заданий и их разовый запуск
доступны и без резидентного режима.


-----------------------------------------------------------------------
Администрирование (uFrmADedtMainSettings.pas/.dfm, uData.pas,
uFrmMain.pas, uWindows.pas, uFrmADedtMailingSettings.pas/.dfm - новая
форма): настройка адресатов почтовых рассылок вынесена с вкладки в
отдельную форму, список рассылок и полей строится динамически из
adm_mailing.

1) Раньше адреса рассылок настраивались на вкладке "Настройки почты"
   формы uFrmADedtMainSettings (TFrmBasicDbDialog) - фиксированный набор
   из 9 полей (MI1..MI10: TMailingInterface), жёстко привязанных к
   конкретным id рассылок. По просьбе пользователя эта вкладка убрана
   целиком, а её функциональность перенесена в новую отдельную форму
   TFrmADedtMailingSettings (uFrmADedtMailingSettings.pas/.dfm,
   наследник TFrmBasicMdi). Форма вызывается новым пунктом меню
   "Настройки почтовых рассылок" (группа "Настройки", uFrmMain.pas),
   через новую константу myfrm_Dlg_MailingSettings (uData.pas) и новую
   ветку в TWindowsHelper.ExecReference (uWindows.pas).

2) В отличие от старой вкладки (фиксированные 10 полей на форме), новая
   форма строит список полей динамически: в
   TFrmADedtMailingSettings.Prepare выполняется "select id, comm from
   adm_mailing where pos > 0 order by pos", и на каждую полученную
   строку создаётся один TDBEditEh (Cth.CreateControls, uForms.pas) -
   рассылки с pos <= 0 или с pos = NULL не показываются (считаются
   неиспользуемыми). Порядок полей на форме соответствует порядку pos.
   Каждое поле, как и раньше, управляется своим TMailingInterface
   (uMailingInterface.pas - класс не менялся) - тот же механизм выбора
   адресатов из пользователей программы и/или произвольных email через
   кнопки редактора. Высота формы подгоняется под фактическое число
   рассылок через Self.ClientHeight (тот же приём, что и в
   TFrmChooseDialog.ShowDialog).

3) На новой форме добавлена кнопка для разработчика "Задать рассылки"
   (FOpt.DlgButtonsL, видна только при User.IsDeveloper). Её обработчик
   (TFrmADedtMailingSettings.btnClick, тег cBtnDevSetMailings) пока
   ничего не делает - предполагается, что через неё в дальнейшем будет
   управление самим списком рассылок в adm_mailing (добавление/удаление
   строк, назначение pos); эта обработка будет сделана отдельным шагом
   позже.

4) В uFrmADedtMainSettings.pas объявления полей убранной вкладки (tsMail
   и 9 TDBEditEh), массив MI1..MI10: TMailingInterface, метод
   LoadComboBoxes (обслуживал только эту вкладку) и сохранение
   MI1.Save..MI10.Save внутри TFrmADedtMainSettings.Save
   закомментированы, а не удалены, со ссылкой на новую форму. В
   uFrmADedtMainSettings.dfm сама вкладка (object tsMail ... end)
   физически удалена - для .dfm это не вопрос стиля, а техническое
   ограничение: текстовый формат .dfm не поддерживает комментарии, а
   оставленная в нём ссылка на компонент, не описанный в .pas (или
   ActivePage на несуществующую вкладку), даст ошибку при загрузке
   формы; ActivePage у pgcMain переключён на tsPaths.

5) Изменений в SQL/d_adm.sql не потребовалось - таблица adm_mailing уже
   содержит нужное поле pos (alter table adm_mailing add pos number),
   используемое в запросе выше для сортировки и фильтра. Проставление
   реальных значений pos для существующих 10 рассылок - шаг
   развёртывания (данные), а не кода, в этом раунде не делался.
   Видимость нового пункта меню использует уже существующую роль
   rAdm_Settings_Main (как и у пункта "Основные настройки") - отдельная
   роль для рассылок не заводилась.


-----------------------------------------------------------------------
Серверные задания (uTasks.pas, uServerTasks.pas,
uFrmADedtMailingSettings.pas): у каждого серверного задания,
отправляющего почту, теперь свой код рассылки вместо нескольких общих на
всех; список этих кодов выгружается в adm_mailing по кнопке разработчика
на форме настройки рассылок.

1) Раньше 21 из 24 отправляющих почту серверных заданий (см.
   InitScheduledTasks, uServerTasks.pas) делили между собой всего 3 кода
   рассылки: TASK_MAILING_ORDERS_FIN (финансовые/по заказам отчёты - 12
   разных заданий), TASK_MAILING_MONITORING_SN (7 заданий) и
   TASK_MAILING_MONITORING_STOCKS (5 заданий) - то есть настроить
   адресатов отдельно для одного конкретного отчёта было невозможно,
   только сразу для целой группы. Каждому из этих 21 задания присвоен
   отдельный код (константа TASK_MAILING_* в uTasks.pas, значения 12-34,
   названы по смыслу задания - например
   TASK_MAILING_OVERDUE_PRODUCTION_ORDERS), и вызовы Tasks.SendMail в
   uServerTasks.pas переставлены на них. Для заданий, у которых один код
   обслуживал несколько вариантов вызова с разным параметром
   (ReportForYesterdayOrders по AOrderTypes - 6 вариантов,
   ReportForOverdueOrders по AForProductionOrders - 2 варианта,
   ReportForEstimatesOverdue по ObjType - 2 варианта), код рассылки
   теперь выбирается по этому параметру (в ReportForYesterdayOrders -
   переменная MailingId, заполняемая в каждой ветке имеющегося case; в
   двух других - S.IIf прямо в месте вызова SendMail). Три общих кода
   (TASK_MAILING_ORDERS_FIN, TASK_MAILING_MONITORING_SN,
   TASK_MAILING_MONITORING_STOCKS) в uServerTasks.pas больше нигде не
   используются, но не удалены - по ним могла остаться настройка
   адресатов в adm_mailing. Два задания (ReportForEarlyCompletionActs и
   не входящая в текущее расписание ReportForOrdersWithoutEstimate) уже
   использовали собственный код и не тронуты.

2) Для переноса кодов, названий и позиций (для интерфейса рассылок, см.
   предыдущую запись в этом файле про uFrmADedtMailingSettings) в
   adm_mailing добавлена TTasks.SyncServerTaskMailings (uTasks.pas) -
   внутри неё прямо в коде задан эталонный список всех 24 серверных
   заданий-рассылок (код/название - как у задания в
   InitScheduledTasks/позиция по порядку появления в расписании). Для
   каждого кода: если строки с таким id в adm_mailing ещё нет -
   вставляется (Q.QSave с сиквенсом '-', то есть id берётся не из
   сиквенса, а как задано); если строка есть, но отличается comm или pos
   - обновляются только эти два поля. Поля
   userids/customemail/addresses/dt (адресаты, заполняются вручную через
   форму) при этом не трогаются, так как не входят в список обновляемых
   полей. Список в коде задаётся один раз (как и просил пользователь) -
   автоматической синхронизации с InitScheduledTasks нет, при добавлении
   нового отправляющего почту задания в будущем список в
   SyncServerTaskMailings нужно будет дополнить вручную.

3) Кнопка разработчика "Задать рассылки" на форме
   uFrmADedtMailingSettings (см. предыдущую запись про саму форму) из
   заглушки превращена в реальный вызов Tasks.SyncServerTaskMailings,
   после которого выводится сообщение с напоминанием, что при появлении
   новых рассылок форму нужно открыть заново (Prepare, создающий поля
   формы по adm_mailing, выполняется только один раз при открытии формы,
   повторно внутри обработчика кнопки не вызывается).


-----------------------------------------------------------------------
Новая рассылка "Материалы, по которым не закрыта потребность"
(SQL/d_order_monitoring_new.sql, uServerTasks.pas, uTasks.pas):
накопительный (не разовый на день) список нехватки материалов, ведущийся
прямо в новом отчете, без хранимых процедур и триггеров.

1) Таблица sn_material_open_requirements (id - айди номенклатуры,
   groupname, dt - дата первого обнаружения нехватки) и вью
   v_rep_sn_material_open_requirements (джойнит эту таблицу с
   dv.nomenclatura/dv.unit/v_spl_qntonstocks_sum) были заведены
   пользователем в SQL/d_order_monitoring_new.sql. От меня потребовалось
   только добавить в существующую вью
   v_rep_required_materials_for_planned_orders_tomorrow (тот же файл)
   столбец n.id_nomencl - он уже вычислялся внутри вью для джойна с
   остатками, но не был в списке select, а он нужен, чтобы заносить в
   sn_material_open_requirements правильный айди номенклатуры. Никаких
   других правок SQL не потребовалось - добавление столбца к вью не
   влияет на уже работающий
   TTasksS.ReportForRequiredMaterialsForPlannedOrdersTomorrow, который
   выбирает только нужные ему поля по имени.

2) Вся логика - в новой TTasksS.ReportForOpenMaterialRequirements
   (uServerTasks.pas, зарегистрирована в InitScheduledTasks как
   "Материалы, по которым не закрыта потребность", ежедневно в 8 утра) -
   как и просил пользователь, без хранимых процедур/триггеров, все
   вставки/удаления прямо в коде отчета. Сначала материалы с
   отрицательным qnt_diff из
   v_rep_required_materials_for_planned_orders_tomorrow (та же нехватка
   на завтра, что считает и
   ReportForRequiredMaterialsForPlannedOrdersTomorrow) заносятся в
   sn_material_open_requirements, если строки с таким id там еще нет
   (Q.QSave с сиквенсом '-') - дата первого обнаружения (dt) у уже
   существующих строк не переписывается. Затем одним коррелированным
   delete по exists убираются материалы, у которых v_spl_minremains.qnt
   >= v_spl_minremains.need (потребность уже покрыта, с учетом резерва и
   "в пути" - см. формулу need в вью v_spl_minremains,
   SQL/d_suppliers.sql). Наконец по оставшимся строкам вьюхи
   v_rep_sn_material_open_requirements формируется и отправляется отчет
   - по образцу ReportForRequiredMaterialsForPlannedOrdersTomorrow, но
   без столбца "Нехватка" (как и просили) и с датой возникновения вместо
   сметного количества, так как эти данные из
   sn_material_open_requirements/её вью, а не из вчерашнего среза по
   заказам на завтра.

3) Код рассылки для нее (TASK_MAILING_MATERIAL_OPEN_REQUIREMENTS = 35,
   uTasks.pas) и её добавление в список TTasks.SyncServerTaskMailings
   сделал сам пользователь, пока я писал
   TTasksS.ReportForOpenMaterialRequirements - он же заодно переработал
   позиции (pos) всего списка рассылок на категоризированную схему
   нумерации (100-е - по заказам, 500-е - мониторинг снабжения, 1000-е -
   фин. отчеты по заказам, 2000-е - акты/остатки на складах, 3000-е -
   СГП, 4000-е - потребность в материалах, 9000-е - просрочки по
   сметам/технологам) вместо моей простой сквозной нумерации 1-25, и
   переименовал TASK_MAILING_MONITORING_SN (id 7, переставшую
   использоваться после разделения кодов, см. предыдущую запись) в
   TASK_MAILING_CREATED_SUPPLIER_DEMAND - тот же числовой код 7 отдан
   под новый смысл "Создание заявки на снабжение". Так как обе правки
   (его в uTasks.pas и моя в uServerTasks.pas) шли параллельно, при
   сохранении оказалось расхождение в порядке слов в имени константы (я
   до этого писал TASK_MAILING_OPEN_MATERIAL_REQUIREMENTS) - система
   сохранения отклонила мою более старую версию файла вместо того, чтобы
   затереть его правки, и я привел
   TTasksS.ReportForOpenMaterialRequirements к его варианту имени
   константы, не трогая остальные его изменения в uTasks.pas.


-----------------------------------------------------------------------
Перевод последней формы старого типа (D_Order, на базе V_MDI) на
basicMDI: новая форма FrmWOrderOld (uFrmWOrderOld.pas/.dfm) - после
этого можно будет полностью убрать V_MDI из проекта.

1) D_Order.pas/.dfm физически остались на диске нетронутыми (удалить
   файл через мост нет возможности - см. заголовок этого файла), но из
   проекта исключены: в Uchet.dpr и в uses uWindows.pas ссылка на
   D_Order заменена на uFrmWOrderOld. Пользователь может удалить
   D_Order.pas/.dfm/.dcu вручную. Строковая константа myfrm_Dlg_Order
   (uData.pas) НЕ переименована и не тронута - это просто ключ типа
   документа (FormDoc) для сохранения позиции окна и блокировок в БД,
   переименование значения было бы разрывающим изменением для уже
   сохранённых данных; сам вызов диалога по этому ключу в uWindows.pas
   уже давно (до этой правки) закомментирован и заменён на
   TFrmOWOrder.Show - то есть D_Order фактически не вызывался.

2) В самой форме (класс TDlg_Order -> TFrmWOrderOld, модуль D_Order ->
   uFrmWOrderOld, предок TForm_MDI -> TFrmBasicMdi) изменения минимальны
   и точечны: 1) SetStatusBar -> RefreshStatusBar (переименован в новом
   предке, та же сигнатура); 2) InPrepare -> FInPrepare (в TFrmBasicMdi
   это защищённое поле без публичного алиаса, доступно из потомка
   напрямую); 3) MinWidth/MinHeight -> FWHBounds.X/FWHBounds.Y (в
   TFrmBasicMdi нет MinWidth/MinHeight/MaxWidth/MaxHeight - их заменяет
   защищённое поле FWHBounds: TCoord (X/Y - мин. ширина/высота, X2/Y2 -
   макс.), которое использует унаследованный (неизменный)
   FormCanResize); 4) в inherited Create добавлен новый обязательный
   параметр AControlValues (передан []), как и в собственных
   Show/ShowModal2 базового класса; 5) добавлено FOpt.DlgPanelStyle :=
   dpsNone, так как форма управляет своими Bt_Ok/Bt_Cancel/pnl_Bottom
   сама, а не через общую панель кнопок фреймворка (pnlFrmBtns) - без
   этого поверх старых кнопок появилась бы ещё и общая панель. Остальные
   методы формы (ControlOnChange, ControlOnExit,
   ControlCheckDrawRequiredState, FormCloseQuery, Save, Verify и т.п.)
   СОВПАДАЮТ по именам с одноимёнными виртуальными методами общего
   фреймворка TFrmBasicMdi, но не помечены override - они просто
   перекрывают (hide) виртуальные методы предка, что безопасно, так как
   эта форма не использует общий фреймворк проверки/сохранения (FOpt,
   btnOk/btnCancel и т.п. остаются в состоянии по умолчанию) - проверено
   по каждому такому методу отдельно, в т.ч. по факту содержащемуся в
   них bare "inherited" (для FormClose он уже вызывался и раньше, для
   FormCloseQuery - раньше не находил метода в предке (V_MDI) и был
   пустышкой, теперь находит TFrmBasicMdi.FormCloseQuery, но тот сам
   ничего не делает при выключенных/невыставленных FOpt/btnCancel).

3) D_Order.dfm использовал настоящее визуальное наследование Delphi от
   V_MDI.dfm (блок "inherited pnl_StatusBar: TPanel ... inherited
   lbl_StatusBar_Right: TLabel"), а не просто одноимённого предка - у
   TFrmBasicMdi другие имена этих компонентов
   (pnlStatusBar/lblStatusBarR) и, главное, собственная рамка pnlFrmMain
   -> pnlFrmClient (+ pnlFrmBtns) для контента и кнопок. В
   uFrmWOrderOld.dfm компонент статусбара переименован в
   pnlStatusBar/lblStatusBarR (те же переопределения позиции), а все 4
   верхнеуровневые панели формы (pnl_Top, pnl_Bottom, pnl_Center,
   pnl_Header - их содержимое не тронуто) вложены внутрь "inherited
   pnlFrmClient" - по образцу уже существующего TFrmOWOrder.dfm
   (нынешняя рабочая замена D_Order), который делает точно так же для
   своего контента.


-----------------------------------------------------------------------
Полное отключение V_MDI (TForm_MDI) от проекта - после перевода
последней формы (D_Order -> FrmWOrderOld) на TFrmBasicMdi потомков этого
класса среди форм не осталось.

1) Проведена сплошная проверка всех 167 модулей, реально входящих в
   сборку (по списку uses в Uchet.dpr, кроме uFrmOWRepDataCheck.pas -
   этот файл по стоящему правилу не трогаем и не смотрим), на предмет
   ссылок на V_MDI/TForm_MDI/Form_MDI. Строка "V_MDI in 'V_MDI.pas'
   {Form_MDI}," убрана из Uchet.dpr. Сам файл V_MDI.pas/.dfm физически
   остался на диске (удалить через мост нет возможности), но из проекта
   больше нигде не подключается - пользователь может удалить его
   вручную.

2) В 7 модулях (uFrmTestMdi1.pas, uFrmCWCash.pas, uFrmMain.pas,
   uMailingInterface.pas, uFrmBasicDbDialog.pas,
   uFrmAWUsersAndRoles.pas, uFrmXWGridOptions.pas) V_MDI был указан в
   uses, но реально не использовался (все эти формы уже давно
   наследуются от TFrmBasicMdi) - это просто неиспользуемый импорт,
   убран без других изменений.

3) В uWindows.pas (модуль Wh/TWindowsHelper - учет открытых окон и бар
   окон в нижней части приложения) и в uFrmXWGridOptions.pas
   (определение FormDoc формы-владельца панели настройки вида таблицы)
   была реальная функциональная логика, различающая старые (TForm_MDI) и
   новые (TFrmBasicMdi) формы через "AForm is TForm_MDI" и приведение
   типа TForm_MDI(AForm) - в 8 местах uWindows.pas (проверка
   принадлежности приложению, подсчет однотипных окон, получение
   FormDoc/Id окна, признак модальности) и в 1 месте
   uFrmXWGridOptions.pas (Prepare). Так как потомков TForm_MDI среди
   форм проекта больше нет, ветки "is TForm_MDI"/приведение к TForm_MDI
   закомментированы (не удалены - оставлены как есть с пояснением),
   оставлена только ветка TFrmBasicMdi. Убирать эти ветки было
   обязательно, а не просто желательно: как только из uses этих двух
   модулей убран V_MDI, идентификатор TForm_MDI перестает быть виден -
   без правки веток модуль просто не скомпилировался бы.

4) Не тронуты (оставлены как есть намеренно): 1) D_Order.pas -
   единственный оставшийся файл с реальным потомком TForm_MDI, но он уже
   выведен из проекта (не в Uchet.dpr) при переводе на FrmWOrderOld,
   поэтому не компилируется и не мешает; 2) uData.pas и
   uFrmAWInstallModule.pas - упоминания Form_MDI/V_MDI/TForm_MDI там
   только внутри текстовых комментариев-записей истории изменений
   (журнал за прошлые даты, а также готовое описание уже сделанного
   ранее перевода F_Adm_Installer на TFrmAWInstallModule) - это
   документация прошлого, а не код, трогать не стал.


-----------------------------------------------------------------------
uScanApi.pas: исправлено зависание процесса модуля Сервер при закрытии
окна (крестиком) - окно закрывалось, а процесс оставался висеть в
диспетчере задач.

1) Причина: HttpCommand вызывается Indy в отдельном потоке на каждое
   HTTP-соединение (см. заголовок модуля), а
   HandleScan/HandleStageAction/EmployeeJsonFields внутри него напрямую
   обращались к общему Oracle-соединению Q (TADOConnection, uDB.pas) -
   COM-объекту, созданному и живущему на главном (VCL) потоке. Вызов его
   методов из чужого потока некорректен: такой вызов должен
   обслуживаться потоком-владельцем. Остановка сервера (TScanApi.Stop ->
   FHttpServer.Active := False, вызывается только из finalization
   модуля, то есть уже после выхода из Application.Run) ждёт завершения
   всех потоков Indy - если в этот момент такой поток ждал обслуживания
   от главного потока, а тот сам стоял внутри Active := False и
   сообщения уже не разбирал - получалась взаимная блокировка, и процесс
   висел в памяти бесконечно.

2) Исправление: вся обработка HTTP-запроса в HttpCommand теперь целиком
   выполняется на главном потоке через TThread.Synchronize (тот же
   приём, что уже использовался в LogDebug через TThread.Queue, только с
   ожиданием результата, а не в фоне) - поток Indy при этом просто ждёт
   готового ответа, для внешнего поведения сервера ничего не меняется.
   Сами HandleScan/HandleStageAction/EmployeeJsonFields не трогал - им
   достаточно того, что к моменту их вызова текущий поток уже главный.

3) Дополнительно стоит иметь в виду (не делал, т.к. не входило в
   задачу): даже после этого исправления остаётся узкое окно - если
   закрытие сервера (Active := False) начнётся ровно в момент, когда
   какой-то запрос уже стоит внутри Synchronize и ждёт обслуживания,
   короткая та же блокировка теоретически возможна снова, просто гораздо
   менее вероятна (нужно попасть точно в момент активного запроса, а не
   в любой момент простоя с keep-alive соединением, как было раньше).
   Для полной защиты от этого можно дополнительно задать
   FHttpServer.TerminateWaitTime (таймаут ожидания остановки потоков
   Indy) и/или останавливать ScanApi явно из TFrmMain.FormClose, пока
   цикл сообщений ещё работает, а не полагаться только на finalization.


-----------------------------------------------------------------------
uServerTasks.pas: запуск сканера штрихкодов (ScanApi) временно отключен
- пользователь решил отложить его отладку и сосредоточиться на остальных
функциях модуля Сервер (тем более что перевод обработки запроса на
главный поток из предыдущей записи не устранил зависание процесса при
закрытии).

1) ScanApi.Start закомментирован в TTasksS.Run (единственная точка
   штатного запуска) и в TServerWatchdog.UpdateCheckTimerTimer (запасной
   перезапуск после неудачной попытки обновления) - оба места помечены
   комментарием. ScanApi.Stop в том же UpdateCheckTimerTimer НЕ трогал -
   вызов безопасен и без активного сервера (Stop ничего не делает, если
   FActive = False). Сам класс TScanApi по-прежнему создаётся и
   уничтожается в initialization/finalization uScanApi.pas, но раз
   FHttpServer.Active никогда не становится True - никакие потоки Indy
   не поднимаются вообще, то есть сканер полностью выведен из процесса
   на время отладки, а не просто не используется.

2) Включить обратно, когда дойдут руки до сканера - раскомментировать
   обе строки ScanApi.Start.

3) Это же попутно является диагностическим шагом: если зависание при
   закрытии процесса модуля Сервер сохранится и без сканера вообще -
   значит, дело не в Indy/ScanApi (или не только в нём), а в чём-то
   другом в резидентной части (TServerWatchdog/таймеры/что-то в самих
   задачах расписания) - это стоит проверить в первую очередь при
   следующей отладке.


-----------------------------------------------------------------------
Группа небольших доработок по модулю Сервер (по запросу пользователя):
просмотр логов заданий в Администрировании, отдельные права доступа на
расписание заданий и на настройки рассылок, прокрутка в окне настроек
рассылок, разбор вопроса про ProcessMessages внутри задачи расписания и
защита планировщика от реентрантного запуска, индикатор выполняющейся
задачи.

1) Вопрос: сломает ли логику планировщика ProcessMessages внутри
   задания, если есть одновременные задачи и срабатывает таймер
   обновления? Ответ - да, это реальный риск, а не гипотетический.
   TServerWatchdog.MinutelyTimerTimer/UpdateCheckTimerTimer выполняются
   на главном потоке через обычные VCL TTimer (WM_TIMER), а WM_TIMER в
   Windows не ставится в очередь - сообщение генерируется по требованию,
   когда цикл сообщений проверяет очередь и видит, что интервал таймера
   уже истёк. Если код внутри выполняющейся задачи (RunLoggedTask ->
   ATaskProc) вызовет Application.ProcessMessages, и к этому моменту
   реальное время уже прошло очередной интервал (60 сек для
   FMinutelyTimer или 5 мин для FUpdateCheckTimer), этот ProcessMessages
   тут же продиспетчеризирует новое WM_TIMER - то есть повторно,
   вложенно вызовет тот же самый обработчик поверх ещё не завершившегося
   внешнего вызова, на той же нити стека. Для MinutelyTimerTimer это
   означает: вложенный вызов увидит ещё не обновлённый
   FLastScheduleCheckMoment и заново выполнит ProcessScheduledTasks за
   то же время - то есть повторно запустит уже выполняющиеся и/или уже
   выполненные в эту минуту задачи, а по возврату внешний вызов затем
   перезапишет FLastScheduleCheckMoment своим (более ранним) значением
   поверх уже обновлённого вложенным вызовом, создавая почву для
   дальнейших повторов. Для UpdateCheckTimerTimer риск ещё серьёзнее -
   при обнаружении новой версии он вызывает Halt(0) (через
   RunUpdaterAndHalt), и вложенный вызов посреди другой задачи оборвал
   бы процесс прямо посреди её выполнения (потенциально посреди
   транзакции БД).

2) Исправление (uServerTasks.pas, TServerWatchdog): добавлено поле
   FIsBusy - простой флаг "занято", выставляемый на входе в любой из
   двух обработчиков таймеров и снимаемый в finally на выходе;
   реентрантный вход в ЛЮБОЙ из них, пока флаг установлен, сразу
   завершается без выполнения тела. Задачи при этом не теряются -
   пропущенные минуты будут учтены на следующем обычном (невложенном)
   срабатывании FMinutelyTimer, так как ProcessScheduledTasks и так
   рассчитан на диапазон нескольких минут (механизм "просрочки", см.
   заголовок процедуры).

3) Индикатор выполняющейся задачи (по решению пользователя - сразу оба
   варианта): в RunLoggedTask (uServerTasks.pas) добавлены
   ShowCurrentTaskIndicator/HideCurrentTaskIndicator, вызываемые вокруг
   ATaskProc() - показывают имя задачи одновременно в левой панели
   статусбара главной формы (FrmMain.StatusBar.Panels[0] - на практике
   почти не используется другим кодом) и через uWaitForm. Важно:
   ShowWaitForm вызвана с явным вторым параметром 0 (таймаут
   автозакрытия) - без этого форма закрылась бы сама почти сразу же при
   первой прокачке сообщений (значение по умолчанию ATimeoutSec = 0.001
   в uWaitForm.pas), в том числе изнутри самой задачи, если она вызовет
   ProcessMessages; закрывается она поэтому только явно, из
   HideCurrentTaskIndicator. Использование uWaitForm здесь безопасно
   ИМЕННО благодаря защите FIsBusy из предыдущего пункта - её
   собственный внутренний Application.ProcessMessages больше не может
   вызвать реентрантный повторный запуск обработчиков таймеров сторожа.

4) Новый экран "Журнал выполнения заданий сервера" (просмотр
   структурированного журнала adm_scheduled_tasks_log - время
   начала/окончания, длительность, текст ошибки по каждому выполнению
   каждой задачи расписания; общий "Лог событий БД", myfrm_Adm_Db_Log,
   этих подробностей не показывает). Добавлено: константа
   myfrm_R_ServerTasksLog (uData.pas), регистрация в списке справочных
   форм TWindowsHelper.ExecReference (uWindows.pas), блок FormDoc =
   myfrm_R_ServerTasksLog в uFrmXGlstMain.pas (по образцу уже
   существующего блока myfrm_R_ServerTasks) - грид только для чтения,
   кнопки только обновить/фильтр/настройки ('rfs'), без
   добавления/редактирования/удаления.

5) Права доступа (uData.pas): для экрана "Расписание заданий сервера"
   (myfrm_R_ServerTasks) вместо жёсткой проверки User.IsDeveloper теперь
   используется User.Role(rAdm_Other_Tasks) - это право ('0-92') было
   объявлено и зарегистрировано в справочном массиве ещё раньше, но
   нигде не проверялось ни одним экраном - теперь оно наконец
   используется по назначению, новая константа под это не заводилась.
   Для нового экрана журнала добавлено отдельное право
   rAdm_Other_ServerTasksLog ('0-93') - сделано отдельным от
   rAdm_Other_Tasks, так как это просмотр, а не редактирование. Для
   "Настроек почтовых рассылок" (myfrm_Dlg_MailingSettings) добавлено
   отдельное право rAdm_Settings_Mailing ('0-94') взамен
   использовавшегося раньше общего rAdm_Settings_Main (тот же, что и у
   "Основных настроек"). Все три права зарегистрированы в описательном
   массиве (строка/раздел/описание) для экрана "Пользователи и роли". В
   меню Администрирования (uFrmMain.pas) добавлен новый пункт "Журнал
   выполнения заданий сервера" рядом с "Расписание заданий сервера",
   предикаты видимости обоих существующих пунктов обновлены на новые
   права.

6) Прокрутка в окне настроек рассылок (uFrmADedtMailingSettings.pas и
   .dfm): раньше при большом числе рассылок форма росла по высоте без
   ограничения (Self.ClientHeight := ... под все рассылки сразу) и могла
   не поместиться на экране без какой-либо прокрутки. Теперь pnlClient
   (с динамически создаваемыми полями рассылок) помещён внутрь новой
   прокручиваемой области sbxClient: TScrollBox (Align = alClient на
   месте, где раньше был сам pnlClient); в Prepare растёт по высоте
   теперь pnlClient (под фактическое число рассылок, как и раньше росла
   форма), а высота самой формы ограничена константой cMaxClientHeight =
   500 - если содержимого больше, лишнее становится доступно через
   полосу прокрутки sbxClient (появляется автоматически,
   TScrollBox.AutoScroll = True по умолчанию), а не за счёт формы,
   вылезающей за экран.


РАЗДЕЛ 6. МИГРАЦИЯ БД-СЛОЯ НА FIREDAC (ГРИДЫ + БАЗОВЫЙ TmyDB)
=================================================================

Сделано (17.09.2026), пилот на гриде "большой таблицы" (frmglstsnmain,
uFrDBGridEh.pas): перевод одного грида с моста ADO-драйвера EhLib
(TDataSetDriverEh) на выборку через FireDAC, без изменения способа
реальной записи в БД (как и раньше, весь реальный INSERT/UPDATE/DELETE
идёт явными Q.QExecSql, EhLib-драйвер их никогда не выполнял по-настоящему).


6.1. Почему отказались от "живого моста" (TDataSetDriverEh) и перешли на
"через массивы"
-----------------------------------------------------------------
Первая версия (myogdmWithFdDriver, "версия 1") держала живой
TDataSetDriverEh поверх FireDAC-запроса, как и старый ADO-режим. Уперлись
в TDataDriverEh.ConsumerClosed (DataDriverEh.pas, EhLib) - при каждом
MemTableEh1.Close он закрывает ProviderDataSet и ставит
ProviderEOF := True; из-за этого повторное открытие/чтение через
TDataDriverEh.ReadData тихо возвращало 0 строк ("защёлка" ProviderEOF).
Итог - пустой грид при myogLoadAfterVisible и "запись была удалена" при
быстром повторном обновлении по кнопке. Решение пользователя - отказаться
от живого моста полностью, делать "всю логику через массивы с
промежуточной загрузкой, получая всё через метаданные" (по образцу
незаконченной ранее работы в соседнем проекте UchetFD).


6.2. Итоговая архитектура (версия 2) - LoadFdData
-----------------------------------------------------------------
- MemTableEh1.DataDriver = nil постоянно в этом режиме - никакого моста
  нет вообще (ровно как и в давно существующем офлайн-режиме, см.
  TFrDBGridEh.CreateDataSet - там тоже DataDriver = nil, это НЕ баг, а
  общий для проекта паттерн: реальная запись всегда идёт в обход
  DataDriver, через Q).
- LoadFdData (uFrDBGridEh.pas) на каждое обновление грида целиком:
  выполняет запрос в отдельном FFdQuery (FireDAC, живёт на самом фрейме),
  затем ПОЛНОСТЬЮ пересобирает MemTableEh1: FieldDefs.Clear -> заново
  из FFdQuery.FieldDefs (реальные метаданные СУБД, а не $-аннотации
  Opt.Sql.Fields, как в остальных режимах) -> CreateDataset -> копирование
  строк Append/Post.
- Одна на всё приложение FireDAC-сессия: TmyDBOra.FdConnection
  (uDBOra.pas) - лениво создаваемый TFDConnection, Database/User_name/
  Password парсятся из старой ConnectionString той же функцией
  ExtractAdoConnParam (перенесена сюда из uFrDBGridEh.pas). Гридовый
  FFdQuery/FFdRefreshQuery используют именно это соединение
  (PrepareFdConnection), отдельного per-грид соединения нет.
- Перечитывание одной строки (Ctrl+R, RefreshBeforeSave/RefreshAfterSave)
  - через отдельный лёгкий RefreshFdRecordDirect (свой FFdRefreshQuery по
  FFdBaseSelectSql + where id = :id), а не через DataDriver.GetRecCommand,
  которого в этом режиме просто нет.


6.3. Найденные и исправленные баги пилота (в порядке появления)
-----------------------------------------------------------------
1) "BCD overflow" при копировании строк - Oracle NUMBER без явных
   precision/scale FireDAC отдаёт как ftBCD/ftFMTBcd; копирование их
   "родных" Precision/Size в FieldDefs МemTableEh1 переполнялось на
   реальных значениях. Исправлено: такие поля переводятся в ftFloat (как
   и везде в проекте - см. $f/$i/$s/$d), значения читаются/пишутся через
   .AsFloat с явной проверкой .IsNull (не через .Value/Variant).
2) Не создавались столбцы грида (оставался один столбец из дизайнтайма,
   в него дублировалось значение) - LoadFdData оборачивала
   MemTableEh1.Active := True в InLoadData := True, из-за чего
   MemTableEh1AfterOpen (флаг InLoadData) не вызывала
   SetColumnsAndEvents (пересборка DBGridEh1.Columns по MemTableEh1.Fields
   - обязательна каждый раз, т.к. CreateDataset каждый раз создаёт новые
   TField). Исправлено явным вызовом SetColumnsAndEvents сразу после
   MemTableEh1.Active := True.
3) Слетало всё оформление грида (границы, цвета, чекбоксы, видимость
   столбцов, заголовки-имена полей вместо подписей) - та же причина, но
   для ОСТАЛЬНОЙ части цепочки MemTableEh1AfterOpen
   (SetColumnsPropertyes/RestoreFrDBGridEhSettings/SetColumnsVisible/
   SetOptions/сортировка+фильтр). Так как LoadFdData всегда делает
   Close+Open (в отличие от ADO-режима, который открывается один раз), а
   MemTableEh1BeforeClose при каждом закрытии безусловно пишет настройки
   пользователя и сбрасывает FIsPrepared - нельзя было просто повторять
   всю цепочку на каждое обновление (затирались бы подобранные
   пользователем ширины столбцов). Решение - новый флаг
   FFdColumnsPrepared (независимый от FIsPrepared): "разовая" косметика
   выполняется один раз за время жизни фрейма, SetColumnsAndEvents - на
   каждое обновление.
4) Ошибка при редактировании ("Dataset not in edit or insert mode") на
   ЛЮБОЙ ячейке (чекбоксы и обычный ввод) - MemTableEh1.ReadOnly в
   дизайнтайме = True; все давно существующие загрузчики
   (LoadSourceDataFromSql/LoadSourceDataFromArray/LoadData) явно сбрасывают
   его в False перед загрузкой, а LoadFdData - нет, поэтому после первого
   же Close/Open грид навсегда оставался нередактируемым. Исправлено
   добавлением MemTableEh1.ReadOnly := False в LoadFdData (после
   CreateDataset).
5) Та же ошибка ("не в режиме редактирования"), но только в двух
   конкретных столбцах ("Заказ/ввод", "Заказ/в заявку") на этом экране,
   даже после исправления (4) - оказалась НЕ багом библиотеки, а кодом
   самого экрана: там значение писалось напрямую
   (MemTableEh1.FieldByName(...).AsInteger := ...; PostAndEdit) в обход
   фреймового SetValue (который сам делает Edit перед записью).
   Исправлено на стороне экрана - добавлен явный MemTableEh1.Edit перед
   прямой записью. Стоит на будущее поискать похожие места в остальном
   коде (актуально при дальнейшем расширении пилота на другие экраны).
6) Окно "Загрузка данных..." на время полной перезагрузки грида: попытка
   через встроенное DBGridEh1.StartLoadingStatus/FinishLoadingStatus
   (обёртки BeginOperation/EndOperation) воспроизвела уже известный по
   этому же файлу глюк (см. закомментированный код в оффлайн-ветке
   RefreshGrid, пометка "затенение с сообщением вызывает глюк - грид
   темный, не отображаются данные") - подтверждено пользователем на
   практике. Заменено на независимое от грида окно uWaitForm
   (ShowWaitForm('Загрузка данных...', 0, procedure ... LoadFdData ...
   end)) - показывается/скрывается само, к гриду не привязано.


6.4. Принятое решение по дальнейшему шагу (17.09.2026, в работе на момент
записи)
-----------------------------------------------------------------
Пилот на одном гриде признан стабильным, следующий шаг - перевести на
FireDAC САМ БАЗОВЫЙ СЛОЙ работы с БД (Q, класс TmyDB, uDB.pas), а не
только выборку данных для гридов. Решено:
- ADO не заменяется, а дублируется - TmyDB одновременно поддерживает ОБА
  бэкенда (ADO и FireDAC), выбор - на уровне объекта/флага; весь
  существующий ADO-код (Q.QExecSql/QLoad/... из старых мест) продолжает
  работать как есть.
- Меняется именно БАЗОВЫЙ класс TmyDB (не только Oracle-потомок
  TmyDBOra) - т.е. это в перспективе касается и MSSQL-класса TmyDBParsec
  (uDBParsec.pas), хотя первое включение FireDAC - только для Oracle.
- Портируется сразу ВЕСЬ публичный API Q (QOpen/QClose/QExecSql/
  QExecSqlSimple/QSelectId/QLoad и все его перегрузки/QLoadToRec/QSave/
  QCallStoredProc/QSetParams/QSetParamsEh/транзакции QBeginTrans/
  QCommitOrRollback/QCommitTrans/QRollbackTrans/логирование ToLog/ToLogEh)
  на FireDAC-путь, а не только точечно QExecSql/QLoad.
- Для гридов (TFrDBGridEh) отдельно предусматривается свой флаг/
  умолчание выбора режима работы (ADO или FD) на уровне фрейма - чтобы
  дальнейшее расширение пилота на новые экраны не требовало ручной правки
  каждого вызова SetDataMode.
Реализация - в процессе, начата сразу после записи этого раздела.

6.5. Реализация п.6.4 (17.09.2026) - TmyDB.Backend, весь API Q на два бэкенда
-----------------------------------------------------------------------------
Сделано в uDB.pas (базовый класс), uDBOra.pas, uFrDBGridEh.pas:

- TmyDbBackend = (mydbbAdo, mydbbFireDac); новое свойство TmyDB.Backend (по
  умолчанию mydbbAdo - без явной установки поведение объекта Q не меняется
  НИКАК). ADO не отключается и не заменяется ни при каком значении Backend -
  это переключатель "через что именно выполнять запросы", а не миграция.
- TmyDB.FdConnection/FFdConnection и функция ExtractAdoConnParam перенесены
  сюда из TmyDBOra (были специфичны только для Oracle, пока это был
  единственный DbType, использующий FireDAC) - DriverName теперь выбирается
  по FDbType (Ora -> 'Ora', MsSql -> 'MSSQL'); TmyDBOra.FdConnection/
  GetFdConnection удалены как класс-специфичные, свойство наследуется от
  TmyDB без изменений для вызывающего кода (в т.ч. для TFrDBGridEh -
  PrepareFdConnection продолжает работать через Q.FdConnection как раньше).
  Добавлены FFdQuery/GetFdQuery (аналог AdoQuery) и FFdStoredProc/
  GetFdStoredProc (аналог AdoStoredProc) - тоже лениво создаваемые, по
  одному на объект Q.
- ActiveDataSet: TDataSet - возвращает AdoQuery либо FdQuery по Backend;
  через него теперь безусловно (без дублирования кода под каждый бэкенд)
  работают QLoadToRec/QLoadToMemTableEh/QLoadToDBComboBoxEh/
  QLoadToTStringList - они обходят результат запроса только через базовые
  методы TDataSet (EOF/FieldCount/Fields/Next), одинаковые что у ADO, что у
  FireDAC.
- Явное ветвление по Backend понадобилось только там, где API двух
  бэкендов принципиально разный (не через общий TDataSet):
  - QOpen/QExecSql - у обоих есть SQL.Text/Open/ExecSQL, но это отдельные,
    не общие через TDataSet, свойства/методы каждого конкретного класса
    компонента (TADOQuery/TFDQuery), поэтому не через ActiveDataSet;
  - QSetParams/QGetReturnValues - ADO использует свой TParameters/
    TParameter (Direction: TParameterDirection, Attributes), а
    TFDQuery.Params - это стандартный Data.DB.TParams (TFDParams - его
    потомок), с ParamType: TParamType вместо Direction - разные API,
    отдельные ветки;
  - QCallStoredProc - AdoStoredProc (TADOStoredProc) против нового
    FdStoredProc (TFDStoredProc) - тоже разные компоненты и их параметры;
  - QRowsAffected - RowsAffected есть у обоих, но это отдельное свойство
    каждого конкретного класса, не у TDataSet;
  - QBeginTrans/QCommitOrRollback - у TFDConnection транзакции называются
    иначе (StartTransaction/Commit/Rollback/InTransaction), чем у
    TADOConnection (BeginTrans/CommitTrans/RollbackTrans/InTransaction).
- Connect/GetConnectState/ReadConnectionFile НЕ трогались - подключение
  FireDAC (FdConnection) создаётся лениво отдельно при первом реальном
  обращении, независимо от жизненного цикла ADO-подключения; таким
  образом весь механизм логина/реконнекта остался полностью на ADO.
- QSetParamsEh и ToLogEh НЕ затронуты - они всегда только для старого
  ADO/EhLib-моста (ADODataDriverEh), к Backend отношения не имеют и при
  Backend = mydbbFireDac просто не вызываются.
- В uFrDBGridEh.pas добавлен глобальный DefaultFrGridDataMode:
  TFrDBGridDataMode (по умолчанию myogdmWithAdoDriver) - устанавливается в
  TFrDBGridEhOpt.Create ДО любого явного вызова Opt.SetDataMode, так что
  экраны, где SetDataMode уже вызывается явно (сейчас - myfrm_R_CarTypes и
  "большая таблица"), не затрагиваются, а остальные гриды при необходимости
  можно перевести на другой режим все разом, поменяв только это значение,
  без правки каждого экрана по отдельности.
Не тестировалось на практике (компиляция/рантайм) на момент записи этого
раздела - следующий шаг пилота.

6.6. Компиляция прошла, первый рантайм-баг - QCallStoredProc/FireDAC (17.09.2026)
-----------------------------------------------------------------------------
Компиляция после п.6.5 прошла успешно (единственная ошибка была в самом
приведении типа - E2010 Incompatible types: 'TParam' and 'TFDParam', т.к.
TFDStoredProc.Params.CreateParam переопределен в FireDAC и возвращает
ковариантный TFDParam, а не базовый TParam из Data.DB - переменная fp
объявлена как TFDParam, добавлен FireDAC.Stan.Param в uses uDB.pas).

Первый же реальный рантайм-тест (Backend = mydbbFireDac для Oracle) на
QCallStoredProc выявил баг: вызов TmyDBOra.QSetContextValue('v_or_std_items_
nds_rate', 0) -> QCallStoredProc('set_context_value', 'par$s;val$i', [par,
val]) падал с ORA-28106 (invalid value specified for argument #2) внутри
SYS.DBMS_SESSION, вызываемого из UCHET22.SET_CONTEXT_VALUE. Смена типа
параметра val на явный varchar2 ошибку не убирала - то есть дело было не
в типе данных.

Причина: в QCallStoredProc имя параметра, передаваемое в CreateParam(eter),
берётся из ParamNamesA[i] "как есть" - вместе с нашим модификатором типа/
направления через "$" (например, "par$s", "val$i"). Для ADO это не имело
значения - Oracle-провайдер ADO вызывает процедуру чисто позиционно через
{call proc(?,?)}, имя параметра игнорируется целиком. А FireDAC (нативный
OCI-драйвер Oracle) для вызова отдельно стоящей (не пакетной) процедуры
использует имя параметра для сопоставления с реальным именем аргумента
в БД - "par$s"/"val$i" не совпадали с настоящими именами аргументов
(par/val), из-за чего значения попадали не в те позиции вызова
DBMS_SESSION.SET_CONTEXT, и число (val=0 -> '0') оказывалось на месте
атрибута контекста (аргумент #2), а '0' как имя атрибута для Oracle
невалидно (не может начинаться с цифры) - отсюда ORA-28106.

Исправление (uDB.pas, QCallStoredProc, только в ветке FireDAC): перед
FFdStoredProc.Params.CreateParam(...) модификатор "$..." обрезается из
имени параметра (аналогично тому, как это уже делается для имён полей в
QGetSql) - в ADO-ветке имя оставлено как было (CreateParameter у ADO имя
игнорирует, трогать незачем).

На момент записи фикс отправлен пользователю на пересборку/тест, результат
ещё не подтверждён.

6.7. Просадка скорости FD vs ADO на "большой таблице" - тюнинг FetchOptions,
     тестовый стенд в TestProcedure3 (17.09.2026)
-----------------------------------------------------------------------------
На сравнении версий на "большой таблице" (сложное вью, экран OGlstSnMain)
обновление данных на FireDAC занимало 9-10 сек против 5-6 сек на ADO -
FireDAC оказался заметно медленнее настроек FireDAC "по умолчанию".

Разбор: TFDQuery по умолчанию не настроен под конкретную задачу - в более
раннем прототипе UchetFD (R:\Projects\UchetFD\uQF.pas, ReadConnectionFile)
уже подбирались опытным путем рабочие настройки для Oracle. Важное
наблюдение оттуда: большой RowsetSize (пробовали 2500 - "оптимально для
13k записей") на практике оказался МЕДЛЕННЕЕ умеренного значения 100 -
итоговые (последние по тексту, реально применявшиеся) настройки в uQF.pas:
  - FetchOptions.Items -= [fiMeta, fiDetails] (не грузить лишние метаданные)
  - FetchOptions.Mode := fmAll
  - FetchOptions.RowsetSize := 100
  - FetchOptions.CursorKind := ckDefault
  - FetchOptions.LiveWindowParanoic := False
  - UpdateOptions.RefreshMode := rmManual
  - FDConnection.FormatOptions.StrsEmpty2Null := True
  - FDConnection.FormatOptions.StrsTrim := False

Перенесено в uDB.pas: новый публичный метод TmyDB.TuneFdQuery(AQuery:
TFDQuery) применяет все перечисленные FetchOptions/UpdateOptions к любому
переданному TFDQuery; вызывается из GetFdQuery (примитивы Q) сразу при
создании FFdQuery. Публичный (не protected), т.к. используется также
напрямую из TFrDBGridEh.PrepareFdConnection - для собственных FFdQuery и
FFdRefreshQuery грида "большой таблицы" (общий Q.FdConnection, но каждый
компонент TFDQuery настраивается по отдельности). FormatOptions
(StrsEmpty2Null/StrsTrim) выставлены в самом GetFdConnection - это
свойство подключения, а не отдельного запроса.
На момент записи результат влияния этой настройки на реальные 9-10 сек
еще не подтвержден пользователем - следующий шаг.

Дополнительно, для дальнейшего подбора настроек и сравнения ADO/FD именно
на загрузке В МАССИВ (Q.QLoadToRec) разных объемов данных из "большой
таблицы", в uFrmTest2.pas (TestProcedure3, меню "Тест 3") добавлен
тестовый стенд:
  - создаёт ОТДЕЛЬНЫЙ экземпляр TmyDBOra (TestQ, свое соединение) - чтобы
    не трогать состояние (транзакции/соединение) "боевого" Q;
  - последовательно на TestQ.Backend = mydbbAdo и mydbbFireDac выполняет
    QLoadToRec с "select * from (<SELECT большой таблицы>) where rownum
    <= :n" для набора объемов [100, 1000, 5000, 20000, 100000], меряя
    время через GetTickCount64;
  - выводит отчёт (мс на каждый объём и бэкенд, число полученных строк)
    через MyInfoMessage.
ВАЖНО: реальный текст SELECT "большой таблицы" (тот же, что уходит в
FFdPendingSelectSql/ADODataDriverEh1.SelectSQL для экрана OGlstSnMain) в
этой сессии был недоступен (модуль экрана не выгружался) - в TestSql
оставлена явная метка-заглушка "<ВСТАВИТЬ СЮДА РЕАЛЬНЫЙ SELECT БОЛЬШОЙ
ТАБЛИЦЫ>", подставить его нужно вручную перед запуском теста.

6.8. v_spl_minremains: сырая скорость ADO=FD, но грид всё равно медленнее -
     профилирование LoadFdData (17.09.2026)
-----------------------------------------------------------------------------
Прогон TestProcedure3 на реальном вью v_spl_minremains (5587 строк) дал:
  ADO:     100:3813мс 1000:3594мс 5000:7250мс 20000:6593мс 100000:6704мс
  FireDAC: 100:2781мс 1000:3329мс 5000:7468мс 20000:6860мс 100000:6390мс
(объемы 20000/100000 фактически вернули все 5587 строк - вью само по себе
тяжелое, время в основном уходит на сервере, а не на фетч клиентом).
Вывод: НА УРОВНЕ СЫРОГО QLoadToRec (Q.Backend) скорость ADO и FireDAC
практически одинакова для этого вью - TuneFdQuery (см. 6.7) полностью
закрыл разницу и здесь тоже.

При этом пользователь сообщил: сам грид (экран с новым FD-режимом,
myogdmWithFdDriver/LoadFdData) на том же вью работает 7-8 сек, а ADO-режим
(старый "живой" мост TDataSetDriverEh/EhLib) - 5-6 сек. То есть разница
есть на уровне ГРИДА, но не на уровне чистой загрузки в массив.

Вывод: причина не в скорости выполнения запроса (одинакова у обоих
бэкендов), а в том, что LoadFdData и старый ADO live-bridge - это ДВЕ
РАЗНЫЕ архитектуры на клиенте, не сравнимые "запрос против запроса":
- старый ADO-режим - "живой" мост, поля/структура берутся из уже открытого
  живого курсора, без явной пересборки на каждое обновление;
- LoadFdData (FD-режим, см. 6.1-6.2) при КАЖДОМ обновлении полностью
  закрывает/переоткрывает FFdQuery, ПОЛНОСТЬЮ пересобирает
  MemTableEh1.FieldDefs из реальных метаданных запроса (FieldDefs.Clear +
  цикл AddFieldDef), затем построчно (Append/Post на каждую строку,
  посл field-by-field копирование Value/AsFloat) копирует данные из
  FFdQuery в MemTableEh1 - и то, и другое стоит клиентского CPU-времени
  само по себе, независимо от того, ADO это или FireDAC "под капотом".
Иными словами - наблюдаемые 7-8 сек это (время самого запроса) + (время
пересборки структуры) + (время настройки колонок грида) + (время
построчного копирования 5587 строк) - и по замерам TestProcedure3 из
всего этого только первое слагаемое сравнивалось между бэкендами.

Чтобы не гадать дальше, добавлено профилирование LoadFdData - в
uFrDBGridEh.pas новый временный глобальный флаг:
  FdLoadDataProfilingEnabled: Boolean = False;
При включении (= True, требует пересборки) LoadFdData после каждого
выполнения выводит через MyInfoMessage разбивку по 4 контрольным точкам:
  - FFdQuery.Open (запрос + фетч данных с сервера)
  - пересборка FieldDefs / MemTableEh1.CreateDataset (структура)
  - настройка колонок грида (SetColumnsAndEvents/SetColumnsPropertyes/...)
  - построчное копирование данных в MemTableEh1 (Append/Post в цикле)
Следующий шаг - включить флаг, обновить экран "большая таблица" один раз,
прислать эту разбивку, чтобы понять, какая из четырех составляющих
реально даёт разницу с ADO-режимом (скорее всего - пересборка структуры
и/или построчное копирование, а не сам запрос).

6.9. Профилирование LoadFdData: разница не в клиенте, а в самом запросе
     (17.09.2026)
-----------------------------------------------------------------------------
Результат включения FdLoadDataProfilingEnabled на реальном экране (вью
v_spl_minremains, "новый вариант работы грида"):
  LoadFdData: всего 5375 мс
    FFdQuery.Open (запрос+фетч): 5234 мс  (97% времени)
    пересборка FieldDefs/CreateDataset:     16 мс
    настройка колонок грида:                 0 мс
    построчное копирование в MemTableEh1:  125 мс

Вывод: гипотеза раздела 6.8 (разница из-за пересборки структуры/построчного
копирования - архитектурных затрат LoadFdData) НЕ ПОДТВЕРДИЛАСЬ - обе эти
операции пренебрежимо быстры (16+125=141 мс). Практически всё время (97%)
уходит внутри самого FFdQuery.Open, то есть в выполнении запроса и фетче
данных - а именно это и должно было быть "одинаковым" по замерам
TestProcedure3 (см. 6.7-6.8, где на v_spl_minremains ADO и FD показали
близкие цифры). Раз здесь FD оказался медленнее ADO именно на Open -
разница, видимо, не в клиентской пост-обработке, а в том, что реальный
(боевой) запрос экрана - с его настоящими WHERE/параметрами - выполняется
на сервере или фетчится клиентом иначе, чем упрощенный тестовый запрос
в TestProcedure3 (без реальных условий фильтрации, просто rownum <= N).

Пользователь отметил, что такая же просадка (на пару секунд) наблюдалась
ранее на другом большом/сложном вью (заказы) и в другой, более ранней и
незаконченной попытке (свой объект EhLib для FireDAC, добавленный в
дизайнтайме, а не текущая LoadFdData-архитектура через массивы) - то есть
эффект не привязан к конкретной клиентской архитектуре (ни к "живому
мосту", ни к LoadFdData) и повторяется на разных сложных вью - похоже на
системную разницу в поведении именно FireDAC/OCI-драйвера Oracle против
ADO/OleDB для такого рода запросов, а не на баг в клиентском коде.

Не является блокером для пилота (пользователь: "не думаю что это очень
принципиально, хотя хотелось бы чтобы работало наоборот быстрее") - если
возвращаться к этому вопросу в будущем, вероятные направления:
  - сравнить РЕАЛЬНЫЙ план выполнения (v$sql_plan/EXPLAIN PLAN) одного и
    того же SQL при выполнении через ADO и через FireDAC - если план
    отличается, скорее всего дело в том, как каждый драйвер биндит
    параметры (тип/точность), см. похожий по природе баг ORA-28106 в 6.6
    (несовпадение имени параметра там, тип/точность бинда здесь);
  - поэкспериментировать с FetchOptions.RowsetSize/Mode именно на реальном
    (боевом) запросе экрана, а не на упрощенном тестовом - оптимальное
    значение может зависеть от ширины строки/количества столбцов конкретного
    вью, а не быть универсальным для всех вью сразу.

6.10. Одна сессия на бэкенд + конфиг-файл выбора бэкенда (17.09.2026)
-----------------------------------------------------------------------------
Пользователь заметил в просмотре сессий Oracle ДВЕ сессии на один процесс
Uchet вместо одной - при Backend = mydbbFireDac реально открывались ОБЕ:
ADO (в Connect, независимо от Backend - подключение/логин всегда шли через
ADO) и FireDAC (лениво, при первом же обращении примитивов Q). Запрос:
попробовать режим "всё через один бэкенд" - либо всё Oracle через FireDAC,
либо всё через ADO, без дублирования сессий; MSSQL по-прежнему только ADO.

Причина и исправление: Backend выставлялся ОТДЕЛЬНОЙ строкой (Q.Backend :=
mydbbFireDac) ПОСЛЕ CreateObject, а CreateObject уже успевал подключиться
через ADO ВНУТРИ себя (AConnectAfterCreate). Чтобы Connect мог решить,
какую сессию открывать, бэкенд должен быть известен ДО попытки подключения.
Поэтому:
- TmyDB.CreateObject (и TmyDBOra.CreateObject) получили новый параметр
  ABackend: TmyDbBackend = mydbbAdo (по умолчанию - для существующих
  вызовов без этого параметра, т.е. MSSQL/Парсек, поведение не меняется
  вообще) - FBackend выставляется В САМОМ НАЧАЛЕ конструктора, до
  ReadConnectionFile/Connect;
- TmyDB.Connect (ветка Oracle): при FBackend = mydbbFireDac устанавливает
  ТОЛЬКО FdConnection (через свойство FdConnection/GetFdConnection),
  AdoConnection/AdoConnectionProviderEh вообще не трогает - при
  FBackend = mydbbAdo (по умолчанию) ветка работает как раньше, без
  изменений;
- TmyDB.GetConnectState: при FBackend = mydbbFireDac проверяет статус
  через FdConnection, иначе как раньше через AdoConnection.
ВАЖНЫЙ ПОБОЧНЫЙ ЭФФЕКТ этого режима: если Backend = mydbbFireDac, ADO для
Oracle вообще не подключается - старые "онлайн"-гриды (myogdmWithAdoDriver,
через ADODataDriverEh1/AdoConnectionProviderEh) для Oracle работать не
будут, пока Backend не вернут в mydbbAdo. Это ожидаемо для тестового
режима "всё через один бэкенд", не баг.

Дополнительно, по предложению пользователя, добавлен общий конфиг-файл
подключений вместо разрозненных случаев - uchet.cfg (и uchet_test.cfg,
если рядом лежит - используется ОН, по аналогии с уже существующим
соглашением "<файл>_test.udl" для тестового режима каждой БД по
отдельности). Новая функция ReadAppDbBackend(ADbSectionName, ADefault) в
uDB.pas читает секцию [Oracle]/[MSSQL] с ключом Backend=ADO|FireDAC;
отсутствие файла/секции/ключа - без изменений в поведении (возвращает
ADefault). В Uchet.dpr вызывается для Oracle:
  Q := TmyDBOra.CreateObject(Application, 'connect', True,
    ReadAppDbBackend('Oracle', mydbbFireDac));
(default mydbbFireDac - пока файла нет, поведение остаётся как было все
последнее время - тестируем FireDAC для Oracle). MSSQL (myDBParsec) этот
механизм не получает - как и раньше, только ADO.
Формат секции в uchet.cfg/uchet_test.cfg:
  [Oracle]
  Backend=FireDAC
Пользователь создаст сам сам файл(ы) и поправит вручную отдельный файл
обновлятора (не входит в это изменение).

Попутно замечено и НЕ ЗАТРОНУТО: пользователь вручную поправил
TmyDBOra.QSetContextValue (uDBOra.pas) - теперь всегда передаёт тип 's' и
явно приводит Val к строке (Val.AsString) вместо динамического выбора типа
по VarType(Val) - изменение сохранено как есть при слиянии с правкой этого
раздела (независимые участки файла, конфликта не было).

6.11. Перенос строк подключения в uchet.cfg, раздельные параметры ADO/FireDAC
     (17.09.2026)
-----------------------------------------------------------------------------
Продолжение 6.10: пользователь предложил не ограничиваться только выбором
бэкенда (Backend=...) в uchet.cfg/uchet_test.cfg, а перенести туда же и сами
строки подключения (сейчас разрозненные connect.udl/parsec.udl), с явными и
ПОЛНОСТЬЮ ОТДЕЛЬНЫМИ параметрами для ADO и для FireDAC (не выводить одно из
другого) - также с прицелом на будущее соединение с Firebird и на тайминги.

Новый формат секций в uchet.cfg/uchet_test.cfg (для БД с именем секции, см.
TmyDB.GetAppConfigSectionName - 'Oracle' для mydbtOra, 'MSSQL' для
mydbtMsSql):
  [Oracle]
  Backend=FireDAC

  [Oracle.ADO]
  ConnectionString=Provider=...;Data Source=...;User ID=...;Password=...;

  [Oracle.FireDAC]
  DriverID=Ora
  Database=...
  User_Name=...
  Password=...

  [MSSQL]
  Backend=ADO

  [MSSQL.ADO]
  ConnectionString=...
[Секция.ADO].ConnectionString - это ОДНА строка целиком, в точности в формате
свойства TADOConnection.ConnectionString (включая, при необходимости, и
"Connect Timeout=N" - отдельного кода под тайминги ADO не потребовалось).
[Секция.FireDAC] - произвольный набор пар Имя=Значение, которые копируются
КАК ЕСТЬ в TFDConnection.Params (TIniFile.ReadSectionValues уже возвращает их
в нужном для этого формате) - поэтому любой параметр FireDAC (DriverID,
Database, User_Name, Password, а в будущем и любые тайминги конкретного
драйвера) поддержан "бесплатно", без отдельного кода на каждый параметр.

Изменения в TmyDB (uDB.pas):
- новое поле FFdConfigParams: TStrings - хранит "сырые" строки секции
  [Секция.FireDAC], если она найдена; nil, если не найдена (старый режим);
- новый метод GetAppConfigSectionName: разбирает FDbType в имя секции
  ('Oracle'/'MSSQL'; для нераспознанного/будущего FDbType - пустая строка,
  сигнал использовать старый *.udl-режим). Специально НЕ привязан к
  FConnectionFile ('connect'/'parsec') - чтобы не требовать переименования
  уже существующих *.udl-файлов;
- TmyDB.ReadConnectionFile теперь СНАЧАЛА (вложенная функция
  TryReadAppConfig) проверяет uchet_test.cfg/uchet.cfg на наличие секций
  [Секция.ADO]/[Секция.FireDAC] для текущей БД - если хотя бы одна из них
  найдена и непуста, ConnectionString/FFdConfigParams/CurrentShema/TestDB
  берутся ИЗ НИХ, а *.udl вообще не открывается. Если для этой БД в файле
  нет ни одной такой секции (или файла нет вовсе) - код проваливается в
  СТАРЫЙ режим (*.udl), полностью НЕ ИЗМЕНЁННЫЙ - это защищает MSSQL/Парсек
  (uDBParsec.pas - файл проверен в этой сессии: своего ReadConnectionFile не
  переопределяет, поэтому изменения базового класса безопасны для него) и
  любую БД, для которой новую секцию ещё не завели;
- TmyDB.GetFdConnection: если FFdConfigParams заполнен - параметры
  подключения FireDAC берутся из него целиком (FDConnection.Params.AddStrings),
  включая DriverID (больше не завязан на case FDbType). Иначе - как раньше,
  выводятся из ConnectionString через ExtractAdoConnParam;
- добавлен деструктор TmyDB.Destroy (у класса раньше не было своего) -
  освобождает FFdConfigParams; учитывая, что Q/myDBParsec живут всё время
  работы программы, к утечке в прежнем виде (без деструктора вообще) это
  практического отношения не имело, но теперь корректно в любом случае.

Совместимость: если ни uchet.cfg, ни uchet_test.cfg не существуют - поведение
ПОЛНОСТЬЮ не меняется (тот же старый *.udl-режим, что и раньше, включая
ReadAppDbBackend из 6.10, которая при отсутствии файла возвращает ADefault).
connect.udl/parsec.udl не удаляются и не трогаются - остаются рабочим
резервным вариантом. Реальные значения (пароли, Data Source, User ID) в эту
переписку не передавались и не запрашивались - пользователь заполнит
uchet.cfg/uchet_test.cfg своими значениями самостоятельно (например,
посмотрев их в своём же connect.udl/parsec.udl, это обычный текстовый
.udl/INI-файл). Отдельный файл обновлятора пользователь поправит вручную сам
(вне рамок этого изменения).

6.12. Дефолтный DataMode грида берётся из Q.Backend, а не из константы (17.09.2026)
-----------------------------------------------------------------------------
Запрос пользователя: тип работы грида "по умолчанию" (DataMode, см. раздел 6 и
DefaultFrGridDataMode) должен браться из фактически привязанной к гриду базы
данных, а не задаваться отдельной ручной константой - при этом, раз новый
FD-режим (myogdmWithFdDriver) не умеет работать через ADO, ручной выбор дефолта
отдельно от Backend вообще не имеет смысла (это явно оговорено пользователем:
"если новый вариант работы допускает использование адо, то имеет смысл оставить
ручной выбор, иначе - незачем").

Проверено: myogdmWithFdDriver (TFrDBGridEh.PrepareFdConnection/LoadFdData)
жёстко подключается к Q.FdConnection - через ADO работать не умеет и не может.
Соответственно, при Backend = mydbbFireDac (см. 6.10) ADO-подключение к Oracle
вообще не устанавливается, и любой грид, оставшийся в дефолтном
myogdmWithAdoDriver (через глобальную константу DefaultFrGridDataMode), был
обречён не работать вообще - именно это несоответствие пользователь наблюдал
и о котором сообщал ранее ("не работали вью в новом режиме").

Исправление (uFrDBGridEh.pas, TFrDBGridEhOpt.Create): вместо
  FDataMode := DefaultFrGridDataMode;
теперь
  if Assigned(Q) and (Q.Backend = mydbbFireDac) then
    FDataMode := myogdmWithFdDriver
  else
    FDataMode := DefaultFrGridDataMode;
То есть дефолт для гридов, которые НЕ вызывают Opt.SetDataMode явно, теперь
следует за Q.Backend автоматически: Backend = FireDAC -> дефолт
myogdmWithFdDriver, Backend = ADO -> дефолт myogdmWithAdoDriver (через
константу DefaultFrGridDataMode, оставленную как есть). Проверка Assigned(Q)
нужна только для безопасности design-time (IDE) - до старта программы
глобальный Q ещё не создан, а сам конструктор TFrDBGridEhOpt.Create выполняется
для любого экземпляра TFrDBGridEh, в том числе потенциально в дизайнере форм;
в реальной работе программы Q создаётся в Uchet.dpr до Application.CreateForm,
т.е. уже доступен к моменту создания любых настоящих экранных гридов.
Экраны, у которых DataMode уже задан явным вызовом Opt.SetDataMode (сейчас
только myfrm_R_CarTypes/"большая таблица", всегда в myogdmWithFdDriver
независимо от Backend) - этот механизм не затрагивает, явный вызов, как и
раньше, побеждает над любым дефолтом.
myogdmFromArray/myogdmFromSql (гриды не по датасету БД, а по явно переданному
массиву/SQL) этот механизм тоже не затрагивает - они всегда задаются только
явным вызовом SetDataMode самим экраном, к Q.Backend отношения не имеют.

Попутно замечено и НЕ ЗАТРОНУТО: пользователь вручную (1) включил
FdLoadDataProfilingEnabled (True вместо False, см. 6.8) и (2) добавил второй
параметр (1) к вызову MyInfoMessage в LoadFdData (профилирование, см. 6.9) -
оба изменения сохранены как есть при слиянии с правкой этого раздела
(независимые участки файла, конфликта не было).

6.13. Отключены вызовы FrmXDmsgNoConnection.ShowModal при первом подключении
     (форма ещё не создана в этот момент) (17.09.2026)
-----------------------------------------------------------------------------
Пользователь заметил проблему в TmyDB.Connect (ветка Oracle, обе подветки -
и FBackend = mydbbFireDac, и обычная ADO): код
  if (not ok) and (not AfterProgramStart) and MessageIfError then
    FrmXDmsgNoConnection.ShowModal;
выполняется именно тогда, когда AfterProgramStart ещё False - то есть ровно
во время самого первого подключения Q при старте программы (Q создаётся и
подключается в Uchet.dpr строкой "Q := TmyDBOra.CreateObject(...)" ДО строки
"AfterProgramStart := true"). Но форма FrmXDmsgNoConnection создаётся
(Application.CreateForm(TFrmXDmsgNoConnection, FrmXDmsgNoConnection)) в
Uchet.dpr ПОЗЖЕ - уже после AfterProgramStart := true и после вызова
TFrmXWNoConnectionAfterStart.Execute. То есть при ошибке подключения именно
на старте программы ShowModal вызывался бы у ещё не созданного объекта формы.

При этом эта ситуация (ошибка подключения именно при старте программы) и так
уже корректно обрабатывается отдельным механизмом: сразу после создания Q и
myDBParsec Uchet.dpr вызывает TFrmXWNoConnectionAfterStart.Execute (файл
uFrmXWNoConnectionAfterStart.pas) - тот проверяет Q.Connected и, если нет
соединения, создаёт СВОЮ форму (FrmXWNoConnectionAfterStart) заново и
показывает её - в этот момент AfterProgramStart уже True, форма создаётся
непосредственно перед показом, поэтому проблемы с ещё не созданной формой
здесь нет.

Решение (временное, по прямому указанию пользователя - "пока выключи"):
оба вызова FrmXDmsgNoConnection.ShowModal внутри TmyDB.Connect, срабатывающие
именно при (not AfterProgramStart), закомментированы (обёрнуты в {...}) с
пояснением в коде. Сама логика подключения (ok/Result) не изменилась -
изменилось только то, что при ошибке первого подключения теперь не пытаемся
показать (потенциально ещё не существующую) форму - пользователь и так
увидит корректное сообщение через TFrmXWNoConnectionAfterStart.Execute сразу
после старта.
Не выполнено (оставлено на будущее, отдельным решением) - сделать ли
что-то (и что именно) взамен непосредственно из Connect для этого случая,
например, явно вызывать логику/метод из uFrmXWNoConnectionAfterStart.pas
прямо отсюда, вместо того чтобы полагаться на отдельный вызов в Uchet.dpr.
Побочный эффект: юнит uFrmXDmsgNoConnection в uDB.pas теперь ничем в коде не
используется (только в комментариях) - компилятор может выдать безобидный
hint о неиспользуемом юните, это не ошибка.

6.14. Найдена настоящая причина краха "всех гридов кроме snmain" - безусловный
     сброс DataMode в FrgSetEvents (uFrmBasicGrid2.pas) (17.09.2026)
-----------------------------------------------------------------------------
Продолжение 6.12/6.13. Пользователь сообщил точное место краха:
TFrDBGridEh.RefreshGrid попадает в последнюю ветку else (ADO-онлайн режим,
случай первого открытия - MemTableEh1.Open; MemTableEh1.First;) и падает при
открытии MemTableEh1 - то есть Opt.DataMode на этот момент НЕ РАВЕН
myogdmWithFdDriver, хотя ожидалось обратное (Backend = mydbbFireDac). Также
пользователь заметил: если убрать явный Frg1.Opt.SetDataMode(myogdmWithFdDriver)
у snmain (myfrm_R_CarTypes), тот тоже начинает падать так же - а добавление
такого же явного вызова для Frg2 (и в базовом классе TFrmBasicGrid2, и в
конкретном экране) эффекта не дало вообще.

Найдена настоящая причина (НЕ связана со сложностью SQL/join'ами, как
предполагалось в предыдущем ответе - это было ошибочное предположение):
TFrmBasicGrid2.FrgSetEvents (uFrmBasicGrid2.pas) - общий метод настройки
ЛЮБОГО грида (Frg1 И Frg2) для ЛЮБОГО экрана на базе TFrmBasicGrid2 - содержал
строку:
  AFrg.Opt.SetDataMode;
Поскольку у TFrDBGridEhOpt.SetDataMode параметр по умолчанию =
myogdmWithAdoDriver, вызов БЕЗ аргумента эквивалентен
"AFrg.Opt.SetDataMode(myogdmWithAdoDriver);" - то есть БЕЗУСЛОВНО, для КАЖДОГО
грида на TFrmBasicGrid2, сбрасывал DataMode в ADO-режим, затирая дефолт,
который TFrDBGridEhOpt.Create теперь вычисляет из Q.Backend (см. 6.12).
Порядок вызовов: TFrmBasicGrid2.Prepare сначала вызывает FrgSetEvents(Frg1) и
FrgSetEvents(Frg2) (сброс в ADO для обоих), и ТОЛЬКО ПОТОМ - виртуальный
PrepareForm (конкретная настройка экрана: SetFields/SetTable/SetButtons и,
если нужно, явный SetDataMode(myogdmWithFdDriver)). Поэтому:
- у myfrm_R_CarTypes (snmain) явный вызов SetDataMode(myogdmWithFdDriver)
  стоит именно в PrepareForm - то есть ПОСЛЕ сброса в FrgSetEvents - и
  поэтому "побеждает", грид реально работает в FD-режиме;
- у ВСЕХ ОСТАЛЬНЫХ экранов (без такого явного вызова в их PrepareForm) сброс
  из FrgSetEvents остаётся последним словом - DataMode = myogdmWithAdoDriver
  ВСЕГДА, независимо от Q.Backend и независимо от исправления в 6.12;
- попытки пользователя добавить Frg2.Opt.SetDataMode(myogdmWithFdDriver) "в
  базовом классе" не срабатывали, видимо, потому что были на самом деле
  либо до этого сброса (там же, в FrgSetEvents, до строки AFrg.Opt.SetDataMode),
  либо в другом месте, выполняемом раньше PrepareForm - в любом случае сброс
  в FrgSetEvents выполнялся ПОСЛЕ и побеждал.

Исправление: строка "AFrg.Opt.SetDataMode;" в FrgSetEvents УДАЛЕНА (не
заменена на условную проверку Backend - в этом нет смысла, т.к.
TFrDBGridEhOpt.Create уже корректно вычисляет дефолт из Q.Backend в момент
создания компонента, задолго до Prepare/FrgSetEvents). Явные вызовы
Opt.SetDataMode(...) в PrepareForm конкретных экранов (сейчас только
myfrm_R_CarTypes/Frg1) это никак не затрагивает - они по-прежнему выполняются
после FrgSetEvents и, как и раньше, побеждают над любым дефолтом.
Проверено: других мест с безусловным ".SetDataMode;" (без аргумента) во всём
проекте не найдено (grep по всем *.pas), других базовых классов "гридов"
(TFrmBasicGridN) в проекте, кроме TFrmBasicGrid2, тоже нет.
Ожидаемый эффект после этого исправления: при Backend = mydbbFireDac все
"обычные" гриды на TFrmBasicGrid2 (то есть подавляющее большинство экранов)
должны теперь по умолчанию открываться через myogdmWithFdDriver (как и
задумывалось в 6.12), а не падать на MemTableEh1.Open в ADO-ветке. Насколько
корректно они при этом реально ЗАГРУЖАЮТ ДАННЫЕ через FD (сложные select'ы,
join'ы, фильтры, GetRec и т.д.) - предстоит проверить пользователю по факту
(это первый раз, когда эти экраны реально проходят через FD-код).

6.15. Аудит всех вызовов QCallStoredProc - сверка имён параметров с реальными
      именами аргументов хранимых процедур, исправление расхождений (17.09.2026)
-----------------------------------------------------------------------------
Продолжение 6.6 (ORA-28106) и 6.12/6.14: после того как большинство гридов
реально начали проходить через FireDAC (6.14), стало ясно, что тот же класс
ошибки - несовпадение ИМЕНИ параметра в вызове QCallStoredProc с реальным
именем аргумента хранимой процедуры в БД - может встречаться и в вызовах вне
гридов. Под ADO имя параметра не имеет значения (Oracle-провайдер ADO вызывает
процедуру чисто позиционно), поэтому такие расхождения годами "работали"
незамеченными; под FireDAC (нативный OCI) параметры биндятся по имени - при
несовпадении вызов либо падает с ошибкой Oracle, либо (реже) тихо передает
значение не в тот аргумент.
Пользователь попросил (перед тем как отойти) проверить ВСЕ вызовы
QCallStoredProc по всему проекту и сверить их параметры с реальными
процедурами в SQL-файлах.

Методика: автоматически извлечены все вызовы QCallStoredProc(...) из *.pas
(87 из ~91 вхождений распознано как ИмяПроцедуры+СтрокаПараметров списком
литералов; оставшиеся 4 - это объявление/реализация самой функции в uDB.pas и
два вызова с динамическим именем процедуры 'p_Or_Payment'+суффикс, разобраны
отдельно) и все определения "create or replace procedure/function" из папки
SQL (169 определений, с учетом что задекларированные параметры могут идти не
сразу за именем, а после блока комментариев). Для каждого вызова автоматически
сопоставлены имена (без учета "$"-модификатора типа/направления) с реальными
именами аргументов по каталогам (alt/, *_old.sql, *_temp игнорировались как
неактуальные). Из-за того что в комментариях к некоторым процедурам
встречаются несбалансированные скобки, авто-сверка для процедур с длинным
"развалившимся" списком параметров (например p_test_estimate_item) не
заслуживает доверия сама по себе - КАЖДОЕ расхождение, включая уже найденные
вручную (6.14 и раньше в этой сессии), проверено вручную чтением реального
SQL-файла, и только после этого исправлялось в Delphi-коде. Также обнаружено,
что часть кода могла уже быть исправлена пользователем параллельно (например
uFrmOGedtEstimate.pas/p_test_estimate_item - при повторном чтении с устройства
вызов уже содержал правильные имена параметров, видимо, правка пользователя -
оставлено как есть).

Отдельная находка: в локальной копии проекта (staging) обнаружился файл
D_Order.pas, отсутствующий на самом устройстве (R:\Projects\Uchet) - сравнение
показало, что это более старая версия текущего uFrmWOrderOld.pas (тот же код,
имя юнита D_Order/TDlg_Order переименовано в uFrmWOrderOld/TFrmWOrderOld,
базовый класс TForm_MDI -> TFrmBasicMdi), не входящая ни в Uchet.dpr, ни в
Uchet.dproj - то есть "призрак" из более раннего этапа этой работы. Правки в
него не вносились (файла для commit не существует); все реальные правки для
этого экрана внесены в uFrmWOrderOld.pas.

Найдены и исправлены следующие расхождения (везде правилась ТОЛЬКО строка
имён параметров ParamNames, типы/направления/значения не менялись):

- p_SetSplDemandValue (d_suppliers.sql): uFrmODedtNomenclFiles.pas - было
  'id$i;op$i;v$i', стало 'IdNomencl$i;PMode$i;PValue$f' (везде в остальных 13
  местах уже было верно).
- p_OrderStage_SetItem (d_order_stages.sql): везде (5 мест - uFrmOWInvoiceToSgp.pas,
  uFrmOGjrnOrderStages.pas x3, uScanApi.pas) параметр 'UpdateOrder$i' исправлен
  на 'UpdateOrders$i' (в реальной процедуре - UpdateOrders, с "s"); в двух
  местах (uFrmOWInvoiceToSgp.pas, uScanApi.pas), где передавался 7-й параметр,
  'AddQnt$i' исправлен на 'Adding$i' (реальное имя - Adding).
- P_SetOrderProdData (d_orders.sql): uFrmOGjrnOrders.pas - было ':id$i'
  (с лишним ведущим двоеточием, похоже на случайную вставку из SQL-синтаксиса
  bind-переменных), стало 'AIdOrder$i'.
- p_UserLogon (d_adm.sql): uUser.pas - было 'IdModule$i;IdUser$i;AVersion',
  стало 'AIdModule$i;AIdUser$i;AVersion$s' (реальные имена - AIdModule,
  AIdUser; третьему параметру также добавлен явный модификатор типа $s).
- p_GetDocumNum (d_adm.sql): везде (uFrmOWInvoiceToSgp.pas x2,
  uFrmOWPlannedOrder.pas) короткие имена d/y/n/ns/yp/dg исправлены на реальные
  ADocum/AYear/ANum/ANoStore/AYPrefix/ADigits.
- p_add_customer (d_orders.sql): в трёх РАБОТАЮЩИХ местах (uFrmOWOrder.pas,
  uFrmWOrderOld.pas, D_Order.pas - см. выше про призрак) числовые "имена"
  '1'..'5' и 'id1'/'id2'/'id3' исправлены на реальные customernamenew/
  contactnamenew/contactnew/legalnamenew/innnew/id_customer/id_contact/
  id_legal. Два ЗАВЕДОМО МЁРТВЫХ тестовых вызова (D_Order.pas:896 - файла
  больше нет; uFrmWOrderOld.pas:900 - идёт СРАЗУ после безусловного exit,
  физически недостижим) НЕ исправлялись - смысла нет, а трогать их лишний раз
  незачем.
- P_SyncOrderWithITM (d_orders.sql): uOrders.pas - было 'AOrItems$s', в
  реальной процедуре аргумент называется AOrImems (опечатка в самой SQL-
  процедуре) - исправлено на 'AOrImems$s', то есть Delphi-код теперь
  СОЗНАТЕЛЬНО повторяет опечатку БД, потому что биндинг идёт по имени.
- p_copy_std_estimate_to_order_item (d_estimates.sql): uOrders.pas - было
  'idestimate$i;idstdestimate$i;pqntinor$f', стало 'p_id_estimate$i;
  p_id_std_estimate$i;p_or_qnt$f'.
- P_CorrectEstimateWithReplace, p_DeleteFreeEstimate (d_estimates.sql):
  uOrders.pas (2 места) - было 'id_estimate$i', реальное имя - IdEstimate (без
  подчёркивания) - исправлено на 'IdEstimate$i'.
- P_CreateOrStdItem_Nstd (d_orders.sql): везде (uOrders.pas, D_Order.pas -
  призрак, не трогалось, uFrmWOrderOld.pas, uFrmOWOrder.pas) - было
  'name$s;newid$io', реальные имена NameItem/IdItem - исправлено на
  'NameItem$s;IdItem$io'.
- P_SetProp (d_adm.sql): везде (uOrders.pas x2, uServerTasks.pas x2) - было
  'p$s;sp$s;st$s;dt$d;i$i;f$f', реальные имена AProp/ASubProp/ASt/ADt/AI/AF -
  исправлено соответственно.
- P_CopyEstimateToUserTemp (d_estimates.sql): uOrders.pas - было
  'id_user$i;id_std_item$i;id_or_item$i', реальные имена AIdUser/AIdStdItem/
  AIdOrItem - исправлено.
- P_CreatePspForSemiproducts (d_orders.sql): uFrmOGjrnSemiproducts.pas - все
  10 параметров были короткими техническими именами (id_t, items, id_u, comm,
  dt_otgr, id_reg, id_ortype, orprops, id, ornum), исправлены на реальные
  AIdTemplate/AStdItems/AIdManager/AComment/ADtOtgr/AIdReglament/AIdType/
  AProps/AIdOrder/AOrNum (порядок и типы/направления сохранены).
- P_ExchangePositions (d_functions.sql): uFrmXGlstMain.pas - было
  't$s;f$s;p$i;d$i', реальные имена ATable/AField/APos/ADirection -
  исправлено.
- P_SetGridLabel (d_adm.sql): uFrDBGridEh.pas - было 'piduser$i' вместо
  реального 'pid_user$i' (с подчёркиванием) - исправлено; остальные 4
  параметра уже были верны.
- P_SaveScheduleHours (d_workers_new.sql): uFrmWWedtWorkSchedule.pas - было
  'Id$i;y$i;h$s', реальные имена AIdSchedule/AYear/AHours - исправлено.
- P_Spl_Create_History (d_suppliers.sql): uFrmOGedtSnMain.pas - было 'st$s',
  реальное имя AState - исправлено.
- p_insert_fin_monitoring_data (d_order_monitoring_new.sql): 2 места в
  uFrmOGrepOrdersFinMonitoring.pas - первые три параметра назывались
  p1/p2/p3, реальные имена p_dt/p_data_type/p_use_wo_list - исправлено
  (остальные 4 параметра уже были верны).

Итого исправлено 17 файлов (~35 конкретных вызовов): uOrders.pas, uUser.pas,
uFrmWOrderOld.pas, uFrmOWOrder.pas, uFrmOWPlannedOrder.pas,
uFrmOWInvoiceToSgp.pas, uScanApi.pas, uFrmOGjrnOrderStages.pas,
uFrmOGjrnSemiproducts.pas, uFrmXGlstMain.pas, uFrmOGjrnOrders.pas,
uFrmOGrepOrdersFinMonitoring.pas, uFrDBGridEh.pas, uFrmODedtNomenclFiles.pas,
uFrmOGedtSnMain.pas, uFrmWWedtWorkSchedule.pas, uServerTasks.pas. Каждый файл
после правки сверен побайтово (commit -> stage обратно -> diff) - расхождений
нет.

НЕ проверено (нет исходников для сверки, трогать не стал): вызовы
dv.p_SyncIzdel, dv.p_SyncOrder_Finish, dv.pnl_SyncOrder (D_Order.pas - файл-
призрак, реальный код для этого экрана - uFrmWOrderOld.pas, там таких вызовов
нет вовсе) и sys.kill_user_session (uFrmAWOracleSessions.pas) - определений
этих процедур нет ни в одном файле папки SQL (видимо, отдельная
схема/пакет dv или системная обвязка SYS, созданная вручную у DBA, не часть
прикладного SQL-репозитория этого проекта). Если по ним тоже нужна сверка -
нужно смотреть определения непосредственно в БД (например через
user_arguments/all_arguments), это уже не то, что можно проверить по файлам
проекта.


6.16. uPrintReport.pas (pnl_Order) - печать паспорта заказа использовала ADO
      напрямую, без Q; исправлено подключением ADO по требованию (17.09.2026)
-----------------------------------------------------------------------------
Пользователь спросил, остались ли в проекте прямые вызовы TMemTable/ADO без
обёртки через Q (TFrDBGridEh), предположив, что в uPrintReport.pas (модуль
печати через FastReport, класс TPrintReport) сделано прямое ADOConnection.

Проверено: весь uPrintReport.pas прочитан целиком. Действительно, на форме
(uPrintReport.dfm) есть frxQT, frxQB - оба TADOQuery, с Connection =
myDBOra.AdoConnection ("myDBOra" - это Name компонента Q (TmyDBOra), заданный
в uDBOra.dfm, а не отдельная Pascal-переменная - глобальная переменная,
хранящая этот же объект, называется в коде Q; DFM резолвит такие ссылки по
Name компонента через FindGlobalComponent, поэтому связь рабочая, хотя и
выглядит как "чужая" переменная при чтении кода). ИЗ ВСЕХ процедур модуля
только ОДНА реально использует frxQT/frxQB с прямым SQL и параметрами -
pnl_Order (печать "паспорта заказа"/"ПЗ с артикулами"). Все остальные
процедуры (pnl_StdItem, D_StdItem, D_Turvl, pnl_OrderLabels, D_OrderLabels,
pnl_PayrollLabels, pnl_Estimate, pnl_InvoiceToSgp) либо используют Q.QLoadRow
(через общую обёртку, от бэкенда не зависит), либо получают уже готовый
TMemTableEh от вызывающего кода (frxDsB/frxDsT.DataSet := MemTableEh1 или
frxMtB, заполняемый через SetReportDataSet) - к ADO/FireDAC никак не
привязаны и уже полностью бэкенд-независимы.

Проблема с pnl_Order: как выяснилось при чтении uDB.pas (см. 6.10, комментарий
в TmyDB.Connect), при Backend = mydbbFireDac обычное подключение (Q.Connect)
ТЕПЕРЬ ВООБЩЕ НЕ трогает ADO (ранняя ветка "if FBackend = mydbbFireDac then
begin ... Exit; end;" в самом начале Connect для Oracle) - именно для того,
чтобы не открывать две сессии Oracle на одно приложение сразу при старте. Но
это означает, что Q.AdoConnection.Connected остаётся False всё время работы
программы при Backend = FireDAC - соответственно frxQT/frxQB, завязанные на
это подключение в DFM, будут падать с ошибкой "не подключено" при попытке
напечатать паспорт заказа.

Исправление (только в uPrintReport.pas, TmyDB.Connect в uDB.pas не менялся -
решение 6.10 не отменяется): в начале pnl_Order добавлена проверка - если
Q.AdoConnection ещё не подключен, подключить его "по требованию", теми же
параметрами, что использует сам TmyDB.Connect в ADO-ветке (ConnectionString :=
Q.ConnectionString; LoginPrompt := False; Connected := True), в try/except с
понятным сообщением пользователю при неудаче. Таким образом вторая ("ADO")
Oracle-сессия открывается НЕ всегда, а только один раз за сеанс работы
программы, и только если реально печатается паспорт заказа - то есть проблема
"двух сессий на всё приложение", решённая в 6.10, не возвращается, а
конкретный отчёт при этом продолжает работать.

ВАЖНОЕ ОГРАНИЧЕНИЕ: это решение работает, только пока в uchet.cfg/
uchet_test.cfg для Oracle остаётся секция [Oracle.ADO] (см. 6.11) - именно
оттуда берётся Q.ConnectionString. Если в будущем эту секцию уберут совсем
(полный отказ от ADO), pnl_Order надо будет переводить на FireDAC по-
настоящему - то есть менять frxQT/frxQB с TADOQuery на TFDQuery в
uPrintReport.dfm (именованные параметры вида :id в SQL FireDAC понимает
нативно, так что сам текст запросов менять не придётся) и Connection - на
Q.FdConnection. Такая правка требует изменения состава компонентов датамодуля
в DFM, что сложнее и рискованнее точечной правки кода - решил не делать этого
сейчас без возможности пользователя сразу проверить результат печати, если
uchet.cfg по-прежнему содержит секцию ADO для Oracle (что сейчас так и есть -
её специально оставили при миграции 6.11 именно для подобных случаев),
сделанного исправления должно быть достаточно.

6.17. QCallStoredProc/FireDAC: конфликт типов параметров с каталогом БД
      (ORA-338 "type changed"), общее исправление в самой QCallStoredProc (17.09.2026)
-----------------------------------------------------------------------------
Продолжение 6.15. После исправления имён параметров p_UserLogon (IdModule ->
AIdModule, IdUser -> AIdUser) при первом же запуске вылезла новая ошибка:
[FireDAC][Phys][Ora]-338. Param [AIdModule] type changed from [ftFMTBcd] to
[ftInteger]. Query must be reprepared.
Смена типа модификатора с $i на $f (ftFloat) не помогла - та же ошибка, просто
уже "changed ... to [ftFloat]". Пользователь верно определил, что это будет
распространённая, а не разовая проблема - в реальных процедурах очень много
параметров объявлено просто как "number" (без точности/масштаба), а FireDAC
для такого типа при работе с Oracle ожидает ftFMTBcd - независимо от того,
какой конкретно тип ($i/$f/...) мы сами укажем в ParamNames, если имя
параметра совпадает с реальным (как и должно быть после 6.15) - при
несовпадении с ftFMTBcd вылетает эта же ошибка.

Причина: TFDStoredProc.Prepare (либо неявно, при первом ExecProc) подтягивает
из каталога БД реальные метаданные параметров процедуры - их настоящие имена
И типы. Прежний код в QCallStoredProc для FireDAC каждый раз создавал параметр
заново через Params.CreateParam(pt, RawParamName, fpt), где pt - наш
СОБСТВЕННЫЙ тип, вычисленный из "$"-модификатора (ftInteger/ftFloat/...). Если
имя совпадает с реальным аргументом процедуры (что правильно и обязательно
для FireDAC, см. 6.6/6.15), а наш тип НЕ совпадает с тем, что БД/FireDAC
считает правильным для этого аргумента - получаем конфликт "type changed".
То есть сама затея "угадывать" тип параметра по одной букве в ParamNames
принципиально ненадёжна для FireDAC - для Oracle "number" может быть и
ftFMTBcd, и ftInteger, и ftFloat в зависимости от точности/масштаба, а по
одной букве в коде Delphi мы этого не знаем и знать не можем.

Исправление сделано ОДИН РАЗ, в самой QCallStoredProc (uDB.pas), а не по
каждому месту вызова - теперь для FireDAC:
1. Сразу после FFdStoredProc.StoredProcName := ProcName вызывается
   FFdStoredProc.Prepare (в try/except, чтобы неудача не ломала следующий,
   запасной путь) - это заставляет FireDAC подтянуть из каталога БД реальные
   параметры процедуры с их настоящими типами.
2. При простановке каждого параметра теперь СНАЧАЛА проверяется, не создал
   ли уже Prepare параметр с таким именем (FFdStoredProc.Params.FindParam) -
   если да, используется он как есть (без смены типа, только .Value:=...);
   создание вручную через CreateParam (со своим типом по "$"-модификатору,
   как раньше) остаётся только запасным путём - на случай, если Prepare не
   удался или параметр с таким именем в каталоге не нашёлся.
3. Сбор результата (Result) для FireDAC ПЕРЕДЕЛАН: раньше он собирался по
   ВСЕМ FFdStoredProc.Params (FFdStoredProc.Params.Count) - но т.к. теперь
   Prepare подтягивает из каталога ВСЕ реальные параметры процедуры (включая
   необязательные, с default, которые вызывающий код мог не указать в своей
   строке ParamNames - см., например, p_OrderStage_SetItem/Adding в 6.15),
   FFdStoredProc.Params.Count может стать БОЛЬШЕ, чем раньше, и Result
   "разъехался" бы по длине/порядку с тем, что ожидают вызывающие места (они
   обращаются к результату по индексу, соответствующему позиции параметра в
   ИХ СОБСТВЕННОЙ строке ParamNames). Поэтому результат теперь собирается по
   именам из ParamNames (как их передал вызывающий код), в том же порядке -
   ровно как было по факту раньше, просто через поиск по имени вместо прямого
   перебора Params.

Важно: для ADO-ветки (Backend = mydbbAdo) ничего не изменилось - там имя и
тип параметра в любом случае не влияют на реальный вызов (позиционный
{call proc(?,?)}), поэтому этот класс ошибок для ADO в принципе невозможен.

Эффект: класс ошибок "[FireDAC][Phys][Ora]-338 type changed" должен быть
закрыт для ВСЕХ вызовов QCallStoredProc через FireDAC разом, а не только для
p_UserLogon - в том числе для мест, которые я мог не заметить/не проверить в
6.15, и для любых будущих аналогичных случаев. "$"-модификатор типа в
ParamNames для FireDAC теперь фактически используется только как ЗАПАСНОЙ
вариант (если Prepare не смог найти реальный параметр по имени) - раньше он
был обязательным для каждого параметра.
uUser.pas: попутную правку AIdModule$i/AIdUser$i -> AIdModule$f/AIdUser$f
(сделанную ДО этого общего исправления, как временную заплатку) оставил как
есть - она больше не имеет значения для FireDAC (тип теперь берётся из
каталога БД), а для ADO-пути $f для целочисленного id тоже безопасен.

ТРЕБУЕТСЯ ШИРОКАЯ ПРОВЕРКА пользователем - это изменение затрагивает общий
код, используемый ВСЕМИ ~90 вызовами QCallStoredProc в проекте, а не точечное
место, как предыдущие правки. Стоит проверить не только вход в программу
(p_UserLogon), но и хотя бы одно действие, вызывающее другую хранимую
процедуру через грид/форму (например сохранение этапа заказа, простановка
дефицита в снабжении и т.п.), прежде чем считать миграцию на FireDAC
полностью стабильной.

6.18. uUser.pas - потерялась метка BOM (UTF-8), из-за чего кириллица стала
      нечитаемой в редакторе (квадратики) (17.09.2026)
-----------------------------------------------------------------------------
После правки p_UserLogon (6.15/6.17) пользователь сообщил, что в uUser.pas
русский текст стал нечитаемым (квадратики) в редакторе Delphi. Все *.pas
файлы проекта хранятся в UTF-8 с BOM (метка \xEF\xBB\xBF в самом начале файла)
- это то, по чему редактор Delphi понимает, что файл в UTF-8, а не в ANSI/
CP1251. Проверка показала, что именно у uUser.pas после моих правок BOM в
начале файла отсутствовал (файл начинался сразу с "unit uUser") - при этом
сам текст внутри файла был полностью корректным UTF-8 (проверено - весь файл
целиком успешно раскодировался как UTF-8, без ошибок), проблема была именно
в отсутствующей метке, из-за чего редактор, видимо, показывал файл как ANSI/
CP1251 и вся кириллица (включая старый, не тронутый мной текст, не только мои
новые комментарии) отображалась неверно.
Проверены на BOM все остальные *.pas файлы, правившиеся в этом сеансе работы
(uDB.pas, uPrintReport.pas и все 17 файлов из аудита хранимых процедур,
6.15) - BOM у них у всех на месте, проблема была именно и только в uUser.pas.
Причина потери BOM именно в этом файле осталась не до конца ясна (возможно,
дело в том, что это была первая правка в файле за весь этот проект работы, и
что-то в процессе синхронизации с устройством не сохранило метку) - тем не
менее, исправление точечное: BOM дописан обратно в начало файла, остальное
содержимое не менялось, сверено побайтово после этого.
Если у пользователя обнаружатся квадратики/нечитаемая кириллица еще в каких-
то *.pas файлах - решение то же самое: проверить наличие BOM (\xEF\xBB\xBF) в
начале файла и, если его нет, дописать обратно, не трогая остальное
содержимое.

6.19. Уточнение к 6.18: реальная причина "квадратиков" в uUser.pas была не в
      BOM, а в потере текста при чтении CP1251-файла; файл восстановлен
      пользователем из git (17.09.2026)
-----------------------------------------------------------------------------
Диагноз в 6.18 (потерялся BOM) оказался неполным - пользователь сообщил, что
квадратики остались даже после перезапуска Delphi, то есть проблема была не в
метке кодировки, а в самом содержимом файла.

Настоящая причина: в отличие от всех остальных проверенных *.pas файлов
проекта (UTF-8 с BOM), uUser.pas изначально был в кодировке CP1251 БЕЗ BOM -
единственный такой файл среди всех, с которыми велась работа в этом сеансе.
Когда этот файл читался впервые (для правки p_UserLogon, 6.15), часть
кириллических байтовых последовательностей не была корректно распознана и
была молча заменена на символ-заменитель Юникода (U+FFFD) - то есть
фрагменты исходного русского текста были безвозвратно потеряны в рабочей
копии файла на этом (Claude-)конце, включая как минимум два места с реальным
влиянием на работу программы (не просто комментарии):
  if ComputerUserLogin = 'Администратор' then Exit;
  ...
  Cb.Items.Add('Администратор');
(итого 17 строк с потерянным текстом, большинство - комментарии). Это не было
обнаружено сразу, т.к. использовавшаяся проверка "побайтовой идентичности
после синхронизации" сравнивала уже испорченную копию саму с собой (после
отправки на устройство и повторного скачивания) - обе стороны сравнения
содержали одну и ту же порчу, и диф совпадал, хотя исходное содержимое было
уже неверным. Последующее добавление BOM (6.18) исправляло только метку
кодировки, а не сам испорченный текст - поэтому квадратики (на самом деле -
символы U+FFFD, а не проблема отображения) никуда не делись.

Т.к. у Claude в этом сеансе нет доступа к shell на стороне пользователя (нет
инструмента device_bash, только GUI-автоматизация), самостоятельно
восстановить исходный файл из git было невозможно - пользователь сделал это
сам ("восстановил прошлую версию"). Проверка восстановленного файла
подтвердила: файл снова корректный CP1251 без BOM (как и было изначально, без
искажений - iconv не сообщает об ошибках при перекодировке в UTF-8, все три
вхождения 'Администратор' на месте), но восстановление также откатило
целевую правку имён параметров p_UserLogon из 6.15 (снова стало
'IdModule$i;IdUser$i;AVersion' вместо 'AIdModule...;AIdUser...'). Эта правка
переприменена ТОЧЕЧНО - заменена ровно эта ASCII-строка параметров (без
затрагивания кириллического текста файла, во избежание повторения той же
порчи), тип параметров оставлен $i (как было изначально) - после общего
исправления QCallStoredProc (6.17) конкретная буква типа для FireDAC уже не
критична, реальный тип определяется по каталогу БД. Синхронизация с
устройством подтверждена побайтово.

Вывод на будущее: при первом чтении ЛЮБОГО файла, который открывается в
рамках этой работы впервые, стоит сначала проверить, является ли он
UTF-8-с-BOM (как большинство файлов проекта), и если нет - обращаться с ним
особенно осторожно (по возможности - точечные правки ASCII-содержимого без
полной перекодировки/перезаписи всего текста), т.к. именно такие файлы-
исключения оказались источником этой порчи.

6.20. uFrDBGridEh.pas - альтернативный фильтр по Alt-F вместо стандартного окна
      фильтра ехлиб (первый срез - числовые столбцы) (17.09.2026)
-----------------------------------------------------------------------------
Пользователь предложил сделать альтернативу стандартному окну фильтра ехлиб
(список всех уникальных значений столбца с чекбоксами - неудобно): для дат -
выбор периода как в Excel, для чисел - пункты "Больше 0"/"Меньше 0", для всех
столбцов - фильтр по цвету шрифта/фона ячейки (эта часть ранее была начата,
но код случайно затерт в git, наработки не сохранились). Решено делать
поэтапно, начиная с самого простого случая (числа), чтобы сразу проверить
всю цепочку Alt-F -> меню -> применение фильтра.

Реализовано (пока только для числовых столбцов):
1. По Alt-F (DbGridEh1KeyDown) для текущего столбца, если его поле числовое
   (ftSmallint/ftInteger/ftWord/ftLargeint/ftFloat/ftCurrency/ftBCD/ftFMTBcd),
   вызывается ShowAltColumnFilter - динамически создает и показывает
   TPopupMenu с пунктами "Больше 0"/"Меньше 0" (с галкой у активного условия)
   и "Сбросить фильтр по столбцу", позиционируется под текущей ячейкой
   (DBGridEh1.CellRect+ClientToScreen). Для нечисловых столбцов пока ничего
   не происходит (в дальнейшем добавится своя реализация для дат и цвета).
2. Выбор пункта (AltNumFilterMenuClick) вызывает SetAltNumFilter(ИмяПоля,
   Условие) - сохраняет условие в новом поле FAltNumFilters (TVarDynArray2:
   [i][0] - имя поля в нижнем регистре, [i][1] - '>0'/'<0'), либо убирает
   запись оттуда при сбросе.
3. Применение фильтра сделано ЧЕРЕЗ ОТДЕЛЬНЫЙ МЕХАНИЗМ, независимо от
   стандартного постолбцового фильтра ехлиб (STFilter/чекбоксы) - через
   MemTableEh1.OnFilterRecord (этот датасет-евент раньше в проекте нигде не
   использовался). TDataSet.Filtered учитывает Filter (строка, которой
   управляет сам ехлиб через STFilter) И OnFilterRecord ОДНОВРЕМЕННО, через
   И - то есть наши условия по Alt-F работают НЕ ЗАМЕНЯЯ, а ДОПОЛНИТЕЛЬНО к
   обычным чекбоксовым фильтрам на других столбцах, которые продолжают
   работать как раньше. Решено не пытаться дописывать наше условие в
   собственную строку-выражение фильтра ехлиб (не факт, что грамматика
   MemTableEh.Filter поддерживает такие проверки, и разбираться в этом
   рискованно) - вместо этого чистый Pascal-код в MemTableEh1FilterRecord.
   Само применение (после изменения FAltNumFilters) делается переключением
   MemTableEh1.Filtered (False, затем True) - это гарантированно заново
   прогоняет фильтрацию по всем строкам; DBGridEh1.DefaultApplyFilter (ехлib)
   специально НЕ используется для этой цели, т.к. он управляет своим
   собственным условием STFilter и мог бы не знать о нашем FAltNumFilters
   вовсе (в частности, потенциально мог бы выключить Filtered совсем, если
   решит, что активных ехлib-фильтров нет).

Пока НЕ сделано (следующие этапы):
- Фильтр по периоду для столбцов с датами (в проекте уже есть готовый
  похожий UI - выбор периода/дней с быстрым меню периодов в
  uFrmXDedtGridFilter (dedt1/dedt2/cmbPeriod/cmbDays/DatePeriods/
  S.GetDatePeriod) - для ДРУГОЙ задачи (фильтр SQL при загрузке из БД), но
  саму механику выбора периода планируется переиспользовать).
- Фильтр по цвету шрифта/фона ячейки - служебное скрытое поле с
  сериализованной информацией о том, где явно задан цвет (заполняется
  отдельным проходом по ВСЕМ строкам MemTableEh, а не только по видимым -
  важно, т.к. OnGetCellParams/ColumnsGetCellParams у ехлib вызывается только
  для реально отрисованных ячеек, и на этом нельзя строить полный кэш).
- Никакой визуальной отметки на заголовке столбца о том, что альтернативный
  фильтр активен, пока нет - надо иметь это в виду при тестировании (фильтр
  может быть незаметно "забыт" включенным).
- Обработка исключений в новых процедурах пока минимальная (без общего
  try/except, как в некоторых других методах фрейма) - сделано намеренно на
  этом этапе отладки, чтобы ошибки не проглатывались молча.

ТРЕБУЕТСЯ ПРОВЕРКА пользователем на реальном гриде с числовым столбцом:
Alt-F должен открывать меню, выбор пункта - фильтровать строки, повторный
Alt-F на этом же столбце должен показывать текущее выбранное условие
галкой, "Сбросить фильтр по столбцу" должен возвращать все строки.

6.21. uFrDBGridEh.pas - меню ShowAltColumnFilter показывалось, но клики по нему
      не работали - показ отложен через PostMessage (17.09.2026)
-----------------------------------------------------------------------------
Продолжение 6.20. Пользователь сообщил: по Alt-F меню появляется, но клик по
пункту меню ничего не делает, при повторном открытии вид меню (галки) не
меняется - то есть обработчик пункта меню (AltNumFilterMenuClick) фактически
не вызывался.

Причина: ShowAltColumnFilter вызывался СИНХРОННО, прямо из обработчика
DbGridEh1KeyDown, в момент обработки нажатия Alt-комбинации (Alt все еще
"держится" с точки зрения Windows - сочетания с Alt обрабатываются через
WM_SYSKEYDOWN/WM_SYSCOMMAND(SC_KEYMENU), а не обычный WM_KEYDOWN). Показ
popup-меню (TPopupMenu.Popup, через TrackPopupMenu) ПОКА Windows еще не
закончил свою системную обработку Alt как модификатора меню, приводит к
тому, что клики по уже показанному меню не доходят до OnClick - меню
визуально показывается, но фактически "не отвечает". Это известная особенность
Windows/VCL, не связанная с логикой самого фильтра.

Исправление: показ меню отложен на следующую итерацию цикла сообщений -
вместо прямого вызова ShowAltColumnFilter в DbGridEh1KeyDown теперь
отправляется (PostMessage) собственное сообщение WM_MY_ALTCOLUMNFILTER
(= WM_USER + 733), которое обрабатывается новым методом
TFrDBGridEh.WMAltColumnFilter (message-метод) - и уже ОТТУДА вызывается
ShowAltColumnFilter. К моменту обработки PostMessage-сообщения (это
происходит уже на следующей итерации цикла сообщений) Windows успевает
полностью завершить обработку Alt-комбинации, и клики по меню начинают
доходить до OnClick нормально. Дополнительно, в KeyDown для Alt-F теперь
явно ставится Key := 0 (штатный способ "погасить" дальнейшую стандартную
обработку клавиши в VCL).

ТРЕБУЕТСЯ ПРОВЕРКА пользователем: меню по Alt-F должно теперь реагировать на
клик, фильтровать строки, а повторное открытие меню - показывать текущее
выбранное условие галкой.

Заодно (по просьбе пользователя) добавлен третий пункт меню - "Равно 0"
(условие '=0', как и '>0'/'<0' - без допуска на погрешность вычислений,
см. комментарий в MemTableEh1FilterRecord).

6.23. uFrDBGridEh.pas - клики по меню ShowAltColumnFilter (TPopupMenu) не
      доходили до OnClick - заменено на обычное модальное окно (17.09.2026)
-----------------------------------------------------------------------------
Продолжение 6.20-6.22. Добавленный отладочный вывод (OutputDebugString) в
ShowAltColumnFilter/AltNumFilterMenuClick дал ключевой факт: pm.Popup(...)
корректно ЖДАЛ действия пользователя (разница тиков до/после вызова - около
1.5-3 секунд в двух проверках, то есть Popup не возвращался мгновенно), но
после его завершения строка "AltNumFilterMenuClick: ВЫЗВАН" НИ РАЗУ не
появлялась в журнале - то есть ни один из пунктов меню (включая заведомо
активный "Больше 0", проверено отдельно) не вызывал OnClick. Причина осталась
не до конца понятной (похоже на особенность взаимодействия TrackPopupMenu в
этом сочетании обстоятельств - показ через PostMessage-отложенный вызов,
дополнительно уже пробовали и без отложенного вызова, 6.21 - тоже не помогло) -
дальше разбираться с внутренностями TPopupMenu/WM_COMMAND признано
нецелесообразным.

Вместо этого меню (TPopupMenu) полностью заменено на обычное МОДАЛЬНОЕ ОКНО
(TForm, создается в коде через TForm.CreateNew, без .dfm) с обычными кнопками
TButton - показывается через ShowModal, что гораздо надежнее (ShowModal не
зависит от маршрутизации WM_COMMAND между окном-владельцем и пунктом меню,
это просто синхронный модальный цикл с обычным возвращаемым Integer).
Кнопки: "Больше 0" (mrYes), "Меньше 0" (mrNo), "Равно 0" (mrAll), "Сбросить
фильтр по столбцу" (mrIgnore, неактивна, если фильтр по столбцу не установлен).
Esc и системное закрытие окна (крестик/Alt+F4, ВНИМАНИЕ - VCL в этом случае
сам подставляет mrCancel, если ModalResult не был явно установлен) - оба
трактуются как отмена без изменений. Активное сейчас условие выделяется
жирным шрифтом на кнопке (было - галкой в меню).

Это, по сути, ближе к первоначальной формулировке задачи ("окно"), чем
промежуточный вариант с выпадающим меню.

ТРЕБУЕТСЯ ПРОВЕРКА пользователем - должно наконец заработать: Alt-F на
числовом столбце -> окно с 4 кнопками -> клик по кнопке фильтрует грид и
закрывает окно.


6.24. Alt-F: альтернативный фильтр по столбцам с датой/датой-временем (TFrDBGridEh)

Пользователь подтвердил, что числовой альт-фильтр (6.20-6.23) работает, и попросил
сделать аналогичный фильтр для столбцов с датами ("теперь все работает давай
сделаем с датами"). Реализовано по той же схеме - независимый от штатного
постолбцового фильтра ехлиб механизм через TDataSet.Filtered/OnFilterRecord
(см. 6.20 - Filtered учитывает Filter и OnFilterRecord одновременно, через И,
поэтому наш альт-фильтр может работать одновременно со штатным STFilter на других
столбцах без конфликтов).

ShowAltColumnFilter превращена в тонкий диспетчер по типу поля (Fld.DataType):
числовые типы (ftSmallint/ftInteger/ftWord/ftLargeint/ftFloat/ftCurrency/ftBCD/
ftFMTBcd) - ShowAltNumFilterWindow (без изменений, 6.20-6.23), типы дат/времени
(ftDate/ftDateTime/ftTimeStamp/ftTime) - новая ShowAltDateFilterWindow.

ShowAltDateFilterWindow(Fld: TField) - модальное окно (TForm.CreateNew, без .dfm,
тот же проверенный на числовом фильтре подход - ShowModal вместо TPopupMenu,
см. 6.23), содержит:
  - TListBox со списком уже существующего в проекте массива DatePeriods (uString.pas,
    12 стандартных периодов вроде "Сегодня", "Вчера", "Этот месяц" и т.п. - тот же
    список, что использует quick-period-меню в uFrmXDedtGridFilter/PmPeriodClick) -
    клик по пункту сразу применяет период и закрывает окно (ModalResult := mrOk),
    выбранный индекс передается в уже готовую S.GetDatePeriod(Period, Date, dt1, dt2)
    (uString.pas), которая и превращает индекс периода в диапазон дат;
  - два TDateTimePicker (Kind := dtkDate) "с"/"по" для произвольного периода и кнопка
    "Применить период" (mrYes) - если даты перепутаны местами (по > с), меняются
    местами автоматически, чтобы не показать пустой грид без объяснений;
  - кнопка "Сбросить фильтр по столбцу" (mrIgnore, неактивна, если фильтр по этому
    полю сейчас не установлен) - симметрично числовому окну;
  - Esc/системное закрытие - mrCancel, без изменений (обработчик
    AltColumnFilterFormKeyDown - общий с числовым окном).
Окно позиционируется под текущей ячейкой так же, как числовое (DBGridEh1.CellRect +
ClientToScreen, через Point(R.Left, R.Bottom) - см. 6.22 про отсутствие
TRect.BottomLeft).

Хранение состояния - новое поле FAltDateFilters: TVarDynArray2, элемент
[ИмяПоля_lowercase, ДатаС, ДатаПо] (по аналогии с FAltNumFilters из 6.20, но три
элемента вместо двух). Управление - SetAltDateFilter(FieldName, DateFrom, DateTo) /
ClearAltDateFilter(FieldName), обе переключают MemTableEh1.Filtered False->True для
гарантированного немедленного перепрохода фильтра (как и для чисел).

Проверка в MemTableEh1FilterRecord - добавлен второй цикл (после уже существующего
цикла по FAltNumFilters), по тому же образцу (DataSet.FindField с Continue если поля
нет в наборе, try/except вокруг чтения значения поля с Continue при ошибке чтения).
Условие отсева: Accept := False, если
  (vd < ДатаС) or (vd >= ДатаПо + 1)
Верхняя граница специально не включительная и увеличена на 1 (сутки), чтобы
захватить весь последний день периода целиком независимо от возможной временной
части в значении поля (например, дата-время 17.09.2026 23:59:59 должна попадать в
период "по 17.09.2026", а простое сравнение vd <= ДатаПо это бы не гарантировало).

НЕ СДЕЛАНО (как и для чисел, 6.20): визуальный индикатор "фильтр активен" на
заголовке столбца, более широкая обработка исключений. Также не проверялось
поведение для полей типа ftTime (только время без даты) - для них S.GetDatePeriod
и TDateTimePicker (dtkDate) могут работать не совсем осмысленно, этот случай не
тестировался.

ТРЕБУЕТСЯ ПРОВЕРКА пользователем: Alt-F на столбце с датой -> окно со списком
периодов и полями "с"/"по" -> выбор периода или произвольного диапазона фильтрует
грид.


6.25. Исправление ошибки компиляции в ShowAltDateFilterWindow (E2009)

При компиляции после 6.24 - ошибка на строке "lst.OnClick := ListClick;":
  [dcc32 Error] uFrDBGridEh.pas(5380): E2009 Incompatible types: 'method pointer
  and regular procedure'
Причина: ListClick была объявлена как ВЛОЖЕННАЯ (nested) процедура внутри самой
ShowAltDateFilterWindow (чтобы иметь доступ к локальным переменным lst, frm,
PickedPeriod через замыкание). Но события вроде TListBox.OnClick имеют тип
TNotifyEvent = procedure(Sender: TObject) OF OBJECT - "процедура объекта" (метод,
несущий два указателя - на код и на Self). Вложенная процедура - это "обычная"
процедура (несёт указатель на стековый фрейм объемлющей процедуры вместо Self) -
эти два вида указателей несовместимы, поэтому и остальные обработчики в этом же
файле (AltColumnFilterFormKeyDown и т.п.) всегда были обычными МЕТОДАМИ КЛАССА
TFrDBGridEh, а не вложенными процедурами - только методы класса можно назначать
в такие события.

Исправлено: ListClick убрана из тела ShowAltDateFilterWindow, вместо неё - новый
метод класса TFrDBGridEh.AltDateFilterListClick(Sender: TObject). Доступ к нужным
данным без замыкания на локальные переменные:
  - выбранный индекс пункта списка - через новое поле класса FAltDateFilterPickedPeriod
    (вместо локальной переменной PickedPeriod);
  - сама форма (чтобы установить ModalResult) - через TForm(TListBox(Sender).Owner),
    т.к. TListBox создан как TListBox.Create(frm), и поэтому Owner листбокса - это
    сама модальная форма frm.
lst.OnClick := AltDateFilterListClick; case-блок после frm.ShowModal читает
FAltDateFilterPickedPeriod вместо локальной переменной.

Тот же самый принцип (только методы класса, не вложенные процедуры, годятся для
OnClick/OnKeyDown и т.п.) стоит иметь в виду и для будущих подобных динамически
создаваемых окон (например, для окна фильтра по цвету, которое ещё предстоит
сделать - см. изначальный запрос про раскраску столбцов).


6.26. Alt-F: альтернативный фильтр по цвету (шрифт/фон) - третья и последняя часть исходного запроса
(после чисел, 6.20-6.23, и дат, 6.24-6.25)

Реализована третья часть изначально запрошенной функциональности - фильтр по цвету, доступный для
СТОЛБЦА ЛЮБОГО ТИПА (в отличие от числового/датового фильтров): напрямую по Alt-F для строковых и
прочих столбцов (диспетчер ShowAltColumnFilter), и дополнительно по кнопке "Цвет..." в уже готовых
окнах числового и датового фильтров (кнопка закрывает текущее окно и сразу открывает окно фильтра по
цвету - два модальных окна одновременно показать нельзя).

ГЛАВНАЯ АРХИТЕКТУРНАЯ ИДЕЯ (по итогам обсуждения с пользователем, т.к. предыдущая реализация этой же
идеи была случайно потеряна в гите): расчет цвета ячейки НЕ дублируется отдельным кодом. В проекте уже
есть ~30+ форм, каждая из которых определяет свою бизнес-логику раскраски ячеек через свойство
TFrDBGridEh.OnColumnsGetCellParams (FOnColumnsGetCellParams) - обработчик, который вызывается штатно
при отрисовке КАЖДОЙ видимой ячейки (см. ColumnsGetCellParams, вызывается из STFilter/DBGridEh1 при
покраске). Вместо того чтобы копировать/переизобретать эту логику для фильтра, кэш цветов заполняется
ВЫЗОВОМ ТОГО ЖЕ САМОГО FOnColumnsGetCellParams, но не во время отрисовки, а синтетически - для каждой
строки/столбца по отдельности, с "чистым" TColCellParamsEh, у которого до вызова обработчика
установлены базовые (неявные) цвета - Column.Font.Color и Column.Color. Это ровно то же самое, что
делает сам ехlib перед вызовом OnGetCellParams при обычной отрисовке (проверено по исходникам ехlib -
TAxisBarEh.FillColCellParams в DBAxisGridsEh.pas: p.FFont.Assign(Font); p.Background := Self.Color).
Если после вызова обработчика Font.Color/Background отличаются от этих базовых значений - значит,
обработчик установил цвет явно, и это фиксируется в кэше. См. ComputeCurrentRowColorFmt.

Ни одна из ~30 форм НЕ модифицируется и не должна ничего дополнительно делать - весь механизм целиком
внутри TFrDBGridEh, соответствует пожеланию пользователя ("вызов в самом фрейме", "не дублировать
логику раскраски").

КЭШ (FColorCache: TObjectDictionary<string, TDictionary<Integer, TCellColorFmt>>) - внешний ключ имя
поля (нижний регистр), внутренний - ID записи (Opt.Sql.IdField). Отсутствие записи в кэше = "явного
цвета нет". TCellColorFmt = (HasFont, FontColor, HasBg, BgColor) - только цвет шрифта и/или фона (без
стилей типа полужирный/курсив - как и в изначальном описании задачи).

РЕЖИМ ПЕРЕСЧЕТА - реализованы ОБА варианта одновременно (по явному пожеланию пользователя - "будем
тестировать"), переключение константой ALT_COLOR_CACHE_EAGER:
- True (сейчас включено по умолчанию) - "энергичный" режим: полный пересчет кэша для ВСЕЙ таблицы
  сразу после каждого обновления грида (RefreshColorCacheAll, вызывается в конце RefreshGrid) и после
  обновления одной строки (RefreshColorCacheCurrentRow, вызывается в конце RefreshRecord).
- False - "ленивый" режим: кэш для столбца заполняется только в момент открытия окна фильтра по цвету
  на этом столбце (RefreshColorCacheField, вызывается из GetDistinctColorFmtsForField). Экономит время
  на обновлении грида, но первое открытие фильтра по цвету на конкретном столбце требует полного
  прохода по таблице именно в этот момент (может быть заметно на больших гридах).
И полный проход (RefreshColorCacheAll/RefreshColorCacheField), и точечный (RefreshColorCacheCurrentRow)
используют DisableControls/сохранение-восстановление текущей позиции через MemTableEh1.RecNo - как и в
других похожих полных проходах по MemTableEh1 в этом же модуле (например, SetIndicatorCheckBoxesByField).

ФИЛЬТРАЦИЯ - активные фильтры по цвету (FAltColorFilters: TObjectDictionary<string, TList<TCellColorFmt>>,
ключ - имя поля, значение - список ПРИНИМАЕМЫХ форматов, условие ИЛИ) проверяются третьим (после чисел
и дат) циклом в уже существующем MemTableEh1FilterRecord - т.е. работает независимо от и вместе со
стандартным постолбцовым фильтром ехlib и с двумя другими альт-фильтрами (см. 6.20 про Filtered/
OnFilterRecord). Проверка - просто чтение уже готового кэша (GetCachedCellColorFmt), без пересчета "на
лету" - поэтому и в "ленивом" режиме к моменту применения фильтра кэш для нужного столбца уже полностью
заполнен (см. выше про RefreshColorCacheField, вызывается ДО открытия окна). "Формат без явного цвета"
специально может быть одним из принимаемых - тогда включаются и строки без явной раскраски (аналог
пункта "Без заливки" в фильтре по цвету Excel).

ОКНО (ShowAltColorFilterWindow) - список форматов, реально встречающихся в столбце сейчас
(GetDistinctColorFmtsForField, первый пункт всегда "без явного цвета" - даже если сейчас в столбце нет
таких строк). Обычный TListBox (не TCheckListBox, чтобы не зависеть от недокументированной раскладки
области отрисовки последнего) с ручной покраской (Style=lbOwnerDrawFixed, ColorFilterListDrawItem):
для каждого пункта рисуется чекбокс (DrawFrameControl/DFCS_BUTTONCHECK - стандартный способ нарисовать
системный чекбокс без реального оконного контрола), цветной образец (заливка фона + буквы "Aa" цветом
шрифта - как в фильтре по цвету Excel) и подпись. Клик по пункту или пробел переключают чекбокс
(ColorFilterListClick/ColorFilterListKeyPress/ToggleColorFilterCheck - методы класса, а не вложенные
процедуры, см. 6.25 про несовместимость с TNotifyEvent). Кнопки "Применить" и "Сбросить фильтр по
столбцу" - по образцу окон чисел/дат.

См. также 6.25 про то, что все обработчики событий должны быть методами класса TFrDBGridEh, а не
вложенными процедурами - это соблюдено везде в новом коде.

НЕ СДЕЛАНО / ОГРАНИЧЕНИЯ:
- только цвет шрифта/фона, без стилей шрифта (полужирный и т.п.) - как и просил пользователь изначально;
- ошибка внутри чьего-то обработчика раскраски при синтетическом вызове (ComputeCurrentRowColorFmt)
  тихо трактуется как "явного цвета нет" (try/except) - чтобы не сломать весь пересчет кэша целиком
  из-за одной проблемной формы/столбца; если реально проявится - можно будет добавить логирование;
- производительность "энергичного" режима на больших таблицах (много строк * много столбцов, для
  каждой ячейки - реальный вызов чужого обработчика) не проверялась - для этого и добавлен переключатель
  ALT_COLOR_CACHE_EAGER, тестируем оба варианта на реальных данных;
- визуальный индикатор "фильтр активен" на заголовке столбца по-прежнему не сделан ни для одного из
  трех альт-фильтров (см. также 6.20).

ТРЕБУЕТСЯ ПРОВЕРКА пользователем (это последняя из трех запрошенных частей): Alt-F на любом столбце,
не являющемся числом или датой, либо кнопка "Цвет..." в окне числового/датового фильтра -> список
реально встречающихся в столбце цветов с чекбоксами -> выбор и "Применить" фильтрует грид.


6.27. Несколько доработок фильтрации по просьбе пользователя: Resync в FD-режиме, включение
InstantApply (условный фильтр наконец применяется сразу), индикация активного фильтра в заголовке
столбца, кнопка полного сброса всех фильтров, и альт-фильтр по дате "диапазон от сегодня" (в днях)

а) RefreshFdRecordDirect (FD-режим RefreshRecord) - добавлен MemTableEh1.Resync([]) после
MemTableEh1.Post. Разбор (по просьбе пользователя объяснить логику): исчезновение строки из грида
после правки, если она перестала проходить фильтр, физически обеспечивается ОДНИМ и тем же
механизмом что для стандартного постолбцового фильтра ехlib, что для наших альт-фильтров - методом
TCustomMemTableEh.IsRecordInFilter (MemTableEh.pas), который вызывается при переопределении текущей
позиции курсора внутри отфильтрованного представления (FRecordsView), и который сам ANDит проверку
скомпилированной строки Filter и вызов OnFilterRecord (это и есть причина, почему Filtered учитывает
оба механизма одновременно - см. 6.20). Сам по себе Post НЕ вызывает эту проверку заново - нужен явный
Resync (или переприсваивание Filter/Filtered/OnFilterRecord на ДЕЙСТВИТЕЛЬНО другое значение - см.
SetFilterText/SetFiltered в MemTableEh.pas, они реагируют только на изменение значения). В ADO-режиме
это уже обеспечено - TCustomMemTableEh.RefreshRecord (используется в TFrDBGridEh.RefreshRecord)
заканчивается вызовом Resync([]) - поэтому там подобной правки не требовалось. Наш собственный
TFrDBGridEh.RefreshRecord (сравнение ID записи до/после) одинаково правильно обрабатывает исчезновение
строки что из-за стандартного фильтра, что из-за любого альт-фильтра - код их не различает, просто
проверяет, осталась ли запись под курсором после Resync.

б) DBGridEh1.STFilter.InstantApply - было False (комментарий в коде "не понял пока за что отвечает").
Прочитав исходники ехlib (DBGridEh.pas): при STFilter.Location = stflInTitleFilterEh (как здесь) именно
это было причиной, что фильтр "по условию" (диалог "Пользовательский фильтр...") не применялся сразу -
соответствующая ветка кода ехlib вызывает SetDataFilter (а значит и наш OnApplyFilter/DefaultApplyFilter)
ТОЛЬКО если InstantApply = True, без всякого запасного варианта. Отсюда и уже существовавшие в проекте
костыли - принудительный DbGridEh1.DefaultApplyFilter по клику в области грида (FLastFilterClick в
DbGridEh1CellMouseClick) и по F3 (DbGridEh1KeyDown, с комментарием "потребовалось, так как поломался
фильтр по условиям"). При этом чекбоксы значений в выпадающем списке (при stflInTitleFilterEh)
применяются по закрытию окна БЕЗ учета InstantApply - то есть реального выигрыша (не дергать фильтр на
каждый клик по чекбоксу) отключение не давало, только ломало диалог условия. Исправлено на True (это в
любом случае было бы значением по умолчанию при Local=True, если явно не задавать False). Оба костыля
(клик по гриду, F3) оставлены как есть на всякий случай - вреда от них при InstantApply=True никакого.

в) UpdateFilterActiveIndicators - единая индикация "фильтр в столбце активен" (жирный шрифт + цвет
ALT_FILTER_INDICATOR_COLOR в заголовке столбца), проверяет сразу все возможные источники фильтра:
Column.STFilter.ExpressionStr <> '' (тот же самый признак, которым уже пользуется существующий
Gh.GridFilterInColumnUsed в uForms.pas, и которым сам ехlib рисует "залитую воронку" вместо пустой
стрелочки в заголовке) ИЛИ наличие поля в FAltNumFilters/FAltDateFilters/FAltDateRelFilters/
FAltColorFilters. "Родной" вид заголовка каждого столбца запоминается лениво в FTitleDefaultFont (по
имени поля, при первом обращении) - чтобы корректно возвращать исходный вид после снятия фильтра,
независимо от того, что конкретная форма изначально задала для Title.Font данного столбца. Вызывается
после RefreshGrid, после DbGridEh1ApplyFilter (стандартный фильтр), после ClearOrRestoreFilter и после
каждого Set/Clear любого альт-фильтра.

г) ClearAllFilters - полный сброс ВСЕХ фильтров грида сразу (стандартный постолбцовый во всех столбцах
и текст SearchPanel - через уже существующую Gh.GridFilterClear, плюс все альт-фильтры - числа/дата/
диапазон дней/цвет, плюс забывает запомненный Ctrl-Q'ом фильтр FLastFilter, чтобы он неожиданно не
восстановился потом). Отдельно от ClearOrRestoreFilter (Ctrl-Q) - та работает только со стандартным
фильтром и умеет его запоминать/восстанавливать, а не сбрасывать полностью. Доступна по Ctrl-Shift-Q
(DbGridEh1KeyDown) и пунктом контекстного меню грида (новая константа mbtClearAllGridFilters = 131 в
uData.pas, добавлена в список пунктов меню рядом с mbtClearOrRestoreGridFilter, если задана опция
myogColumnFilter).

д) FAltDateRelFilters - новый альт-фильтр по дате "диапазон от сегодня" (в днях, могут быть
отрицательными - например, вперед +2 (послезавтра), назад -7 (неделю назад)). В отличие от
FAltDateFilters (фиксированные даты DateFrom/DateTo, вычисленные один раз в момент установки фильтра),
здесь хранятся смещения в днях (DaysForward/DaysBack), а границы пересчитываются заново от текущей
даты (Date) при КАЖДОЙ проверке строки в MemTableEh1FilterRecord - иначе "от сегодня" переставало бы
быть актуальным уже на следующий день. Верхняя граница, как и у обычного FAltDateFilters, не включительно
и увеличена на 1 день - чтобы захватить весь последний день диапазона независимо от временной части
значения поля. На одно и то же поле одновременно не может быть активен и обычный FAltDateFilters, и
FAltDateRelFilters - включение одного явно сбрасывает другой (см. SetAltDateFilter/SetAltDateRelFilter).
В окне ShowAltDateFilterWindow добавлены два поля ввода ("вперед"/"назад", простой TEdit +
StrToIntDef с 0 по умолчанию - без строгой валидации, как и у остальных полей этого окна) и кнопка
"Применить диапазон" (ModalResult = mrAll, ранее в этом окне не использовался). Кнопка "Сбросить фильтр
по столбцу" сбрасывает оба вида фильтра по дате сразу.

е) ИССЛЕДОВАНО (по вопросу пользователя - можно ли сделать полностью свое окно фильтра взамен
стандартного окна ехlib для ВСЕХ столбцов, не только числовых/дат/цвета): прочитаны исходники ехlib
13 (DBGridEh.pas, DBGridEh.DBUtils.pas) - технически возможно БЕЗ правки исходников ехlib: единственный
поддерживаемый рычаг - Column.STFilter.Visible := False (отключает и отрисовку, и попадание кликов в
штатный обработчик для этого столбца целиком), после чего рисовать и обрабатывать клик по кнопке
фильтра в заголовке самостоятельно, показывая свое окно (переиспользуя существующий конвейер -
Grid.SetDataFilter/OnApplyFilter/Column.STFilter.ExpressionStr - чтобы остаться совместимыми с
остальной логикой ехlib). Готового события "перед показом стандартного выпадающего списка" с
возможностью его отменить в ехlib нет - методы, определяющие "это клик по кнопке фильтра"
(GetMouseHitCellState/CheckTitleCellFilterButtonPos), не virtual, поэтому их нельзя переопределить в
потомке. Таким образом, полная замена штатного окна фильтра на свое (список значений с чекбоксами +
кнопка задания условия с проверкой типа поля, взамен нынешнего диалога ехlib, который такую проверку
не делает вообще - отсюда и падения на неверном типе) технически осуществима, но это большая, заметная
для пользователей во ВСЕХ ~30+ формах переделка (меняет обычный клик по кнопке фильтра в заголовке
каждого столбца) - НЕ РЕАЛИЗОВЫВАЛОСЬ в эту сессию, требует отдельного согласования масштаба (сразу
везде или как опция фрейма для постепенного внедрения, какие операторы условия предлагать для текстовых
столбцов, и т.д.) до начала реализации.
