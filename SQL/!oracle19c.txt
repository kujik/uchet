[oracle@srv-oracle-full ~]$ cat .bashrc
# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]
then
    PATH="$HOME/.local/bin:$HOME/bin:/u01/app/oracle/product/19.0.0/bin:$PATH"
fi
export PATH

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# User specific aliases and functions

---Экспорт этих параметров обязателен
--без последнего глюки с русским языком в выводе команд, склплус а также портятся русские слова в коде напр вьюх!
export ORACLE_HOME=/u01/app/oracle/product/19.0.0
export ORACLE_SID=orcl
export NLS_LANG=RUSSIAN_RUSSIA.AL32UTF8

----------------------------------------
НЕ ПОДКЛЮЧАЮТСЯ 11g - клиенты

[oracle@srv-oracle-full admin]$ cat sqlnet.ora
# sqlnet.ora Network Configuration File: /u01/app/oracle/product/19.0.0/network/admin/sqlnet.ora
# Generated by Oracle configuration tools.

NAMES.DIRECTORY_PATH= (TNSNAMES, EZCONNECT)

SQLNET.ALLOWED_LOGON_VERSION_CLIENT = 11    #добавляем для устранения ошибки Протокол не поддерживается
SQLNET.ALLOWED_LOGON_VERSION_SERVER = 11    #добавляем для устранения ошибки Протокол не поддерживается

#SQLNET.AUTHENTICATION_SERVICES = 11
--------------
после добавления строк не можем залогиниться, уже неверный пароль/пользователь, в том числе и с сервера,
но пройдет sqlplus / as sysdba
делаем для существующих пользователей
alter user XXXX password expired;
alter uses XXXX identified by PASSWD;
в том числе делаем для sys, так как он не входит тоже. в итоге под sys и system будет входить


----------------------
--отключепние различия регитстра в паролях
ALTER SYSTEM SET SEC_CASE_SENSITIVE_LOGON=FALSE SCOPE=BOTH;
-- Отключить устаревание пароля
ALTER PROFILE default LIMIT password_life_time unlimited;
-- Отключить период уведомления
ALTER PROFILE default LIMIT password_grace_time unlimited;


---------------------
изменение размера БД

в оракле если файла авторасширение не сделано то просто   увеличь его если сделано дак он сам будет увеличиваться
alter database datafile '/.../....' resize 7g
или
alter database datafile '/.../....' autoextend on maxsize  7g


------------------------
service firewalld stop
Для отключения OL7 firewall «навсегда», так чтобы не стартовал после перезагрузки, run:
systemctl disable firewalld
